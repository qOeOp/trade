# 能力采用

本页把现有交易引擎连接到目标产品架构。它不会新增 Owner，不会声称目标契约已经实现，也不会让 crate 成为权威。全局 Flow 定义业务权威，package 只是权威背后可替换的实现单元。

## 采用规则

- 一个 crate 只有通过相互独立的有类型端口才能支撑多个 Owner。共享代码绝不代表共享写入权威。
- 直接复用表示在目标 Owner 契约后保留有用计算或协议代码，不表示保留当前所有修改路径。
- 基础设施不拥有交易事实。它只能代表已提交事实的 Owner 存储 传输 序列化 观测或暴露事实。
- Flow 命名了目标产品表面或底层 API 可以访问，都不能证明该产品表面已经实现。

## Windmill 架构变更前到目标的 gap 处置

选择 Windmill 闭合了产品壳决策，但没有自动闭合实现合同。本表是规范 gap 审计；
`Product Edge` 页面拥有由此产生的运行规则。

| 本次审计前的 gap                                  | 采用的 Windmill 能力                                       | Trade 必须补建或禁止的内容                                                                                            | 目标证明                                                                      |
| ------------------------------------------------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| App 技术与调用者身份未指定                        | Full‑code React App 与 `viewer` execution policy           | 禁止 public、anonymous 与 publisher execution                                                                         | 浏览器测试证明两个 principal 保持不同权限并到达同一类型化 operation           |
| 已选择 native MCP 但没有 tool boundary            | OAuth/scoped‑token MCP 与 per‑tool selection               | 准确 deny‑by‑default allowlist；禁止 workspace mutation 与 preview tool                                               | 枚举 tool receipt 只包含精选 operation 与最小只读 job tool                    |
| 长时间工作混合了运维真相与业务真相                | Job、progress、log、SSE、flow 与 worker                    | Windmill id/log 只属运维；Owner receipt 与 artifact 才是持久真相                                                      | restart/timeout 测试解析同一 Owner request 且没有第二次业务写入               |
| 周期自动化缺少 due‑slot 和重叠语义                | Schedule 与 flow error handling                            | 稳定 due‑slot request identity；无 lifecycle、deployment 或 live‑runtime authority                                    | 重复及重叠 delivery 汇合到同一 Owner receipt                                  |
| 无人值守身份依赖未指定账户                        | Run‑on‑behalf 与专用 virtual user；可选 EE service account | 每类 workload 使用最小权限身份；禁止共享 admin 或 editor identity                                                     | 分别验证 schedule、App 与 MCP 归因及有效 scope                                |
| Retry 与 recovery 语义停留在 flow success/failure | Flow retry、timeout、error handler 与 resolver job         | 只重试同一 request meaning；未知调用查询 Owner receipt                                                                | 在 Owner 接受前后注入丢失都不会创建裸 successor                               |
| Workspace state 可能变成影子产品数据库            | Data table、resource、variable 与 transient flow state     | 只保存 UI preference 和可重建 cache；不得保存 Owner fact 或持久 artifact 真相                                         | 删除 Windmill cache 后能重建 view 且不丢业务事实                              |
| Deployment 与 upgrade cut 未指定                  | `.raw_app/`、`wmill.yaml`、CLI sync 与 resource version    | Repository‑first source 和准确 server/worker/CLI/image digest compatibility cut                                       | clean deployment 复现已声明 App、operation set、policy 与 rollback target     |
| CE 与 EE 能力混为一谈                             | 自托管 CE base 与可选 EE enhancement                       | 正确性不得依赖 EE‑only service account、Agent Workers、schedule error handler、alert、search、debouncing 或 retention | 所有可选 EE 功能缺失时 CE acceptance suite 仍通过                             |
| 内部 AI 与外部 chat credential 混为一谈           | Windmill AI resource 与 native MCP                         | 两个 credential plane 分离；model key 不得授权 Trade 或泄露到 request/log                                             | Secret‑canary 与 authorization test 证明 provider credential 不会成为业务权威 |

任何一行都不会让 Workbench 变成 `CURRENT`。它们共同把 TARGET 收敛到实现 Agent 无需选择新架构即可
开发的确定程度。如果某个 Windmill 版本无法满足必需行，实现必须停止；不得静默增加第二个 shell、
scheduler、MCP service 或 truth store。

## Strategy Factory 能力处置

Strategy Factory 不是整体一次性迁移。`PRESENT` 表示已直接核实具名来源 facet；`PARTIAL` 表示仅列出的
facet 存在，缺失的目标语义仍未实现；`ABSENT_TARGET_ONLY` 是目标 seam 或禁止规则，不是现有可复用能力。
目标采用处置与来源可用性相互独立，也不会默认为旧 writer 续权。

| 能力                                            | 来源事实                                                                                                                                                                                                                | 目标处置与唯一写入者                                                                                                            | 缺失目标契约 / 首个门禁                                                                                               |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| ResearchIntent                                  | `PRESENT` - 公共 `ResearchIntent` 冻结表示                                                                                                                                                                              | `ADAPT` 到 Research Intent；Research 写入                                                                                       | 对账 identity provenance falsifier typed dimension census budget 与 stop rule                                         |
| TrialFamily                                     | `PARTIAL` - 准确 capability port `strategy-factory-intent-family-counters` 绑定 `crates/strategy_factory/src/intent.rs` 的公共 `IntentFamily`；现有内容只有 pilot‑local artifact‑attempt parameter‑tuple successor 计数 | 只把这些现有 facet `ADAPT` 到 R&D-owned `trial-family-census-frontier`；该 port 是迁移证据而非 writer                           | 增加持久 family 身份 穷尽 append‑only census ancestry 和改名重置防护；禁止源计数器重置 holdout 预算或声称完整 census  |
| StrategyArtifact                                | `PRESENT` - 公共 content‑addressed `StrategyArtifact` 表示与验证                                                                                                                                                        | `ADAPT` 到 Research Strategy Artifact                                                                                           | 复现 bytes dependency/toolchain market semantics capability 与 security admission                                     |
| ProgramHost SDK Wasm                            | `PARTIAL` - 已有 `DecisionContract` `PreparedDecisionRuntime` 和受限 Wasm；没有通用 SDK 或独立 Product Host                                                                                                             | `ADAPT` 到 Research Artifact Security Admission 后                                                                              | 证明能力有界 process‑tree 禁止 确定失败及无账户效果权威                                                               |
| Trial receipt                                   | `PARTIAL` - 准确现有 port `strategy-factory-trial-receipt` 绑定 `crates/strategy_factory/src/receipt.rs` 的公共 `TrialReceipt`；冻结 pilot 回执存在，但缺少完整规范结果契约                                             | `MIGRATE_PRESENT_FACET_ONLY` 到 Backtest `exploratory-result`；只有 Backtest 写 Run Result                                      | 增加规范 request‑result correlation 及完整负例与 unknown disposition；来源回执不得取得 Research 或 Qualification 权威 |
| Formation receipt                               | `ABSENT_TARGET_ONLY` - 已检查 `crates/strategy_factory/src/receipt.rs` 与 `crates/strategy_factory/src/lib.rs`；不存在 Formation Receipt 或 formation request‑result port                                               | 为 Research `strategy-artifact` 执行 `DEFINE_TARGET_CONTRACT`；不存在可复用来源 facet                                           | 实现前定义 Formation/build correlation 和完整负例/unknown disposition                                                 |
| Qualification receipt                           | `ABSENT_TARGET_ONLY` - 不存在 Candidate Intake Protected Attempt Eligibility 或 holdout‑custody 来源                                                                                                                    | 在 Qualification 下 `DEFINE_TARGET_CONTRACT`；当前无代码可迁移                                                                  | 实现单向保护托管且不存在细节反馈路径                                                                                  |
| Research Status                                 | `ABSENT_TARGET_ONLY` - 不存在 global status Iteration Decision Selection 或 Research View 来源                                                                                                                          | 在 Research 下 `DEFINE_TARGET_CONTRACT`；不存在可替换的旧权威                                                                   | 实现明确终态 unknown 并分离 Research Qualification Governance 事实                                                    |
| R&D 产品工作台                                  | `ABSENT_TARGET_ONLY`；已选择 Windmill，但不存在公共 Product Edge Research View、Run Detail/Compare 或从 Source 到 Decision 的类型化用户动作；当前来源只暴露窄 Strategy Factory pilot                                    | 在 Product Edge 以同一组 Windmill App 与 MCP operation 通过 R&D Backtest Qualification Owner port 执行 `DEFINE_TARGET_CONTRACT` | 实现可见旅程 回执 未解析状态和准确 Qualification 交接；Windmill orchestration 不得成为 workflow truth writer          |
| Data adapters 与 admission                      | `PARTIAL` - 公共 `run_frozen_pilot` 集成固定 Binance Vision 加载与有界规范化；私有 `data.rs` 不是公共端口                                                                                                               | `SPLIT_AND_ADAPT` 到 Market Data 后                                                                                             | 增加通用 source binding 四时间 PIT license 与 correction frontier                                                     |
| Backtest reuse                                  | `PARTIAL` - 只有公共 `run_frozen_pilot` integration 并返回权威 receipt；没有通用探索端口或保护隔离                                                                                                                      | `ADOPT_THROUGH_OWNER`；Backtest 写结果                                                                                          | 证明规范 request‑result equality 和探索保护 namespace 隔离                                                            |
| Matching contract                               | `ABSENT_TARGET_ONLY` - 不存在 matcher due‑slot schedule 或完整 Scanner Receipt 来源                                                                                                                                     | 在 Scanner 下 `DEFINE_TARGET_CONTRACT`                                                                                          | 实现 proposal‑only 匹配且无隐藏激活或 scheduler 权威                                                                  |
| Portfolio contract                              | `ABSENT_TARGET_ONLY` - Strategy Factory 中不存在 Performance Capacity Interaction receipt 来源                                                                                                                          | 在 Portfolio 下 `DEFINE_TARGET_CONTRACT`                                                                                        | 实现来源截面投影且不拥有分配或 Risk headroom                                                                          |
| Protected feedback                              | `ABSENT_TARGET_ONLY` - 不存在保护结果边界 opaque frontier 或 holdout ancestry 来源                                                                                                                                      | `DO_NOT_CREATE_DIRECT_PATH`；Qualification 仍是唯一保护 writer                                                                  | 证明没有可解引用保护结果返回 R&D                                                                                      |
| Scheduler deployment registry service authority | `ABSENT_TARGET_ONLY` - 不存在 scheduler registry 或 deployment service 来源                                                                                                                                             | `DO_NOT_CREATE_SECOND_AUTHORITY`；原生事实仍归 Scanner Governance Runtime                                                       | 不创建第二 scheduler registry lifecycle manager 或 deployment service                                                 |

因此 `IntentFamily` 类型与 `strategy-factory-intent-family-counters` port 是同一条明确 source-to-target
link，不是两种能力。Research 仍是 TrialFamily/Census 唯一 writer，Qualification 只读；在缺失目标 facet
实现前，源计数器不能证明穷尽 membership ancestry 改名安全或 holdout custody。

## R&D 产品表面能力处置

下列状态分类的是目标产品范围与优先级，不是当前实现。每个 `TARGET_REQUIRED` 与
`DEFERRED_TARGET` 行都保持 `ABSENT_TARGET_ONLY`，直到规范 consumer 满足 Product Edge 验收契约。
`NOT_ADMITTED` 是产品禁止项，并不声称工程工具或策略源码不存在。

| 能力                                                                   | 状态              | 采用契约                                                                                                                                                          |
| ---------------------------------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 通过 Windmill App 或 Windmill MCP 的自然语言 Agent 创作                | `TARGET_REQUIRED` | 只能提交已接纳的 R&D 类型化 operation；对话与 Agent 输出不是业务事实                                                                                              |
| Conversation 与 R&D Execution Agent 分离                               | `TARGET_REQUIRED` | 把客户端交互与持久服务端 R&D job 分开；即使两个角色使用同一已配置 provider 或计费 gateway，MCP 也不传递模型 session 或 credential                                 |
| Research Source Intake 应用表面                                        | `TARGET_REQUIRED` | 通过共同 Product Edge 调用可替换、带版本的 connector flow；展示获取终态、provenance、解释、triage 与 Research Queue，不建立 connector 专属 MCP 或直接 Intent 路径 |
| Research run、时间线、Agent 活动、迭代进度、日志与允许动作             | `TARGET_REQUIRED` | 组合 R&D 回执、Research View 与运维进度，但不建立影子 workflow 事实                                                                                               |
| 结构化 Artifact Review                                                 | `TARGET_REQUIRED` | 无需原始源码即可展示 Artifact identity 与血缘、策略逻辑摘要、参数、依赖、构建状态、允许的探索引用、语义变更解释和允许动作                                         |
| 探索比较、有界 Qualification 状态、Scanner、Runtime 与 Operations 视图 | `TARGET_REQUIRED` | 保留各原生 Owner 与保护反馈边界；Windmill Job 成功绝不是业务成功                                                                                                  |
| 完整源码查看                                                           | `DEFERRED_TARGET` | 以后若准入，只能把不可变 Artifact 源码作为高级审计表面只读展示                                                                                                    |
| 源码级 diff、受控下载与源码关联诊断                                    | `DEFERRED_TARGET` | 从准确不可变 Artifact identity 派生，绝不产生修改路径                                                                                                             |
| Notebook‑first 产品创作或内嵌代码 IDE                                  | `NOT_ADMITTED`    | 外部工程工具留在产品契约之外，不能满足 Workbench 验收                                                                                                             |
| 原地编辑 Artifact 或覆盖版本                                           | `NOT_ADMITTED`    | 每个已接纳修改都创建新的受治理 attempt 和不可变后继 Artifact，或产生原生终态 disposition                                                                          |

## 能力映射

| 现有 crate 或能力                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 目标归属                                                     | 采用契约                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 基础设施门面                                                 | 保留 `vibe-trader` 容器作为稳定 package 门面。它导出类型和入口，但不拥有可变业务事实。                                                                                                                                                                                                                                                                                                                                                    |
| `crates/data`, `crates/model`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Market Data 以及共享领域类型                                 | 在 Data Engine 和 Instrument Master 后复用标准化 订阅 行情记录和标的类型。Model 类型定义表示形式，但其 crate 不拥有订单 账户或生命周期状态。                                                                                                                                                                                                                                                                                              |
| `crates/adapters/architect_ax`, `crates/adapters/betfair`, `crates/adapters/binance`, `crates/adapters/bitmex`, `crates/adapters/blockchain`, `crates/adapters/bybit`, `crates/adapters/coinbase`, `crates/adapters/databento`, `crates/adapters/deribit`, `crates/adapters/derive`, `crates/adapters/dydx`, `crates/adapters/fred`, `crates/adapters/hyperliquid`, `crates/adapters/interactive_brokers`, `crates/adapters/kraken`, `crates/adapters/lighter`, `crates/adapters/okx`, `crates/adapters/polymarket`, `crates/adapters/sandbox`, `crates/adapters/scheduled_events`, `crates/adapters/tardis` | Market Data Data Clients Execution Adapters                  | 使用下方逐 provider 有类型 inventory。标为 `ABSENT` 的 port 是 Stop，不能靠另一 port 推断。`fred` 与 `scheduled_events` 是离线受托管来源读取器，没有 effect port。`sandbox` 是 `PAPER` Execution Scope 选择的模拟 Execution Adapter，绝不是历史 Backtest Sim Exchange。Adapter 保留来源身份和场所语义，但不能写 Owner 投影，也不能绕过 Risk 和 Execution。                                                                                |
| `crates/persistence`, `crates/persistence/macros`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Owner 范围内的持久化基础设施                                 | 在 Owner repository 后复用数据库和对象存储机制。Persistence 不是全局业务权威，也不能把不同 Owner 的事实合并为一个可写真相。                                                                                                                                                                                                                                                                                                               |
| `crates/event_store`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Owner 范围内的审计和重放基础设施                             | 通过 PIT Catalog Run Result Runtime 内部 checkpoint 持久化 Effect Journal 和 Recovery Case 的追加端口拆分捕获 快照 验证和重放。只有 Runtime 暴露 Strategy Instance checkpoint readiness 与 Incident 事实；只有 Execution 暴露 Effect Journal Reconciler 与 Recovery Case 状态。Event Store 不是 Event Rail，也不能成为第二套 ledger 或闭合权威。捕获 扫描或隔离状态不确定时必须围栏受影响的 Runtime generation，直到原生 Owner 完成对账。 |
| `crates/indicators`, `crates/indicators/kernel`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | R&D 所有的 Develop 能力 Backtest 和 Runtime 消费者           | 将原生 wrapper 与纯可移植数值 kernel 以及参数 依赖和版本绑定进 Strategy Artifact。Native Replay 和受治理 Strategy Instance 消费同一个身份，paper 或 live 不能替换为没有回执的实现。                                                                                                                                                                                                                                                       |
| `crates/analysis`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Backtest Run Result Portfolio Performance Qualification 证据 | 将确定性统计作为 Owner 事实上的只读计算复用。Analysis 可以产生证据和投影，但不能授予资格 分配资金或写入账户状态。                                                                                                                                                                                                                                                                                                                         |
| `crates/strategy_factory`, `crates/strategy_factory/programs/sdk`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Strategy Factory 边界                                        | 复用窄范围 intent artifact 数据准入 native replay receipt 和受限 program ABI 纵切。no‑std SDK 只定义传输和 guest program 类型；它不授予 host 进程 账户或 effect 权威。Strategy Factory 不能成为第二套 Research registry Backtest engine Qualification 权威 调度器或部署服务。                                                                                                                                                             |
| `crates/operator_authorization`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Operator Authorization Issuer                                | `CURRENT` 仅为密封的 PostgreSQL issuance 与 revocation‑frontier 契约。Issuer 是这些事实的唯一 writer；Product Edge 只消费其直接加锁 read contract，不能签发或撤销。R&D、Strategy Factory、Windmill、API transport、credential 与配置都不取得业务权威或 authorization 写权威。在集成动态 gate 通过前，product runtime 与 Windmill acceptance 保持 `TARGET` 且 `NOT_ADMITTED`。                                                             |
| `crates/product_edge`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Product Edge 边界                                            | `CURRENT` 仅为内容寻址 operation manifest、deployment binding 与 history head、不可变 request admission 及其 outbox 的密封 PostgreSQL 契约。它单向消费 `crates/operator_authorization` 并直接解析 Issuer cut；它不向 R&D 或其他业务 Owner 授权。在真实 consumer 与动态 gate 通过前，Strategy Factory 和 Windmill 集成、restart/cache‑loss recovery 与产品验收保持 `TARGET` 且 `NOT_ADMITTED`。                                            |
| `crates/product_edge_admin`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 显式 authority bootstrap composition                         | `CURRENT` 是一个 opt‑in 管理可执行单元，仅为显式 genesis 请求组合具体 Operator Authorization Issuer 与 Product Edge Owner。它不拥有任何事实，从不作为 service‑start 或 request‑path 默认，也不能把任一 Owner 的写权威转给 Strategy Factory、R&D、Windmill 或 transport。Runtime bootstrap/recovery 验收仍为 `TARGET`；自动 genesis、业务权威、provider invocation 与交易仍为 `NOT_ADMITTED`。                                             |
| `crates/qualification`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Qualification Owner                                          | `CURRENT`：拥有 sealed protected‑feedback projection、head、原始 outbox 与 incident‑specific recovery receipt 托管。`TARGET`：只能通过 Qualification 原生的规范 intake、protected‑attempt 与 eligibility 契约扩展。`NOT_ADMITTED`：运行时或产品准入、续期有效性、直接 protected‑detail 路径、新 domain wake，以及对 R&D、Product Edge 或 Windmill 的权威。                                                                                |
| `crates/backtest`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Backtest                                                     | 在 Native Replay Sim Exchange 和 Run Result 后复用确定性引擎 模拟交易所 时钟和结果机制。增加保护评估隔离时不得分叉交易语义。                                                                                                                                                                                                                                                                                                              |
| `crates/trading`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Runtime Strategy Instance 以及 Execution Order Engine 迁移   | 将策略和 actor 执行保留在 Runtime 后。执行算法 订单初始化和拒单写入必须迁移或封装到 Execution 后，使 Order Engine 成为订单生命周期唯一写入者。                                                                                                                                                                                                                                                                                            |
| `crates/risk`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Risk                                                         | 在 Risk Engine 后复用检查 限流 sizing 输入 halt 和 reduce 机制。使用明确终态 Risk Decision 一次性 Risk Reservation 和有范围 Kill Switch fence 取代原始命令转发。                                                                                                                                                                                                                                                                          |
| `crates/execution`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Execution                                                    | 在 Order Engine Effect Journal Reconciler Recovery Case 和 Execution Adapters 后复用订单路由 模拟 对账和 client 管理。正常效果必须绑定匹配许可，恢复效果必须绑定生效 case 和 fence。Execution 不写 Runtime readiness 或 checkpoint。                                                                                                                                                                                                      |
| `crates/portfolio`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Portfolio                                                    | 将估值 账户 持仓 PnL 和暴露计算复用为已提交 Execution 与 Market Data 事实的投影。Portfolio 不能继承场所效果或资金分配权威。                                                                                                                                                                                                                                                                                                               |
| `crates/system`, `crates/live`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Runtime 组合基础设施                                         | 复用 kernel controller trader 和 live node 装配来组合 Owner 端口。Runtime 拥有 Strategy Instance checkpoint readiness fence 与 Incident 事实，但不拥有 Recovery Case Recovery Command 或闭合。组合代码不能保留全局交易权威，也不能写入其他 Owner 的事实。                                                                                                                                                                                 |
| `crates/common`, `crates/core`, `crates/infrastructure`, `crates/network`, `crates/serialization`, `crates/cryptography`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 基础设施                                                     | 在 Owner 契约后保留时钟 标识符 消息传输 cache 机制 网络 codec 和密码学能力。通用 MessageBus 不是 Event Rail，共享 Cache 必须拆成按事实划分的写入门面。                                                                                                                                                                                                                                                                                    |
| `crates/pyo3`, `python/vibe_trader`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 语言和访问边界                                               | 将 Python binding 复用为相同 Owner 端口和 Strategy Artifact 依赖的有类型入口。Python 可访问性不能创建另一个状态写入者，也不能绕过准入和风控。                                                                                                                                                                                                                                                                                             |
| `crates/cli`, `crates/plugin`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 产品访问和扩展基础设施                                       | 只在存在明确消费者时保留 CLI 和扩展机制。它们不能证明 Windmill App、Windmill MCP operation set、外部对话客户端、Telegram 或通用 plugin marketplace 已经实现或准入。                                                                                                                                                                                                                                                                       |
| `crates/testkit`, `examples/tutorials`, `tests`, `scripts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 验证和参考基础设施                                           | 将 fixture 确定性 harness example 和仓库检查作为证据复用。测试辅助和示例本身不能成为生产权威或产品能力。                                                                                                                                                                                                                                                                                                                                  |

### 当前密封边界

- `crates/deployment_attestation` → **Strategy Factory 部署验证。** 只在 executable 使用边界复用密封的固定策略 verifier。其 evidence 是 consumer 输入，不是部署权威或业务事实。
- `crates/observability` → **Observability 非权威边界。** 保留基于规范 Owner 记录的只读、可重建投影。它不拥有来源事实、command、retry、终态决策或交易权威。
- `crates/runtime` → **Runtime foundation。** 保留非权威的 `NOT_READY` 状态与精确 revalidation dependencies。规范 Runtime custody、generation、checkpoint、readiness、deployment 与 effects 仍为 `TARGET`。
- `crates/scanner` → **Scanner Owner。** 采用 fail-closed Scanner core 生成密封的 attempt admission 与 receipt。time 或 source authority 不可用时在 attempt admission 前失败；sealed admission 后 membership 不可用时提交终态 `Failed(MembershipUnresolved)` receipt，并抑制 matcher、proposal 与下游效果。
- `crates/strategy_governance` → **Strategy Governance Owner。** 采用静态 fail-closed Governance core 生成精确 authorization 与 lifecycle receipts。adapter evidence 在规范 Owner 回读前保持不可信，authorization 也不证明 Runtime 已应用。
- `crates/product_edge_claim_custody` → **Product Edge provider invocation custody。** `CURRENT` 是 claim 与 start custody 的 Owner 内部存储和加锁解析 seam。caller 记录仅是 proposal；provider 执行与交易仍为 `NOT_ADMITTED`。
- `crates/rd_artifact_invocation_custody` → **R&D invocation reservation custody。** `CURRENT` 针对精确 Product Edge custody 密封并解析一个 R&D-owned reservation，不转移任一 Owner 权威。provider 执行与交易仍为 `NOT_ADMITTED`。
- `crates/strategy_factory_rd_owner_api` → **R&D 与 Product Edge API composition。** `CURRENT` 只组合现有 Owner ports，不拥有事实，也不把 transport、credential、配置或 provider output 变成权威。生产写与交易仍为 `NOT_ADMITTED`。

## Provider 有类型端口证据

`PRESENT` 表示已检查列出的公共来源 port。`ABSENT` 表示已检查该 member 且不存在该 port，不能通过
另一 port 实现。`UNVERIFIED` 表示采用必须停止，直到检查到来源 locator。Market Data port 首先进入
`relation:data-research`；效果 port 首先进入 `relation:runtime-execution`，并始终受 Risk 与 Execution
准入约束。

| Provider member                       | Market Data source port                                   | Execution effect port                                     |
| ------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------- |
| `crates/adapters/architect_ax`        | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/betfair`             | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/binance`             | `PRESENT` - `src/futures/data.rs`                         | `PRESENT` - `src/futures/execution.rs`                    |
| `crates/adapters/bitmex`              | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/blockchain`          | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution/mod.rs`                        |
| `crates/adapters/bybit`               | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/coinbase`            | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/databento`           | `PRESENT` - `src/data.rs`                                 | `ABSENT` - 已检查 `src/lib.rs`；`STOP_SOURCE_PORT_ABSENT` |
| `crates/adapters/deribit`             | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/derive`              | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/dydx`                | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution/mod.rs`                        |
| `crates/adapters/fred`                | `PRESENT` - `src/lib.rs`                                  | `ABSENT` - 已检查 `src/lib.rs`；`STOP_SOURCE_PORT_ABSENT` |
| `crates/adapters/hyperliquid`         | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/interactive_brokers` | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution/mod.rs`                        |
| `crates/adapters/kraken`              | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution/mod.rs`                        |
| `crates/adapters/lighter`             | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/okx`                 | `PRESENT` - `src/data.rs`                                 | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/polymarket`          | `PRESENT` - `src/data/mod.rs`                             | `PRESENT` - `src/execution/mod.rs`                        |
| `crates/adapters/sandbox`             | `ABSENT` - 已检查 `src/lib.rs`；`STOP_SOURCE_PORT_ABSENT` | `PRESENT` - `src/execution.rs`                            |
| `crates/adapters/scheduled_events`    | `PRESENT` - `src/lib.rs`                                  | `ABSENT` - 已检查 `src/lib.rs`；`STOP_SOURCE_PORT_ABSENT` |
| `crates/adapters/tardis`              | `PRESENT` - `src/data.rs`                                 | `ABSENT` - 已检查 `src/lib.rs`；`STOP_SOURCE_PORT_ABSENT` |

## 共享状态迁移

现有共享 Cache 是迁移表面，不是未来 Owner。只有 Market Data 写入标的和行情事实，只有 Execution 写入原始订单 成交 费用 场所和 Recovery Case 事实，只有 Portfolio 写入账户和暴露投影，只有 Runtime 写入 generation checkpoint 和 readiness。其他消费者只取得只读视图。

通用 MessageBus 可以承载命令和未提交内部流量。Event Rail 是更窄的投影，只发布已经提交的 Owner 事件。投递 重试和重放都不能替代来源 Owner 事实或其终态。

## Observability 能力处置

现有 MessageBus 与 Event Store 是可复用机制，不是 Observability 权威。MessageBus 只能放在 typed Owner port 与 Event Rail 后；Event Store 只能放在 Owner-scoped append/read API 与 transactional outbox 边界后。不能从 crate 名称推断现有 logging、tracing、metrics、collector、broker durability、Dashboard 或 OpenTelemetry 能力已经实现。

产品闭环不是 Observability 能力。Windmill Dashboard、trace 或 alert 可以解释当前阶段，但不能闭合用户
旅程、选择下一动作或替代原生 Owner 回执。因此在 Windmill App 与 MCP consumer 实现相同的上述类型化
请求与视图旅程前，选定的 Windmill R&D Workbench 保持 `ABSENT_TARGET_ONLY`。

目标 Telemetry Gateway 在一个可替换端口后接纳 OTLP-compatible receiver、processor 与 exporter。目标 Status Projection 幂等消费已提交 Event Wake 与策略接纳 telemetry，保存 checkpoint 并支持重建。Alert Routing 封装 Telegram 与未来适配器，但投递不是业务结果。Dashboard API 只提供 Global Status View。Kafka、NATS、Redpanda、OpenTelemetry Collector、ClickHouse、PostgreSQL 与 vendor backend 都保持实现候选，不是预选产品依赖。

## 权威迁移门禁

1. **订单唯一写入者** - Strategy Execution Algorithm Risk 和 adapter 不能在 Execution Order Engine 外写入拒单或订单生命周期状态。
2. **许可绑定效果** - 对新增风险，Runtime 必须先取得明确终态 Risk Decision 和一次性 Reservation，再创建 Authorized Order Command。
   decrease-only 改为要求准确 `PERMIT_DECREASE_ONLY` 以及明确空
   Reservation/claim lineage。两条路径都必须在调用前经过 Execution `PREPARED`、Risk adapter admission
   和匹配 `ADMITTED_ONCE`。
3. **Owner 范围内持久化** - Event Store 和 persistence 只能通过原生 Owner 边界追加。不确定的捕获或重放状态必须保持未知并激活对应恢复围栏。
4. **工件身份复用** - 指标 特征 策略 配置和依赖身份冻结在 Strategy Artifact 中，并在 replay paper 和 live Runtime 保持相同。
5. **产品表面与遥测不能靠暗示实现** - Windmill App、Windmill MCP operation set、外部对话客户端、Telegram、OTLP collection、broker 与 Dashboard 必须等到 canonical consumer 满足契约后才成为实现能力。App 与 MCP channel 共用一个 `WINDMILL_PRODUCT_EDGE` 准入网关，不能成为竞争 writer。每个写请求准入绑定权威 shell history head 及其中唯一 `ACTIVE` binding；已准入工作保持原不可变来源绑定。本地安装或 Flow 中出现它们都不是实现证据。

## Owner 迁移包络

迁移每次只处理一个事实表面和一个 Owner。每个切片绑定迁移身份、事实表面、前驱与后继 revision、
现有写入者与读取者、目标 Owner、依赖、共同证据截面、准入阶段、回滚处置、事故 Owner，以及写入者
身份、围栏、差异、未解析效果、对账和来源新鲜度观测。本包络记录必需证据，不表示实现进度。

概念阶段固定为 `UNSTARTED → SHADOW_READ_ONLY → PREDECESSOR_FENCED → RECONCILED →
SUCCESSOR_ACTIVE → VERIFIED`。任何时刻最多一个写入者；没有、未知或多个写入者都不能写入，允许
存在围栏停机窗口。后继激活前必须证明前驱已围栏、依赖完整且共同截面对账一致。

后继已经写入事实后，只有证明反向兼容和准确对账才能回滚。否则表面保持围栏并向前恢复。旧的
进行中请求在切换后按原身份加入原 Owner 回执，不能创建第二外部效果。影子遥测不能覆盖过期、
不匹配或不可用的权威 Owner 事实。

| 迁移切片                                | 目标权威或托管边界          | 后继激活证明                                                                                      | 回滚边界与事故权威                                                                                                       |
| --------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 行情与标的事实                          | Market Data                 | 来源谱系 新鲜度 修正前沿 许可范围和共同 PIT 截面对账一致                                          | 后继事实不能丢失或改写；Market Data 拥有迁移准入和差异事故。                                                             |
| 订单与外部效果                          | Execution                   | 前驱已围栏，准确许可绑定效果集 场所回读和对账截面闭合                                             | 后继产生效果后，回滚需要反向读取证明和准确场所对账；否则由 Execution 向前恢复。                                          |
| 账户 暴露和表现投影                     | Portfolio                   | Execution 与 Market Data 来源截面汇合为同一账户投影                                               | 投影不能成为场所或行情真相；Portfolio 拥有投影迁移事故。                                                                 |
| 策略 generation checkpoint 与 readiness | Runtime                     | 前驱 generation 已围栏且 checkpoint 重放对账一致                                                  | 后继不能通过前驱状态恢复；Runtime 拥有迁移事故但不拥有 Recovery 闭合。                                                   |
| Recovery Case 与闭合                    | Execution Reconciler        | Runtime readiness 活动 Risk fence Effect Journal 回读 Risk closure Portfolio closure 共享一个截面 | 不完整或未知 case 不能成为 `KNOWN_CLOSED`；闭合不恢复交易。                                                              |
| Agent Shell 请求来源                    | Product Edge 非业务托管边界 | 前驱 Shell 已围栏，后继 binding 生效，全部已准入进行中请求按原 binding 可解析                     | 反向切换不能改写请求身份 授权或 Owner 回执；Product Edge 只拥有 binding 和 Shell 切换事故，不拥有 Owner 回执或业务转换。 |

迁移业务 Owner 或边界表面声明的非业务托管方拥有阶段准入和差异处理。边界托管方不会因此获得
业务事实或回执权威。未知交易效果必须停止迁移，并进入现有 Runtime/Risk/Execution Recovery Case
路径；达到 `KNOWN_CLOSED` 前不能继续。一个切片失败不会促进或回滚无关切片，也不会产生全局
权威转移。

发现双重或未知写入者、证据过期或缺失、差异漂移、未解析效果、状态不兼容、消费者读取失败或
事故权威不可用时，迁移必须停止。

## 开发使用方式

修改现有 crate 前，先确定其所在行 目标 Owner 允许的写入者和迁移门禁，再填写一个
[开发切片契约](../guide/development-chunk-contract/)。保持这些契约的改动可以自由选择内部数据结构
进程拓扑 协议和性能策略。移动业务事实 创建第二写入者或绕过门禁的改动，必须先完成架构变更再开始实现。
