# Tech Spec

当前阶段不支持写入该层文档。

原因：

- 现在还没有进入“具体实现”阶段
- 如果在 `vision` 与 `prd` 未稳定时提前写接口、schema 或算法细节，容易把未确认方向误写成既定实现

当前约束：

- 不在本文件写 API 设计
- 不在本文件写数据库 schema
- 不在本文件写接口协议
- 不在本文件写状态机、算法或错误处理细节

待上层文档收敛并进入实现准备阶段后，再开启这一层。
