---
title: R&D Module Audit
role: research-audit-log
status: audit-log
owner: research-strategy-development
last_verified: 2026-07-22 CST
---

# R&D Module Audit

- 2026-07-22：M5 `historical_artifact_read_migration_certified` gate 已关闭，Replay 保持 M4、M5 进度为 `3/9`。Compatibility certification 冻结 P10 reallocation、P11 two-cycle、P13 sequence-accounting 三种 exact-v1 Artifact Manifest；版本化 reader 只按 capability↔schema 固定映射读取，复用旧合同验证 role 顺序、commit marker、Result/Evidence identity 与 manifest self-hash，并只返回 `readable-legacy-no-write` identity/integrity projection。Fixture pack 自哈希，schema、role、manifest、projection 任一篡改均 fail closed；maturity checker 直接执行 pack，布尔值不能脱离证据置真。该认证不恢复旧 writer，不写当前 Result v53 / Artifact v55，不重解释经济语义。限制明确：fixture 为不含 payload bytes 的 synthetic manifests，故 payload rehydration、生产历史全集与跨版本经济升级尚未认证；下一门是 crash recovery / exactly-once publication。
- 2026-07-22：M5 `cross_process_reproducibility_bundle` gate 已关闭，Replay 保持 M4、M5 进度为 `2/9`。唯一 certification owner 冻结 canonical single-position fixture、member script、四个 public entrypoint 与 exact owner assertion 的源码 SHA-256；每次 `bun run reproducibility` 先并发启动两个 fresh Bun process，要求相同 input hash 直接产生相同 canonical Result hash，再为 single-trial、independent-lane-batch、integrated-portfolio、terminal-aware-bounded-cycle 各启动两个 fresh process，要求同一冻结语义断言均通过。Receipt 记录 runtime、distinct PID、Result/断言 hash 和 limitations 并自哈希；source/bundle 漂移在 process launch 前 fail closed。Resume 没有被夸大：single Trial 为 direct engine checkpoint，independent Batch 仅委托 child Trial，两个 Portfolio profile 仍为 `explicit-not-supported`。该 gate 不认证 cross-host/cross-runtime parity，不替代后续 release fixture pack，也未新增 simulator capability。
- 2026-07-22：Replay 的最后一个 M4 gate 已关闭，成熟度由 M3 原子切换为 **M4**。唯一 certification owner 现在用 TypeScript AST 扫描 `modules/` 下非测试、非认证的静态 import/export/import()：23 个 Replay package 全部归入 canonical runtime、canonical certification、compatibility runtime、compatibility certification 或 certification owner；66 条实际生产依赖边的消费者全部归入 Replay canonical/compatibility runtime、Research Control Plane、Forward Evidence Plane 或 Agent Roles。注册表冻结分类计数与完整闭包 SHA-256；新增 package、跨域消费者、依赖边或 package 名漂移必须显式更新并通过 owner test 与 repository quality judge。该 closure 是当前依赖债务的可审计快照，不把 canonical→legacy 等现状升级为目标制度。M4 九门现为 `9/9`，M5 的 `m4_exit_complete` 同 change set 置真；下一阶段只能做固定 M5 release certification，继续禁止 P30 与新 simulator capability。
- 2026-07-22：P10/P11/P13 compatibility 回归已迁入 `certification/legacy-portfolio-cycle-certification`：独立验证 reallocation→two-cycle、三周期 P13 consolidated accounting、幂等、Artifact、fail-closed 与 Trial Balance 篡改。Canonical runner 巨型测试删除重复 P10/P11/P13 coverage，并删除唯一 runner→compatibility test-only boundary exception；P12/P17/P18 等 canonical/opt-in successor 回归保持原 owner。
- 2026-07-22：Replay 已停止 Pxx treadmill，正式进入固定 `M4-CONVERGENCE`。机器 gate 升为 v2，`M4-P30` 与新 simulator capability 均 forbidden；成熟度不再由 commit/schema/successor/test 数量驱动，只由封闭的九项 M4、九项 M5 exit gates 驱动。P1–P29 inventory 已逐项冻结并由 checker 验证：14 canonical、12 explicit opt-in、3 compatibility、0 obsolete；P12 因仍被 terminal-aware/opt-in sequence 复用而归 canonical internal primitive。四个 canonical Runner 公共入口与全部 12 个 opt-in activation owner/export 已登记并验证。P10 fixed cycle-2、P11 fixed two-cycle 与 P13 legacy sequence accounting 的执行消费者已物理迁入 `compatibility/legacy-portfolio-cycle`，回归只由独立 certification 单向消费；canonical tests 的反向边已删除。通用 writer epoch 已冻结为 Result v53、Artifact v55、Engine Checkpoint v32、Diagnostic Commit v2、Terminal Checkpoint v1 与 Run Outcome v35。唯一 `replay-certification` owner 逐一分类 9 个 canonical、13 个 compatibility suite，并以 `bun run certify` 顺序 fail-fast 执行全部 owner checks；同一 owner 的 profile evidence registry 将四个 public profile 逐项绑定到 golden、resume、idempotency、tamper 测试。Resume 语义不伪造：single Trial 直接认证跨进程 checkpoint，independent Batch 委托 child Trial，Integrated Portfolio 与 Terminal-aware Bounded Cycle 固定为 no checkpoint writer。Checker 同时拒绝第二个 `certify`、漏登记 package、分类漂移、canonical test 反向 import 与 profile evidence/checkpoint-mode 漂移。该记录完成时 M4 为 `8/9`；随后 module/production-consumer closure gate 已补齐并切换 M4，未创建 P30 fidelity gap。
- 2026-07-22：M4-P29 在 `6/6` 预算内完成，成熟度仍为 M3。正式 `runReplayTrial` 现在只在显式 sidecar 且 Request、Dataset、单根 bar、Control Plane Path Authority、Kline↔Aggregate Trade Bar Link、Coverage/Event 全部精确闭合时，使用 ordered aggregate prices 决定初始 Stop-market 入场后同根 bar 的 stop/target owner；触发入场的 trade 不参与保护观察。Checkpoint v32、Result v53、Fingerprint 与 Artifact v55 均绑定同一 Authorized Path Step hash，幂等读回逐文件验 hash/内容；clean/resume `result_hash` 相同，重算 checkpoint hash 后删除 authority binding、或篡改已提交 Step payload 均 fail closed。缺少/unlinked sidecar 继续返回既有 `stop-entry-same-bar-path-ambiguous` 且无 partial Result；默认 OHLCV、Portfolio 和 P15–P28 全量回归保持通过（Contracts 32、Engine 91、Runner remaining 98）。该证据仍固定 `external_completeness=not_verified`，aggregate trades 无 Fill quantity/cost/slippage/impact authority，也未进入通用 SourceEvent stream。P29 到此封口；下一步不再自动创建 P30，而是建立有限 M4/M5 exit gate 并清点 P1–P29 的 canonical/opt-in/compatibility/obsolete 状态。
- 2026-07-22：M4-P29 第三纵切完成，成熟度保持 M3，进度 `3/6`。Replay Engine 新增有界 `Authorized Stop-entry Path Step v1`，Runner 仅通过显式 `explicit_opt_in_pre_result_binding` 消费。输入必须联合闭合 exact Request、Dataset、单根 MarketBar、Control Plane Path Authority、Kline↔Aggregate Trade Bar Link、Coverage 与 ordered Events；任一 Request/Order/Dataset/bar/window/PIT/coverage/events/hash 漂移 fail closed。Step 复用 certification-only exact-price resolver，只决定初始 Stop-market 入场后同根 bar 的 stop/target owner；观察从触发入场的 aggregate trade 之后开始，该笔成交即使越过保护价也不能追溯触发。long/short golden 均证明相同 OHLC 可因成交顺序不同得到相反 owner。aggregate trades 仍无 Fill quantity/cost authority，`external_completeness=not_verified`；Runner outcome 固定 `result_published=false`、`artifact_published=false`、`checkpoint_bound=false`，尚未接入 `runReplayTrial`，因此默认 OHLCV typed unresolved、Checkpoint、Result、Artifact、Portfolio 与 P15–P28 都未改变。下一纵切必须把该 Step 绑定到 Checkpoint/Result/Fingerprint/Manifest 后才能产生经济 Result。
- 2026-07-22：M4-P29 第二纵切完成，成熟度保持 M3，进度 `2/6`。Research Control Plane 新增 immutable `Bar-linked Aggregate Trade Path Authority v1`，不修改 Trial Reservation v9、父 Aggregate Trade Evidence/Cross-source Ordering Admission、既有 Projection/Wire 半开窗口或 Replay Request v38。State Store 只对 authoritative reserved Trial 且仍处 Reservation window 的 exact Request 签发；receipt 联合冻结 Reservation/Request/entry Order、Dataset、两个父 Admission、Kline↔Aggregate Trade Bar Link、单根 bar window、价格/保护边界和 aggregate events hash。bar-link 必须完全落在父 Evidence/Ordering window 内且与 Ordering 使用同一 aggregate events hash；父窗口可额外容纳闭合 K 线的 `bar_close` 事实，不把它错误塞进 trade bar 的半开窗口。唯一 capability 是 `bounded_initial_stop_market_same_bar_post_entry_protection_ordering`，观察严格从触发入场的 aggregate trade 之后开始。receipt 固定 `runner_compatibility=not_bound`、activation forbidden、Fill quantity/cost authority none、external completeness not verified，并保留无 cross-source global sequence、无 queue/maker/slippage/impact/insurance/ADL、无 Result/Artifact/default cutover 限制。同一 Reservation/Request/bar-link 只能 create-or-identical，SQL 禁止更新/删除；父 lineage、Dataset、Stop-market liquidity binding、可执行窗口、trigger/protection touch 或 Request 类型漂移均拒绝。下一纵切才能由 opt-in Runner/Step Engine 消费该 authority；本步未改变任何经济结果。
- 2026-07-22：补齐 compatibility R&D 的 provider-native lookahead 防线。`tech-indicators` 在每个 feature timeframe 上最多选择 200 个确定性 cutoff，用截断 OHLCV 重新执行同一 Go factor formula，并按正式六位小数输出精度对照全量历史值；报告显式记录 complete/sampled coverage、factor/comparison/mismatch 数量与最多 20 条差异。Artifact helper 不再复用缺少 passed causality evidence 的旧缓存；factor discovery 在选因子前 fail closed，任何带外部 `factor_conditions` 的候选在证据缺失/失败时由 `RND-FEATURE-CAUSALITY` 阻断。对抗测试证明故意读取最终 close 的 provider 公式会被捕获；这与 replay consumer 的 full-vs-cutoff decision detector 分层，前者证明 feature producer，后者证明 strategy consumer。当前仍是 compatibility R&D 证据，不替代 Replay M4 的 PIT Wire/Control Plane authority。
- 2026-07-22：M4-P29 第一纵切完成，成熟度保持 M3，进度 `1/6`。新增 immutable `Kline Source Record v1` 与非经济 `Kline Aggregate Trade Bar Link Attestation v1`，将既有 ReplayMarketBar、冻结 Kline source/hash、Aggregate Trade Coverage/Event hash、半开 bar window、PIT availability、aggregate/underlying trade-id 范围和对账结果闭合为一个自哈希 sidecar；未修改 ReplayMarketBar、Request、Runner、Engine Source Reducer、Result、Artifact 或默认路径。Data Adapter 只在 Kline 与 aggregate trades 的 open/high/low/close、base/quote volume、taker-buy volume、underlying trade count、窗口及连续 ID 全部精确一致时生成 attestation，任一价格、数量、计数、ID、coverage、hash 或可见性漂移 fail closed。该证据固定 `economic_authority=none`、`runner_compatibility=not_bound`、`price_path_admission=forbidden_until_control_plane_and_runner_cutover` 与 `external_completeness=not_verified`；它只证明 bar-relative ordered public-trade path 可被后续授权，不证明 insurance/ADL、hypothetical queue、Fill quantity、maker probability、slippage、impact 或 venue-global completeness。下一纵切只能由 Control Plane 冻结 bar-link/exact-path authority，不能绕过 Reservation 直接把 sidecar 交给 Engine。
- 2026-07-22：P28 后 fidelity blocker 重审完成，选择 M4-P29 `bounded-bar-linked-aggregate-trade-stop-entry-path-end-to-end`，尚未实现，成熟度保持 M3，预算 `0/6`。选择原因不是 aggregate trade 已足够完整，而是仓库已经有 immutable Archive、Market Data Provider、Control Plane certification/admission、Cross-source Ordering/Wire 和 certification-only exact-price resolver；当前真正缺的是 OHLCV↔aggregate-trade bar link 与 official Runner consumer。P29 只处理 pre-entry Stop-market 在同一 bar 触发后，OHLCV 无法判断保护 stop/target 先后的现有 typed failure。进入 opt-in 经济路径前必须以同一 frozen Kline 的 open/high/low/close、base/quote volume、underlying trade count、半开时间窗和 aggregate/underlying trade-id 连续性完全对账；任一缺失、边界、PIT availability 或 hash 漂移继续 unresolved 且无 Result。即使完全对账，Evidence 仍固定 `external_completeness=not_verified`，只证明 bar-relative ordered price path；aggregate trade 只是 100ms 内同价同 taker side 的压缩市场成交，且官方接口排除 insurance/ADL，因此不得升级为 hypothetical order queue、Fill quantity、maker probability、slippage、impact 或 venue-global completeness。未选择第三次 partial（计数扩张）、reentry（缺新的 runtime Allocation authority）、cross-margin/borrow（缺 venue collateral/interest facts）、真实 partial/impact（缺 L2 queue/depth）或 Fast（缺独立等价后端）。P29 必须在六个以内 functional change sets 完成 bar-link、Control Plane authority、Step/Checkpoint/Result/Artifact consumer、long/short opposite-path golden、resume/idempotency/tamper 与 default/P15–P28 regression；否则停止而不继续生成 pre-integration schema。
- 2026-07-22：M4-P28 第八纵切完成，里程碑在 M3 范围内 complete。正式 opt-in 路径已由 official Lane Result/Artifact 进入 dedicated post-partial Risk projector、owner-keyed Accounting Artifact 和 1–8 个 Control Plane 预声明 full-flat Cycle Sequence；同一证据链闭合 Result quantity/risk、current-stop risk、Ledger/Journal/Trial Balance、cash roll-forward、manifest-last publication 与幂等重试。默认 Integrated Portfolio 仍只支持其原有单 mutation 边界，不接纳“一/两笔 partial 后 replacement”的组合，也未修改 P15–P27 schema/hash/consumer。全量收口回归通过 Replay Contracts 20、Engine 91、Accounting 21、P27 dedicated Runner 4、P15–P28 Runner 83、Control Plane Contracts 23、State Store 17 个测试及相关 package typecheck。P28 明确仍不认证 between-partial/repeated mutation、dynamic sizing、第三次 partial、reentry、cross-margin/borrow、真实 liquidity/impact 或 Fast；成熟度保持 M3，下一步必须重新审计并选择新的有界 M4 fidelity blocker，不能沿 P28 自动扩权。
- 2026-07-22：M4-P28 第七纵切完成，成熟度保持 M3，进度 `7/8`。Research Control Plane 新增专用 `Post-partial Stop-replacement Cycle Sequence Reservation v1`，State Store 只在同一事务内对仍为 current reserved Trial、且 Reservation/Request hash 与 active Attempt Lease 完全一致的 1–8 个预声明周期签发；冻结 exact Lane Trial/Run/Request identity、严格递增 earliest cycle time、full-flat successor policy 与禁止 runtime append/search expansion。Replay Sequence Runner 逐周期消费第四纵切 Risk Evidence 与第六纵切 committed Accounting Artifact；只有 predecessor 的 collateral、gross/net exposure、unrealized、reserved admission risk、current active-stop risk 全为零，且 historical admission risk 已全部 release，才把 committed Trial Balance `ending_available_cash` 滚入下一周期。四周期真实 Lane golden 以零成本/零价格变化锁定 `1000→1000→1000→1000→1000`、四组不同 Trial/Request hash、唯一 opening equity、child Ledger/Journal hash 保留、historical/released risk `60=60` 与 ending current risk `0`。第三周期 child failure 只执行前三周期且无 Sequence Evidence/manifest；第二周期 open predecessor 阻止第三周期；`consolidated-journal.json` 前中断无 manifest，相同重试复用 orphan payload 后提交。九 payload Sequence Artifact 使用 manifest-last；现有 P15–P27/default materializer/cutover 尚未切换，最后第八纵切只做全量回归、默认边界与里程碑收口。
- 2026-07-22：M4-P28 第六纵切完成，成熟度保持 M3，进度 `6/8`。新增独立 `Portfolio Post-partial Stop-replacement Accounting Artifact Manifest v1` 与 Runner publisher，未改 P19/P20/P27、Lane Result/Artifact 或第四纵切 Risk schema。八个固定 payload 依序保存 exact Lane Result Artifact Manifests、Lane Results、Risk Evidence、owner bindings、Ledger、Journal、Trial Balance、Accounting Evidence；只有最后 create-if-absent 的 manifest 是权威 commit marker。Artifact namespace 由 source Risk/bindings/accounting policy 与 Accounting Evidence hash 决定；已提交重试逐文件重读 ref、SHA-256 和 canonical payload，并把 artifact/portfolio/source/accounting/owner identity 回绑当前 Evidence，完全一致才返回 `idempotent_replay=true`。官方 owner matrix 锁定首次提交与幂等重开、owner-binding Manifest 篡改、重算 Manifest hash 后 source Risk 头部篡改、已提交 Ledger 读取漂移；在 `journal.json` 写入前注入中断时 Outcome 返回 `evidence=null / artifact_manifest=null / partial_portfolio_result_published=false`，无 manifest，随后只复用 create-identical orphan payload 完成提交。Control Plane authority、cycle/sequence 与 successor cash roll-forward 仍关闭。
- 2026-07-22：M4-P28 第五纵切完成，成熟度保持 M3，进度 `5/8`。新增只读 `Portfolio Post-partial Stop-replacement Accounting Evidence v1`，直接消费第四纵切 Risk Evidence 及其 exact Lane Result/Artifact。最终 owner 固定为 replacement stop、preserved target、strategy exit、exact liquidation、open-at-end；每条非零 Portfolio Ledger posting 保留 source Lane Ledger EventKey/ref/hash 与 `lane_id + risk_record_hash + final owner`。Portfolio Journal 不重新解释经济事实：仅删除每个 Lane 的 local opening balance、合并为一次 Portfolio opening equity，其余 collateral reserve/release、fee、Funding、realized PnL、liquidation fee、mark-to-market legs 逐条复制 certified Result；因此 Funding 归属 live isolated collateral 的事实不会被错误改记 wallet。Trial Balance 重新汇总所有 account legs，并严格等于 Risk Evidence 的 available/settled cash、live collateral、unrealized/NAV、`historical admission = reserved + released` 与 current active-stop risk。官方 owner matrix 覆盖 replacement stop/保留 target/strategy exit/open，另以 long/short × 一/两 partial 覆盖 exact liquidation/open；source Journal leg 篡改无法通过 Trial Balance。P19/P20/P27 与默认 materializer 未修改；manifest-last Artifact、幂等发布和 cycle/sequence 仍关闭。
- 2026-07-22：M4-P28 第四纵切完成，成熟度保持 M3，进度 `4/8`。新增独立 opt-in `Portfolio Post-partial Stop-replacement Risk Evidence v1`，不修改 P19/P27 schema/hash/default materializer，也不重复读取 OHLCV 或建立第二套 terminal matcher。consumer 只接受 official completed Lane Request/Result/Artifact，逐 Lane 重验一/两笔 fixed partial Fill、final-partial 后 replacement decision/Order activation、terminal exact remaining quantity、Result Position/Risk binding 与 Ledger balance chain。历史 admission risk 永远按 initial entry quantity + original stop 保存；open-at-end 的 current active-stop bounded risk 按 replacement adverse execution + ending quantity 重算，full-flat 归零并释放 admission reservation。settled cash、live isolated-collateral account、available cash、unrealized/NAV、gross/net exposure 全部取自或闭合到 Result Trial Balance/Valuation；collateral account 可吸收 attributed Funding/PnL，只有 full-flat 才为零，不能把“原始 collateral allocation 不变”误写成账本事实。官方八 Lane golden 覆盖 long/short × 一/两 partial × exact liquidation/open：`8000` initial cash、`160 = 80 reserved + 80 released` admission risk、open current risk `22`、gross `220`、net `0`，并拒绝重算 hash 后 risk 漂移、Result collateral 漂移和 replacement activation 缺失。owner-keyed Accounting、manifest-last Artifact、cycle/sequence 仍关闭。
- 2026-07-22：M4-P28 第三纵切完成，成熟度保持 M3，进度 `3/8`。本步不新增 matcher/schema，只完成 official combined Lane 的 terminal-owner 认证矩阵：两笔 partial 后 generation-4 replacement stop gap；保留 target 单触达；replacement stop 与保留 target 同 bar collision 继续标记 `resolution_limited` 并按 stop-first；可选 final strategy exit 在下一 executable open 关闭剩余 `0.4` 并取消 current siblings；exact Mark liquidation 沿用第二纵切的 long/short × `0.7/0.4`；无终止信号时 open-at-end 仅按 close 标记剩余 Position，不造 Fill。pre-replacement generation-3 stop 的唯一生命周期为 `submitted→activated→cancelled(protective-stop-replaced)`，后续不得 trigger/fill。对 combined Result 的 protection generation、stop trigger、target order lineage 重算 protection/evidence hash 后仍拒绝；quantity 篡改由第二纵切的 Position/Risk binding 拒绝。Portfolio historical/current risk 分离、owner accounting、cycle 与 manifest 尚未进入。
- 2026-07-22：M4-P28 第二纵切完成，成熟度保持 M3，进度 `2/8`。没有另建风险计算器，也没有升级 wire：现有 EventKey reducer 已正确让 Funding phase 10 先于同刻 bar-open partial mutation，并让后续 Funding/Mark/Margin 读取最新 Position projection；缺口在于 published Result 尚未强制把这条数量因果链重新闭合。新增 `assertReplayResultPositionRiskBindings`，Runner 在首次发布与幂等 Artifact 重开时均校验：每个 Margin Snapshot 必须绑定最新 causal Position 及 exact signed quantity；每条 Funding Ledger 必须绑定同 EventKey `funding_mark` snapshot 和 t-minus Position；Liquidation 必须绑定 breach Snapshot、剩余 open Position 与稍后 forced full-close Fill 的同一绝对数量。官方 Lane 四组 golden 覆盖 long/short × 一/两笔 partial，锁定 Funding 在第一笔前为 `1`、两笔之间为 `0.7`、第二笔及 replacement 后为 `0.4`，以及 replacement 后 exact Mark 仅清算 `0.7/0.4`；Funding margin quantity 与 liquidation quantity 篡改均 fail closed。Portfolio historical/current risk、owner accounting、cycle 与 manifest gate 仍关闭。
- 2026-07-22：M4-P28 第一纵切完成，成熟度保持 M3，Portfolio/Accounting/Cycle gate 仍关闭。Request v38、Schedule v13、Protective Stop Replace Intent v2、Partial Reduce Intent v3、Simulator v24、Result v52、Artifact v54 与 Checkpoint v31 首次认证单资产“一或两笔 bounded fixed partial 全部完成后，再执行一次预声明 tighten-only stop replacement”。组合 policy 必须逐 Intent 一致；replacement 严格晚于最后 partial，禁止插入两笔 partial 之间，之后只允许可选 final full exit。runtime Harness State 读取最后 partial 已提交后的 signed Position、cash 与 current bracket；同 boundary exact risk/current stop/target terminal 先决，成功后只取消 current stop、保留 current target、按剩余绝对数量激活新 stop，并将 protection generation 从 2/3 推进到 3/4。两-partial long golden 锁定 `1→0.7→0.4`、generation-4 replacement、新 stop gap 而 former looser stop 未触达、replacement boundary 被 generation-3 stop 抢占、clean/resume Result hash 一致，以及重算 checkpoint hash 后 generation 回退仍拒绝。单独 partial 与单独 replacement 旧路径保持原 policy；Portfolio materializer 继续 fail closed，故 funding/Mark/Margin/liquidation 的组合证据、long/short 对称、owner accounting、Artifact/cycle 与 cutover 尚未完成，P28 为 `in_progress 1/8`。
- 2026-07-22：M4-P27 后 fidelity blocker 重新排序完成，下一条有界纵切选择为 M4-P28 `bounded-post-partial-tighten-only-stop-replacement-end-to-end`，尚未实现，成熟度保持 M3。选择依据是当前单资产分别认证了“一至两笔预声明 fixed-quantity partial reduce”和“一次 tighten-only protective-stop replacement”，Portfolio 也分别拥有两条 owner/accounting/cycle successor，但 Schedule 明文禁止组合，真实的“分批减仓后收紧剩余仓位止损”只能被错误简化为不变 stop 或整段拒绝。P28 只允许一或两笔已冻结、next-open、full-fill、reduce-only partial 全部完成后，再执行至多一次同样预声明的 full-current-position tighten-only stop replacement；replacement 必须严格晚于最后 partial，decision boundary 上 current stop/target、exact risk 与 terminal 先决，成功后取消 current stop、保留 target，并以当前剩余绝对数量激活下一 protection generation。历史 admission risk 永不改写，current active-stop risk 按新 trigger 重估；Funding、Mark、Margin、liquidation、terminal owner、Ledger/Journal/Trial Balance、manifest-last Artifact 与 1–8 full-flat cycle 必须在同一 change set 闭合。未选择“第三次 partial”，因为只增加计数而不形成新责任边界；未选择 capacity/impact，是因为现有 static Liquidity Capacity Attestation 明示不是 depth/queue proof，且仓库尚无 PIT 校准算法，不能把假设先升级为经济真相；reentry、cross-margin/borrow、真实 partial fill 与 Fast 分别缺 runtime Allocation authority、venue collateral/interest facts、L2/queue completeness 或独立等价后端。P28 不开放 replacement 插入两次 partial 之间、重复 mutation、target mutation/cancel、动态 sizing、第三次 partial、add/reversal/reentry、订单 partial fill、真实 impact、cross/shared margin、borrow 或 Fast。
- 2026-07-22：M4-P27 第八纵切完成，里程碑在 M3 范围内 complete。Research Control Plane 新增专用 Two-Fixed-Partial Cycle Sequence Reservation，并在同一事务中仅对仍匹配 current reserved Trial 与 active Attempt Lease 的 1–8 个 exact child Reservation 签发；周期、Lane/Request identity、严格递增时间、full-flat successor policy 和禁止 runtime expansion 均冻结。Replay Sequence Runner 逐 child 调用正式 P27 terminal/accounting consumer，只有 committed Trial Balance、零 collateral/exposure/unrealized/current risk 且 successor entry 严格晚于 predecessor close 才滚动现金。四周期 golden 锁定 `100→112.5→125→137.5→150`、12 笔 owner-keyed realized PnL、唯一 opening equity、child hash 保留和 balanced consolidated Trial Balance；第三周期执行失败不生成 sequence Evidence，第二周期 open-at-end 禁止 successor，中断 `consolidated-journal` 写入不生成 manifest，相同重试逐 payload 幂等复用，cash bridge/sequence evidence 篡改 fail closed。P27 十 payload sequence Artifact 使用 manifest-last；动态 sizing、第三次 partial、post-partial mutation、reentry、cross-margin/borrow、真实 liquidity 与 Fast 仍明确禁止，成熟度保持 M3。下一步必须重新审计剩余 fidelity blocker，不自动创建 M4-P28。
- 2026-07-22：M4-P27 第七纵切完成，成熟度保持 M3。新增只读 owner-keyed accounting 与 manifest-last Portfolio Artifact：Accounting 只消费官方 Control Plane Reservation、P27 terminal Evidence 及其绑定的 certified Lane Result/Artifact；每笔非零 fee、Funding、realized PnL、liquidation fee 保留原 EventKey/source-ledger hash，并绑定 `lane_id + terminal_record_hash + final owner`。Journal 只记一次 opening equity，entry 时 reserve 原 isolated collateral，只有 committed full-flat 才 release；open-at-end 保留 terminal valuation，Trial Balance 必须精确对齐 settled/available cash、collateral、unrealized 与 NAV。Artifact 固定八个 payload 先写、manifest 后写；相同 retry 逐 payload 验证并幂等复用，中断写只留下非权威 orphan payload，不返回 aggregate Evidence/manifest。该闭环同时发现并修正 P27 partial checkpoint 对标准 Result Ledger `initial_cash` 的二次累计，现只累计经济 cashflow。剩余唯一纵切是 1–8 个预声明 full-flat cycle 的 cash/owner bridge、失败与 sequence manifest-last 认证；完成前不得把 M4-P27 标记 complete。
- 2026-07-22：M4-P27 第二至六个 functional slice 完成。Control Plane-issued Reservation、Runner projection、零/一/二 Fill terminal preemption、完整 owner matrix 与 quantity-aware exact-risk chain 均已接入，不修改 P26 schema/hash。每个 exact Funding/Mark Margin Snapshot 必须匹配 authoritative source EventKey、t-minus Position、generation 1/2/3 与 Funding ledger；证据固化 signed/absolute quantity、mark/notional、collateral、settled cashflow、unrealized、margin balance、initial/maintenance requirement/headroom、breach/liquidation state。Exact liquidation 还必须从 exact breached Snapshot 闭合到 forced full-close Fill 与 flat-without-deficit settlement。golden 覆盖 partial 前、两笔之间、第二笔后的 Funding/Mark 数量 `1→0.7→0.4`，并锁定 risk quantity drift、late terminal、错误终态数量与重哈希 generation 篡改。Portfolio Artifact、owner-keyed accounting 与 bounded cycle 仍缺，因此 M4-P27 继续 `in_progress`、成熟度仍为 M3。

结论：方向是对的，已明显强于普通“参数搜索 + 漂亮回测”。当前机制适合做**有限假设验证**，不适合扩大成持续自动挖策略；要投入真实策略研发，最关键的是持续守住 holdout、edge margin、shadow 执行归因三道门。

## 修复状态

- M4-P27 单资产权威链、受限 Portfolio terminal/risk、owner-keyed accounting 与单周期 manifest-last Artifact 已实现，里程碑仍 in progress，成熟度保持 M3：Request v37、Schedule v12、Partial Reduce Intent v2、Simulator v23、Result v51、Artifact v53 与 Checkpoint v30 允许同一 Lane 执行最多两笔全部预声明的 next-open fixed-quantity reduce-only partial。Schedule 强制两笔 Intent 使用双 partial policy、严格 decision/execution 顺序、累计 quantity 小于 initial；Engine 为每笔 Order 使用 decision-scoped id，第二次 Harness State 读取第一次 Fill 后的 Position/cash/protection，Funding 在同刻 phase-10 读取 partial 前 t-minus quantity，partial phase-20 后于 phase-90 原子重建 generation 2/3。Control Plane-issued Reservation、正式 Lane Runner projection、逐 partial cash/collateral/exposure/current-risk、第一/第二 executable boundary 抢占、完整 post-second owner matrix、quantity-aware exact Funding/Mark/Margin/liquidation、Ledger/Journal/Trial Balance 与幂等发布均已闭合。1–8 cycle 仍未实现，故 P26 仍是完整 cycle authority，M4-P27 不得标 complete。
- M4-P27 选择审计已完成、尚未实现，成熟度保持 M3：下一纵切定为 `two-predeclared-fixed-partial-reduces-end-to-end`。P26 证明了一次 fixed partial 能以认证 Lane Result 贯通 Portfolio，但当前 Schedule/State/Checkpoint 明确只允许一次，无法表达常见的两段预声明 scale-out；把 P26 结果在 Portfolio 层重复投影会丢失第二次 Harness authority、generation-3 protection、两次 Fill 间的 t-minus Funding/Mark/risk 和 resume fence，因此不能用现有 successor 拼装。P27 只允许同一 frozen Schedule 中最多两笔严格递增的 next-open market、fixed-quantity、reduce-only partial；两笔 Order 各自必须 full-fill，每次 Fill 后都必须保留正仓位，累计数量严格小于 initial quantity。每个 executable boundary 的 current protection 与 exact risk 先决；成功 Fill 后在同一 source boundary 取消当前 stop/target 并按原 trigger、剩余绝对数量原子激活下一 generation，第二次成功进入 generation 3。第二次 decision State 必须绑定第一次 Fill 后 quantity/cash/realized PnL/fee/protection；terminal preemption 写 not-reached，clean/resume 不重放 Fill/Funding。Portfolio 继续保留原 collateral 与历史 admission risk，只重估 current quantity、exposure、active-stop risk、maintenance/liquidation 和 owner；只有最终 full-flat 才释放预算、滚入 1–8 cycle。完成必须在一个 change set 内贯通单资产 Result/Checkpoint、Portfolio terminal、owner-keyed accounting、manifest-last Artifact、cycle、long/short/race/open/liquidation/failure/retry/tamper。未选项：partial 后 stop/target mutation 会引入组合状态爆炸；reentry 缺 runtime Allocation authority；cross-margin/borrow 缺 venue collateral/interest facts；真实 partial 缺 L2/queue completeness；Fast 缺独立等价后端。动态 sizing、第三次 partial、订单 partial fill、reentry 与上述能力均不进入 P27。
- M4-P26 已完成、成熟度保持 M3：默认 Integrated/Risk projection 继续拒绝 fixed partial，显式 successor 才消费一次冻结的 next-open fixed-quantity reduce-only partial。Portfolio 以每条 admitted Lane 的认证 Replay Result/Artifact 为 execution authority，不复制撮合器：generation-1 与 exact risk 同边界先决；partial 必须全成且留非零仓位；phase 90 以原 trigger、剩余绝对数量重建 generation 2。归并链覆盖 partial 前后 t-minus Funding、realized PnL/fee、后续 stop/target/strategy-exit/liquidation owner、exact-Mark unrealized、gross/net exposure 与 current stop risk；历史 admission risk 独立保留，原 isolated collateral 到 full-flat 才释放，utilization 下降不授权 entry/reentry/successor。owner-keyed Ledger/Journal/Trial Balance、terminal/accounting/1–8 cycle manifest-last Artifact 已闭合；long/short、initial-bracket race、open-at-end `0.6` quantity/collateral/exposure/risk、四周期 cash `100→108→116→124→132`、唯一 opening equity、retry、第三周期失败、publication interruption 与重哈希语义篡改均锁定，P15–P25 全量回归通过。动态 sizing、第二次 partial、partial 后 mutation、reentry、cross-margin/borrow、真实 liquidity 与 Fast 仍禁止；下一步重新审计 blocker。
- M4-P26 选择审计已完成、尚未实现，成熟度保持 M3：下一纵切定为 `fixed-partial-reduce-portfolio-risk-resize-end-to-end`。选择依据不是“单资产已经有代码”，而是它是当前唯一同时具备冻结 authority、确定性执行、完整单资产 Result/Checkpoint 证据且不依赖新市场数据的 Portfolio fidelity 缺口：`ReplayPartialReduceIntent v1` 已限制为一次 next-open market、fixed quantity、reduce-only，订单自身必须 full Fill 且保留 open Position，并在同一 source boundary 取消 generation-1 stop/target、按剩余绝对数量以原 trigger 重建 generation 2；单资产测试已锁定 long/short、partial realized PnL/fee、partial 前后 t-minus Funding、stop/target/strategy-exit/open-at-end/exact-liquidation owner、clean/resume 与重哈希篡改。Portfolio 当前却在 Risk materialization 前拒绝该 Schedule，P8/P9/P14/P15 仍把 entry quantity、full-position exposure、Funding、Margin 与 frozen stop risk 当作全程不变，若仅放行 mutation 会同时错算钱包、gross/net exposure、maintenance/liquidation quantity、保护代际与 cycle eligibility。P26 必须用显式 opt-in successor 一次闭合 fixed partial-reduce full Fill、generation-2 protection、quantity-aware Funding/Mark/Margin/liquidation、partial realized PnL/fee、exposure/current-risk resize、terminal owner、Ledger/Journal/Trial Balance、manifest-last Artifact 和 1–8 full-flat cycle；历史 admission fact 与 current remaining-position risk 必须分开。由于当前没有冻结的 venue collateral-release policy，partial 后原 isolated collateral 继续保留到 full-flat，risk utilization 的下降也不得自行授权新 entry、reentry 或 successor。默认 Portfolio projection 继续拒绝，P15–P25 不改写。动态数量、第二次 partial、partial 后 stop/target mutation、同周期加仓/reentry、订单 partial fill/真实 liquidity、cross-margin/borrow 与 Fast 均禁止。其他候选未选：reentry 需要新的 runtime Allocation authority；cross-margin/borrow 需要场所级共享抵押与利息事实；真实 liquidity 缺 L2/queue completeness；Fast 尚无独立等价后端，无法先于 Step 语义闭合认证。
- M4-P25 已完成、成熟度保持 M3：默认 Integrated/Risk projection 仍拒绝 protective-stop cancel；显式 successor 只消费一个 frozen final `cancel_active_stop_preserve_target`，decision-boundary generation-1 stop 先决，cancel 后 former stop 永久不可达而 target 保持 active。历史 admission `frozen_stop_risk` 与 current protection risk 已拆分：cancel 不产生 Fill、cash/Ledger/Journal posting，不释放 collateral、exposure 或 risk budget；open-at-end 明确为 `unbounded_by_active_stop`，active-stop bounded risk=`null`，同时保留 settled/available cash、isolated collateral、gross/net exact-Mark exposure、unrealized 与 admission reservation；exact Funding、maintenance/liquidation 继续执行。只有 target 或 exact liquidation committed full-flat 才释放风险并允许 successor cycle。long/short target、cancel-boundary stop race、former-stop gap、open-at-end、post-cancel liquidation、owner tamper、retry 与 Engine failure均已锁定；四周期 owner=`target,target,liquidation,target`、cash=`100→111→122→102→113`，cycle-local ledger hash 保留、全局只有一次 opening equity，第三周期 Risk failure与中段 publication interruption均不生成 sequence Evidence/manifest。P15–P24 未改写；重复 mutation、partial、reentry、cross-margin、borrow、真实 liquidity 与 Fast 未进入。下一步必须重新审计并选择一个真正降低 fidelity blocker 的 bounded M4 纵切，不自动把 fixed partial 升格为制度。
- M4-P25 选择审计已完成、尚未实现，成熟度保持 M3：下一纵切是 `protective-stop-cancel-risk-degradation-end-to-end`，不是“删掉一个 stop Order”式的局部适配。排序依据是 fidelity blocker reduction 与已有证据可复用性：单资产 Request/Schedule 已正式锁定唯一 final `cancel_active_stop_preserve_target`、target 保留、decision-boundary stop 先胜、former stop 不可达、target-only OHLCV exact evidence、open-at-end、checkpoint/resume 与重哈希篡改拒绝；Portfolio predecessor 仍在 Engine 前拒绝该 mutation，因 P8/P9 的 `frozen_stop_risk` 同时承担 admission 与持续 risk-state 表示，直接忽略 stop cancel 会把已不存在的保护价伪装为当前损失上界。P25 必须分离两种事实：`frozen_stop_risk` 永久保留为历史 admission lineage；cancel 生效后的 current protection risk 显式降级为 `unbounded_by_active_stop`，不得生成 cash/Ledger posting、不得释放 collateral/exposure/risk budget，Funding、exact Mark、maintenance/liquidation 继续运行。只有 target 或 liquidation 已提交 full-flat terminal 才可一次释放并进入下一预声明周期；open-at-end 必须保留 available/settled cash、collateral、gross/net exposure 和降级风险状态，cycle successor fail closed。范围在一个 change set 内闭合显式 projection、target-only terminal owner、risk-degradation state、owner-keyed Ledger/Journal/Trial Balance、manifest-last Artifact 和 1–8 full-flat cycle，并锁定 long/short、cancel-boundary race、former-stop unreachable、open-at-end、liquidation、failure、retry、publication interruption 与 tamper。默认 Portfolio projection 继续拒绝，P15–P24 不改写。fixed partial 次位：虽然单资产已有一次 fixed-quantity reduce/reprotect，但进入 Portfolio 会同时改变 quantity、Funding、Margin、gross/net exposure、risk release 与 protection generation；repeated mutation、reentry、cross-margin/borrow、真实 liquidity partial 与 Fast 跨越更大 authority 或输入边界。
- M4-P24 已完成、成熟度保持 M3：默认 Portfolio/Risk materialization 继续拒绝 strategy-exit cancel；只有 successor 显式 opt-in 后，Runner 才验证同一 frozen Schedule 中唯一 exit/cancel 的 target sequence、intent hash 与严格时间窗，并以内部 `cancelled_exit` closure 让 Risk Engine 接受“Plan 仍绑定原 exit authority、经济 Lane 不再执行该 exit”。因此 cancel 后的 exact Funding、Mark/risk、liquidation 与 generation-1 full bracket 不会被原 exit 截断，也不靠事后重放补账。P24 terminal Evidence 绑定 cancel intent、原 earliest executable time、source P15 record、bracket OHLCV resolution 与唯一 owner；long target、short stop 均在 former-exit open 由保护单终结，原 strategy exit 不可达；cancel-close bar stop touch 先胜并记 `terminal_before_or_at_decision`。owner-keyed Ledger/Journal/Trial Balance、单周期和 1–8 full-flat cycle manifest-last Artifact 已闭合；四周期 owner=`target,target,stop,stop`，cash=`100→111→122→112→101`，cycle-local ledger hash 保留且全局只有一次 opening equity。第三周期 Risk failure 无 aggregate Evidence，中段 publication 失败无 sequence manifest，retry 与重哈希 journal/owner tamper 均 fail closed。P15–P23 保持兼容；stop cancel、重复 mutation、partial、reentry、cross-margin、borrow、真实 liquidity 与 Fast 未进入，下一步重新排序 blocker。
- M4-P24 实施前选择审计：当时下一纵切定为“一个预声明 pending strategy-exit cancel 的端到端 Portfolio successor”。排序依据是 fidelity blocker reduction：单资产 Simulator 已锁定 `submit full reduce-only market exit -> decision-close cancel -> bracket remains active -> later stop/target terminal`、terminal-before-cancel、Order State、checkpoint/resume 与篡改拒绝；Portfolio predecessor 仍会把 scheduled strategy exit 当作可执行 terminal，无法表达撤销后由保护单重新取得 owner，进而错误影响 realized PnL、fee、cash bridge 与下一周期资格。P24 只允许同一 frozen Schedule 中 zero-or-one strategy exit 及其唯一 cancel，要求 `exit.signal_time < cancel.effective_at < exit.earliest_executable_time`，cancel 后 initial stop/target 的 id、trigger、quantity、generation 全部不变；cancel boundary 已发生的 exact-risk 或 protective terminal 仍先胜，原 strategy-exit earliest open 此后不可达。完成必须在一个 change set 内贯通显式 predecessor projection、bracket-preserved terminal rearbitration、owner-keyed Ledger/Journal/Trial Balance、manifest-last Artifact、1–8 full-flat cycle cash roll-forward，以及 long/short、race、former-exit unreachable、failure、retry、tamper 证据。默认 Portfolio projection 继续拒绝该 Schedule，P15–P23 不改写。stop cancel 暂不选：它会移除 admission 使用的 frozen stop-risk 上界，必须先有风险降级/再授权合同；fixed partial 会同时改变 quantity、Funding、Margin、exposure/risk release 与 protection generation；真实 liquidity partial 缺 L2/queue completeness；reentry、cross-margin/borrow/partial liquidation 与 Fast 跨越更大 authority 或输入边界。
- M4-P23 已完成、成熟度保持 M3：普通 Portfolio projection 继续拒绝 take-profit cancel，只有 successor runner 显式启用；一个 frozen initial full-position target 在 decision close 后转为 `stop_only`，保留 generation-1 full stop，former target 永久不可达，而 decision-boundary 已成交旧 target 仍先胜。long/short 单周期均由后继 stop gap以 owner=`initial_protective_stop` 结算 `100→89`；旧 target 的原 terminal hash 进入 `excluded_preempted_source_hashes`。cancel 专属 Evidence/Fingerprint、owner-keyed Ledger/Journal/Trial Balance、单周期及 1–8 full-flat cycle manifest-last Artifact 已闭合。四周期依次覆盖 long stop、short stop、旧 target race、long stop，cash=`100→89→78→88→77`；consolidated Ledger 保留每笔 cycle-local ledger hash，Journal 仅一笔 opening equity。第三周期 Risk failure 不生成 aggregate Evidence，中段写失败不生成 sequence manifest；retry 与重哈希 journal tamper 均锁定。stop cancel、重复 mutation、partial、reentry、cross-margin、borrow、真实 liquidity 与 Fast 未进入，P15–P22 未改写；下一步必须重新做 blocker 排序。
- M4-P22 已完成但成熟度保持 M3：按选择审计要求，在一个 change set 内交付 take-profit replacement terminal、owner-keyed Ledger/Journal/Trial Balance、single-cycle Artifact 与 1–8 cycle consolidated Evidence/Artifact，没有拆出三个里程碑。每 Lane 只接受 zero-or-one frozen full-position reduce-only initial-target replacement；decision-close bar 先由 generation 1 结算，generation 2 只在后继 boundary 生效且 initial stop 不变。Risk materialization 使用显式 target-projection opt-in，普通 Integrated/Portfolio 路径仍 fail closed。四周期 golden 同时锁定 long `120→gap 121`、short `80→gap 79`、旧 target 在 decision bar 先成交、replacement 后 stop/target 同 bar collision 仍 stop-first；cash `100→121→142→152→142`，全局账为 realized income 52/loss 10 且只有一次 opening equity。retry、第三周期 Risk failure 无 aggregate Evidence、中段写失败无 sequence manifest、journal 重哈希 tamper 均已锁定；P15–P21 不改写。下一步回到 blocker 排序，不自动把 sibling cancel、partial 或 Fast 固定为下一制度。
- M4 下一纵切选择审计已完成：唯一允许进入实现的是“一个预声明 take-profit replacement 的端到端 Portfolio successor”，范围从 terminal owner 直接贯通 accounting 与 bounded cycle，不再拆成三个只增加 phase 编号的里程碑。排序依据不是实现方便，而是 fidelity blocker reduction：单资产 Simulator v22 已认证 initial target 的 cancel→submit→activate、旧 target race、former-target 不可达、stop preserved、clean/resume 与重哈希篡改；Portfolio P15–P21 却仍拒绝该 Schedule，无法对同一策略给出 shared-wallet terminal/accounting evidence。该缺口不需要新数据、不会扩大搜索空间，且会实质改变 realized PnL、terminal fee、cash bridge 与周期后继资格。target/stop cancel 暂列次位：target cancel 尚可保留 frozen stop，stop cancel 会使现有 frozen-stop risk budget 失去保护含义，必须先设计风险降级；fixed partial 会同时改变 quantity、Funding、Margin、gross/net exposure、risk release 与 protection generation；真实 liquidity partial 缺 L2/queue completeness；reentry、cross-margin/borrow/partial liquidation 和 Fast 均跨越更多 authority 或尚无可认证输入。下一纵切只接受 zero-or-one frozen full-position reduce-only take-profit replacement、旧边界 generation-1 先决、新边界 generation-2 target 与 initial stop 竞争、P15 predecessor lineage、唯一 owner、双重记账、manifest-last 和 1–8 full-flat cycle cash roll-forward；不开放重复 replacement、stop mutation、cancel、partial、reentry 或通用 amend。
- M4-P21 已完成但成熟度保持 M3：没有给 P17/P18 增加可选字段，而以新 successor 将 P19/P20 传播到 Control Plane 预声明的 1–8 个 full-flat cycle。Runner 复用既有 cycle authority materialization，每周期真实提交 Allocation、exact Risk、P14/P15、P19 与 P20；只有 committed P20 Trial Balance 的 ending available cash 能成为下一周期 opening cash。Sequence commit 绑定 replacement-aware owner/generation、P19/P20 evidence/manifest、Risk Result、close time、cash 与 Trial Balance hash；consolidated Ledger 保留 child entry/hash，Journal 仅 cycle 1 保留 opening equity，十一 payload manifest-last。双周期 long/short golden 锁定 `100→94→88`、realized loss 12、唯一 opening equity、retry、第二周期 Risk failure 无 sequence Evidence、中段写失败无 commit manifest，以及 journal 重哈希篡改拒绝。下一步先重新排序剩余 fidelity gap，不把 target mutation、partial、reentry、cross-margin 或 Fast 自动升级为既定路线。
- M4-P20 已完成但成熟度保持 M3：新增 committed P19 的只读 Accounting successor，不改写 P16/P17/P18。Runner 先重开 P19 namespace 并逐文件验证六个 payload，再沿其中嵌入的 P15 Evidence/Manifest 重开 P15 namespace，取得并验证 P19 fingerprint 所绑定的真实 Risk Result；不接受仅来自调用栈的未提交对象。Ledger/Journal 以 P19 replacement-aware record hash 与 owner 为主键，写 entry fee、terminal 前 Funding、winning terminal realized PnL、terminal/liquidation fee、collateral reserve/release 和 open mark；preempted terminal 与 terminal 后 Funding 进入 exclusion set。long/short golden 均锁定 replacement gap 后 `100→94`、realized loss 6、唯一 opening equity、balanced Trial Balance、manifest-last、retry、真实中段写失败无 commit manifest，以及 owner 重哈希篡改在 source binding 处拒绝；same-close race 仍以 generation-1 initial stop 结算为 90。下一 fidelity 缺口是将 P19→P20 作为新 successor 传播到 bounded cycle sequence，而不是原地修改 P17/P18 或开放更多 mutation。
- M4-P19 已完成但成熟度保持 M3：新增 P15 的 replacement-aware terminal successor，不改写 P15–P18。每 Lane 最多消费一个 frozen `authorized_protective_stop_replace`，仅允许 tighten-only、full-position、reduce-only stop-market，target 保持不变；replacement decision close 的 bar-range 仍由 generation 1 先取得 terminal，只有后续 bar open/range 才激活 generation 2。旧 Risk/Integrated predecessor projection 必须显式 opt-in，普通入口仍拒绝 mutation，防止“忽略 replacement”被误当作已执行。Golden 锁定 long/short 后续 gap 以 worse-open `94/106` 对称产生 `-6`、新 stop owner、同一 close 的旧 stop 胜出、rejected Lane 不复活、P15 source/hash closure、retry、语义篡改和发布失败无部分 Evidence；六个 payload 后写唯一 manifest。下一 fidelity 缺口是独立 replacement-aware Accounting successor；P16/P17/P18 当前仍只认 P15 initial bracket，不能宣称 replacement 已跨周期传播。
- M4-P18 已完成但成熟度保持 M3：新增 P17 committed Artifact 与逐周期 P16 Artifact 的只读 accounting successor。Runner 先逐文件校验 P17 manifest/ref/sha，再由每个 cycle commit 的 terminal evidence hash、P16 policy 和 accounting evidence hash 定位并验证真实 P16 namespace；不重跑 Allocation、Risk、terminal 或 cash bridge。Consolidated Ledger 保留全部 cycle-local entry/hash 并按 cycle/global sequence 包裹；Journal 仅 cycle 1 保留 `opening_cash`，cycle 2+ opening posting 仍留在原 P16 Artifact，不进入全局 Trial Balance。三周期 golden 锁定唯一 opening equity 100、realized income 10、最终 settled/available/NAV 110、collateral/unrealized 0、P16 evidence/manifest hash closure、retry 幂等、发布失败无 Accounting Evidence、Trial Balance 重哈希篡改拒绝；九个 payload 后写唯一 manifest。P7/P12/P13/P16 均不改写。下一 fidelity 缺口转为把已预声明、已由单资产认证的 protective stop replacement 纳入 Portfolio terminal owner，而不是继续扩 cycle schema。
- M4-P17 已完成但成熟度保持 M3：保留 M4-P12/P13 历史 Result/Artifact，不把旧 `integrated_result.ending_available_cash` 继续当作新周期资金事实；新的 production sequence runner 从同一不可变 Control Plane Sequence Reservation 确定投影每周期 authority，按顺序真实提交 P9 Integrated、P14 Mark/Risk、P15 protective terminal、P16 Ledger/Journal/Trial Balance。下一周期 opening cash 只取前一周期 committed P16 Trial Balance；前序仍有 open Position、reserved collateral、unrealized、gross/net exposure 或 frozen stop risk 时 fail closed。三周期 golden 锁定 `100→110→110→110`、四级 child manifest hash closure、retry 幂等、第二周期 Risk failure 无 Sequence Result、cash bridge 语义篡改拒绝；五个固定 payload 后写唯一 sequence manifest。已提交 child Artifact 在顶层失败时仍保持独立权威，不得称为部分 Sequence Result。下一缺口是把新的逐周期 P16 Journals 合并为单一 opening-equity 的 successor consolidated accounting，而不是开放 runtime cycle append、stop mutation 或 reentry。
- M4-P16 已完成但成熟度保持 M3：新增 P15 committed Artifact 的只读会计后继，不重跑撮合、风险或终态仲裁。Ledger 以 P15 terminal record 为主键串联 entry fee、terminal 前逐笔 Funding、winning terminal realized PnL、统一 terminal trading fee 与 liquidation fee；Journal/Trial Balance 记录 wallet、isolated collateral、收入/损失/费用和 open unrealized，并逐项对齐 P15 cash/NAV。被抢占的旧 terminal 与 terminal 后 applied Funding 进入 hash-bound exclusion set，不得产生经济 posting。Golden 锁定带双边费 stop 的 `100 -> 88.1`、并发 short open collateral/unrealized、phase-15 liquidation、语义篡改、幂等和中段写失败无 commit manifest。P7 历史账保留，P16 是单次 initial Allocation 范围的新权威；下一缺口是把该终态与会计传播到 P10–P13 bounded cycle sequence，而不是开放自由 reentry。
- M4-P15 已完成但成熟度保持 M3：没有改写 P6/P9 历史 Result，而以 M4-P14 committed outcome、P9 Artifact 和 hash-bound Trial bars 生成新的 authoritative protective-terminal successor。范围严格限定一次 initial Allocation/no reentry；每个 Lane 只允许 `not_opened / initial stop / initial target / exact liquidation / strategy exit / open-at-end` 一个 owner。同刻顺序固定 funding `10`、exact risk/liquidation `15`、market protection/strategy exit `20`；同 open stop/target gap 先于 strategy exit，exact liquidation 先于同刻 stop。single touch/gap exact，stop-target collision 复用双路径证据并保守 stop-first，long/short 对称。Evidence 重建 entry fee、终态前 funding、realized/fee、cash/collateral/NAV 与 exposure/risk release；八个 payload manifest-last，retry 幂等，语义重哈希篡改和发布失败无部分结果。该阶段暴露的 P7 双账缺口已由 P16 successor accounting 关闭；P15 本身仍不发布 Journal。
- M4-P14 已完成但成熟度保持 M3：新增 Integrated Portfolio Artifact 的只读 exact-Mark risk revaluation successor。每个 P6 权威 queue event 都生成 hash-chain transition；entry/exact Mark/full close 分别建立、重估、释放 gross/net exposure，admission frozen-stop risk budget 不随 Mark 漂移，prospective mark-to-stop drawdown 独立呈现。动态 cap breach 只写 observation，不触发自动交易；exact Mark 穿越尚未拥有 Portfolio 终态权的 initial stop 时标记 `resolution_limited` 且不制造 Fill。八个 immutable payload 后写唯一 manifest；golden 锁定 `100 → 120` exposure、risk `10` 不变、prospective drawdown `10 → 30`、net-cap breach、stop crossing、幂等和失败无部分结果。权威复读同时暴露并修正 Allocation/Risk/Integrated validator 依赖对象键插入顺序的问题，改为 canonical hash 等值，不改变经济语义。下一 fidelity 缺口是 initial protective stop terminal ownership，不是 stop mutation 或自动 cap action。
- M4-P13 已完成但成熟度保持 M3：没有重写 M4-P12 或周期本地 P7 Evidence；新增只读 Sequence Accounting projector/Runner，从已提交 manifest 逐文件验证 Result 与 cycle Evidence 后，保留本地 entry hash 并生成全局 Ledger/Journal/Trial Balance/Fingerprint。统一 Journal 只接受 cycle 1 opening equity，cycle 2+ opening posting 仅留在原 Evidence，不重复进入 consolidated 账。三周期 golden 从 100 实现 10 收益后以 110 滚存两周期，最终 settled/NAV 均为 110、realized PnL income 为 10；八个 companion payload manifest-last 提交，retry 幂等，读取/语义/发布失败均无部分 accounting evidence。下一 fidelity 缺口转为 dynamic Mark exposure/risk revaluation，不继续增加 cycle schema。
- M4-P12 已完成但成熟度保持 M3：新增 Control Plane 自哈希 `Cycle Sequence Reservation v1`，一次冻结连续 1–8 周期的 Allocation/Risk Plan、Trial/Lane、priority、caps 与时点；后续 opening cash 不使用 Control Plane 估值，只由前序 committed ending available 派生。通用 Sequence Runner 逐周期复用 P8/P6/P7/P9，强制 predecessor full-flat 且 successor 严格更晚，固定六种 Artifact role 承载动态 cycle evidence，禁止运行时 append/search expansion。Golden 同一入口通过 1/2/3-cycle、cash roll-forward、第二周期 Risk failure 无部分 Result/Artifact、幂等与扩容拒绝。P10/P11 未被改写，继续是 cycle-2 历史兼容合同；下一 fidelity 缺口是跨周期 consolidated Journal/Trial Balance，而非增加 cycle 9。
- M4-P11 已完成但成熟度保持 M3：没有新增重复 Control Plane authority；Two-Cycle orchestration 精确闭合 P10 Reallocation authority/evidence 与既有 P6 Risk Reservation。cycle-2 Allocation Result 成为第二段 entry 唯一 admission source，随后 exact funding/Mark/full exit/liquidation 复用 P6；跨周期 state chain 强制 cycle-1 full-flat release 严格早于 cycle 2，且前序 ending available、Allocation/Risk shared initial 与首 transition wallet 完全一致。十四个 payload 后最后写 two-cycle manifest。Golden 锁定第二周期 net-cap rejection、full exit exposure/risk release、cash bridge、authority drift、幂等与 Risk failure 无部分 Result/Artifact。当前只认证恰好两个周期，不是 bounded-N scheduler。
- M4-P10 已完成但成熟度保持 M3：Control Plane 新增 immutable `Replay Portfolio Reallocation Reservation v1`，绑定前序 Integrated Result/committed Manifest、cycle-2 Plan、最早时点、caps、priority 与 current Trial authority。Replay 必须证明前序 open Position 为空、gross/net/risk 归零，并定位 full exit/liquidation 的实际归零 transition；cycle 2 必须严格晚于该 transition，opening cash 只取前序 `ending_available_cash`。生产 Runner 复用 P8 Allocation Engine，七个 immutable payload 后最后提交 reallocation manifest；Golden 锁定第二周期 priority/caps、幂等复读、非全平前序拒绝和 Engine failure 无部分 Result。当前尚未把 cycle-2 Allocation 接入 funding/Mark/exit/liquidation，且明确无 cycle 3、任意循环、partial close 再分配、cross-margin、borrow、真实 liquidity 或 Fast。
- M4-P9 已完成但成熟度保持 M3：新增无独立经济 authority 的 `Integrated Portfolio Plan/Result/Outcome/Artifact v1`，只闭合 Control Plane Allocation/Risk Reservations 及同一 Experiment/Trial Group、cash、settlement、priority、child Trial authority。所有 Lane 仅允许一次共同 initial entry time；P6 successor admission 必须逐字段匹配 P8 Allocation Event 并继承其 fill hash/reason，故 exposure/risk rejected Lane 不会被 cash-only 路径复活。统一 state chain 对每个 P6 queue event 串联 wallet、fixed entry-notional gross/net exposure 与 frozen-stop risk；full strategy exit 或 forced liquidation 原子释放 exposure/risk。九个 payload 后写唯一 integrated manifest，Fingerprint 同时绑定双 Plan/Reservation/Result、P7 evidence 与 state chain。Golden 锁定 net-cap rejection、exit/liquidation 两类释放、幂等复读、重哈希 state tamper 与 Risk failure 无部分 Result/Artifact。当前仍无第二次 Allocation/re-entry、dynamic exposure、partial、cross-margin、borrow、真实 liquidity 或 Fast。
- M4-P8 已完成但成熟度保持 M3：Control Plane 新增 `Replay Portfolio Allocation Reservation v1`，冻结共享 initial cash、唯一 priority、gross/absolute-net/portfolio risk caps、逐 Lane risk cap、计量口径与拒绝优先级；Replay 新增独立 Allocation Plan/Result/Outcome、Engine 与生产 Runner。Engine 对同一 execution time 先收齐并 hash 完整 candidate set，再按 `lane risk -> cash -> gross -> absolute net -> portfolio risk` 逐 priority full-fill-or-reject；Cycle 完成后才写 Fill，long/short 只抵 net、不抵 gross。Exposure 使用 entry execution notional；risk 使用 entry 到 adverse frozen stop 的价格损失加 entry/stop round-trip fee。Golden 锁定输入重排、priority winner、long/short gross/net、成本风险、拒绝优先级、重哈希语义篡改与 Engine failure 无部分 Result。该 entry-only epoch 尚未接入 M4-P6 funding/risk/liquidation 生命周期和 M4-P7 Artifact，不认证 exit、dynamic exposure、cross-margin、partial fill/liquidation、borrow、真实 liquidity 或 Fast。
- M4-P7 已完成但成熟度保持 M3：Accounting 将已验证的 M4-P6 全局经济队列只读投影为 Portfolio cash Ledger、双重记账 Journal、Trial Balance 和 authority/source-complete Fingerprint；available wallet 与 isolated collateral 独立入账，并强制 `available + reserved = settled`、`settled + unrealized = NAV`。Runner 以 certified immutable store 先写 11 个 canonical payload，最后写唯一权威 `portfolio-artifact-manifest.json`；中断孤儿非权威，create-or-identical 重试可补齐，语义漂移 fail closed。该能力不重新撮合、不修改风险结果，也不授权 Review、晋级或策略落盘。
- M4-P6 已完成但成熟度保持 M3：M4-P5 明文禁止 liquidation，故新增 Control Plane `Runtime Shared Wallet Risk Reservation v1` 与独立 Risk Plan/Result/Outcome、Engine、生产 Runner。Plan 冻结完整 Funding/Mark event set、venue-risk/status epoch、Numeric increments 与 cost facts；Runner 仅接纳 `complete_grid` exact Mark、event-time availability、完整 status history、unit linear multiplier 与 isolated margin。全局队列固定 `funding -> risk observation -> full liquidation -> strategy exit -> entry -> Control Plane priority`；risk 读取 Lane-attributed entry fee/funding、exact-mark unrealized PnL 与 frozen maintenance tier，breach 取得同刻终态所有权。Golden 锁定 long `100 -> 80`、20 collateral：margin balance 0/maintenance 4，forced close realized `-20` 后钱包 `100/20/80 -> 80/0/80`，再允许同刻 Lane B 以 80 collateral 入场；另锁定 long/short 各 `-20` 对称、输入 permutation、risk-event tamper、halted breach、isolated deficit、缺 Mark 与 Engine failure 无部分 Result。当前仍无完整 Portfolio Journal/Artifact/Fingerprint、cross-margin、partial liquidation、borrow、insurance/ADL、真实 liquidation trade 或 Fast parity。
- M4-P5 已完成但成熟度保持 M3：M4-P4 明文禁止 funding，故新增 Control Plane `Runtime Shared Wallet Funding Reservation v1` 与独立 Funding Plan/Result/Outcome、Engine、生产 Runner。Plan 对每 Lane 冻结 funding event count/hash 与 settlement increment；Runner 只接受 event-time availability、严格递增 UTC event、finite rate/positive mark，遗漏、重复或替换 fail closed。全局队列按 event time、同刻按 `funding -> exit -> entry` role、角色内按 Control Plane priority；funding 使用 t-minus Position 和 Numeric Policy v3，正费率 long 支付、short 收取，并在后续 exit/admission 前提交 settled/available cash。Golden 锁定 long 支付 1.1 后把同刻后续 107 cash admission 从 filled 改为 rejected，另一个 fixture 锁定 long `-1`/short `+1` 对称、未持仓 funding `not_reached`、输入 permutation parity、event-set omission/duplicate 与 Engine failure 无部分 Result。当前仍无 Portfolio risk/liquidation、partial、cross-margin、borrow、完整 Journal/Artifact 或 Fast parity。
- M4-P4 已完成但成熟度保持 M3：M4-P3 Reservation 明文禁止 exit/cash release，故没有原地放宽；Control Plane 新增 `Runtime Shared Wallet Lifecycle Reservation v1`，Replay 新增独立 Lifecycle Plan/Result/Outcome、Engine 与生产 Runner。Plan 绑定 frozen child Request/Reservation/Lease 及最多一次 full-position reduce-only market exit；全局队列按 event time、同刻按 `exit -> entry` role rank、角色内按 Control Plane priority。每次 exit 原子提交 realized PnL、exit fee 与 isolated collateral release，下一 entry 才读取 available cash。100 USDT golden 锁定 Lane A 入场后 `settled=99/reserved=60/available=39`，110 全平后 `107.9/0/107.9`，使同刻需要 101 的 Lane B 成交，终值 `106.9/100/6.9`；输入 permutation parity、闭仓证据 tamper、未获资金 exit `not_reached` 零变更、unsupported funding 与 Engine failure 无部分 Result 均已测试。当前仍无 exact funding、risk/liquidation、partial、cross-margin、完整 Portfolio Journal/Artifact 或 Fast parity。
- M4-P3 已完成但成熟度保持 M3：P2 静态 Reservation 明文禁止 runtime reuse，故未原地升级；Control Plane 新增 `Runtime Shared Wallet Reservation v1`，只对同一 frozen Experiment/Trial Group 的 current reserved Trials 授权唯一本金、settlement asset 与连续 priority，不含 Lane 资金配额。Replay 新增 Runtime Plan/Result/Outcome、entry-only Engine 与生产 Runner；child Request/Reservation/Lease hash 仍是身份权威，但 child `initial_cash`/Result 不被重解释。Runner 只接受 isolated、market next-open，同一全局 bar-open 队列按 event time、同刻按 authority priority；每个 Fill 先以共享 decimal/accounting 原语计算 slippage/fee，再原子提交 `settled_cash/reserved_collateral/available_cash`，下一 Order 读取新余额。100 USDT fixture 锁定 `60 collateral + 1 fee` 后只余 39，后续 40 请求拒绝；输入重排 parity、priority owner 翻转、跨时刻 time-first、Position↔Fill hash、资金守恒、unsupported Limit 与 Engine failure 无部分 Result 均已测试。当前仍无 exit/funding/liquidation/cash release、完整 Portfolio Journal/Artifact、cross-margin 或 Fast parity。
- M4-P2 已完成但成熟度保持 M3：Control Plane 新签发 `Shared Initial Capital Reservation v1`，只接受同一 Experiment/Trial Group 的当前 reserved child Trials，authority window 必须被全部 child Reservation 包含；一个 settlement asset 资金池按显式连续 `priority_rank` 完整静态分配，确定性十进制求和拒绝超配、少配、重复 Lane/Trial/run/Reservation 和弱化 limitations。Replay Shared Batch 复核 Plan/Request/Reservation/Lease/observed-time，按 authority priority 调用正式 `runReplayTrial`，但 child Result 保持 canonical lane order；priority 改变会改变 Shared Outcome hash，不会改写 child evidence。Shared Result 绑定单一 initial cash、aggregate ending/net 与 independent Result hash，child failure 时 shared/child Result 均为 null。测试锁定 3000 USDT 不重复计本、反向 priority、allocation/window drift、数据库 Trial terminal fencing、field/hash tamper 与 failure overclaim。该纵切只是 static preallocation，不是 runtime shared wallet、同刻资金竞争、cash reuse、cross-margin 或 concurrent matching。
- M4-P1 已完成但成熟度保持 M3：新增 `Independent Lane Batch Plan/Result/Outcome v1` 与正式 Batch Runner，至少两个 Lane 分别绑定 child Request、Trial Reservation、Attempt Lease 和预分配初始资金；执行固定按 `lane_id` canonical order 调用现有 `runReplayTrial`。每个 child 的 cash、isolated margin、Order State、checkpoint、Result/Artifact 完全隔离，外层只汇总 child initial/ending/net evidence，并永久标注不是可支配 Portfolio NAV、无 cross-lane netting/rebalancing/cross-margin/global priority。任一 child failed/cancelled 或缺 Result/Artifact，Batch `result=null` 且不发布部分结果。测试锁定输入重排 parity、资金合计、exact lane coverage、authority/allocation drift、Plan/Lease/Artifact tamper 与 all-or-nothing failure。这只是多 Trial 编排 seam，不认证 shared cash、并发撮合或 cross-margin。
- M3-G8 已完成：新增受限 initial take-profit Replace，而未把它升级为通用 runtime amend。Schedule v11 最后一项且唯一 position mutation 才可冻结；Intent 精确绑定 run-scoped initial target、原/新价格、full-position reduce-only、stop-preserved 与 cancel→submit→activate。runtime 必须证明 target/stop active、数量与 open Position 对齐，新 target 不等于旧值、仍在 stop 另一侧且按 closed-bar mark 尚未触发。bar/risk terminal 先于 phase-90 Replace，故 Replace bar 旧 target Fill 胜并使 Decision not-reached。成功后 stop id/trigger/quantity 不变，原 target 以 `take-profit-repriced` 取消，新 target 使用 generation-scoped id；former-target touch 不可达。Request v36、Simulator v22、Checkpoint v29、Timeline v13、Result v50/Fingerprint 与 Artifact v52 绑定 replacement generation。测试锁定后续新 target、旧 target 不可达、同-bar 原 target 优先、clean/resume 与 rehashed checkpoint tamper。多次 reprice、组合 mutation、已触发 target 重挂和自由 amend 仍未开放。
- M3-G7 已完成：真实 liquidity partial 继续因缺 L2 snapshot、continuous depth delta、queue-ahead、allocation policy 与外部 completeness 而拒绝，不新增空准入合同。正式纵切改为冻结 initial protective stop Cancel：Schedule v10 最后一项且唯一 position mutation 才可授权，runtime 必须证明 stop/target active、数量与 full open Position 对齐；bar/risk terminal 先于 phase-90 Cancel。成功后进入 `target_only`，former-stop touch 不再可达，保留 target、exact liquidation 与 EOD。OHLCV Evidence v5 绑定双 Order status，target-only 两条 path 只能归属 target；Request v35、Simulator v21、Checkpoint v28、Result v49/Fingerprint、Artifact v51 与 Order State Snapshot 绑定取消事实。测试锁定后续 target、stop-vs-cancel race、EOD、clean/resume、单一 Cancel 与 rehash tamper。无保护状态、组合 mutation、通用 runtime cancel/amend 与真实 partial 仍未开放。
- M3-G6 已完成：pre-entry Stop-market 进入与 Limit 共用的 closed-bar GTD 协议。Request v34 允许 `stop_market+gtc|gtd`，GTD expiry 必须严格晚于 earliest executable 且落在 supplied executable bar close；到期 bar 的 open/range trigger 先于 phase-90 expiry，触发后沿既有 Fill 与 Stop Entry Same-bar Path Ambiguity 协议，只有确定未触发才 expired。Order State 保存 Stop entry TIF/expiry；Pending Resolution v4、Checkpoint v27、Result v48/Fingerprint、Artifact v50 绑定终态。测试锁定 zero-execution expiry、trigger-first、同 bar ambiguity 不被 expiry 掩盖、clean/resume 与 rehash tamper。FOK 因当前 bounded-full-fill/zero-fill 下与 IOC 无可观察差异而未开放；任意 intrabar GTD 仍 unsupported。
- M3-G5 已完成：真实 liquidity partial 的数据前提审计结论为“不足”。现有 aggregate-trade archive 没有 hypothetical order queue-ahead、L2 snapshot/continuous delta 或 allocation policy，且其外部 completeness 与 OHLCV bar-link 未认证，故不得把聚合成交量升级为 maker partial 事实。改以可证明的 pre-entry Limit GTD closed-bar expiry 作为 bounded 纵切：Request v33 只接受严格晚于 earliest executable、且等于 supplied executable bar close 的 `expires_at`；到期 bar 先处理 open/range，strict-cross Fill 胜，exact touch typed-fail，确定 non-fill 才在 phase-90 expired。Pending Resolution v3、Checkpoint v26、Result v47/Fingerprint、Artifact v49 与 Order State Snapshot 绑定 TIF/expiry；测试锁定 admission、终态优先级、clean/resume、Artifact retry 和 rehash tamper。任意 intrabar GTD、Stop GTD、FOK、IOC remainder 与真实 partial 保持 unsupported。
- M3-G4 已完成：Take-profit Cancel 只允许作为最后且唯一的持仓后 Order mutation，精确绑定 run-scoped 初始 target，并在 runtime 证明 target/stop active、数量与仓位一致后于 closed-bar phase-90 撤销 target；stop id/trigger/quantity 原样保留。Source Reducer 支持 `bracket -> stop_only` 单向保护状态；former-target touch 不再成交或制造路径歧义，stop、exact liquidation、EOD 仍有效。同一 cancel bar 内 target touch 先于 close-time Cancel，Decision 记 `not_reached_terminal`。OHLCV Evidence v4 绑定 protection mode/target status；Checkpoint v25、Result v46/Fingerprint、Artifact v48 与 Order State Snapshot 绑定取消事实。测试锁定后续 stop、EOD、target race、clean/resume、单一 Cancel 与重新哈希篡改拒绝。止损 Cancel、无保护持仓、组合 mutation 与通用 order book 未开放。
- M3-G3 已完成：新增首条受限 position-open runtime Cancel，只允许后续冻结 closed-bar 决策在 `strategy_exit` 的 signal 之后、earliest executable boundary 之前撤销该唯一 `submitted` Order。Intent 绑定目标 exit decision sequence、effective time、固定 policy/reason；Harness/Decision Timeline 必须复算同一 hash。Engine phase-90 只产生 `strategy_exit cancelled` OrderEvent，保护 stop/target 不变并继续拥有 terminal；late/equal-boundary、错误 target、缺失或非 submitted Order 均失败关闭。Checkpoint v24 保存 cancelled Order 与已消费 Timeline，resume 不重复 Cancel；Result v45/Fingerprint、Artifact v47 通过 Order State Snapshot 绑定终态。Golden 覆盖 `submit exit -> cancel -> target fill`、clean/resume hash parity、单一 cancel event、late/wrong-target tamper。该能力不等于通用 runtime cancel/amend 或多 pending matching。
- M3-G2 已完成：新增权威 Order State Snapshot，不再要求审计者从 `order-events.jsonl` 猜测最终订单集合。Engine 每次 transition 同步更新按 submission sequence 排序的 Order map；Snapshot 强制 Order id 唯一、数量守恒、每个 OrderEvent order 全覆盖，以及 Order last sequence/EventKey/status/remaining 与最后事件一致。Checkpoint v23 保存完整 Snapshot，修复 replace/partial checkpoint 只保留当前 protection、resume 丢失旧 cancelled Order 的缺口；clean/resume Result hash 已恢复一致。Result v44/Fingerprint 绑定 `order_state_snapshot_hash`，Artifact v46 新增独立 `order-state-snapshot.json` required role，Runner 首次发布与幂等复读均重验。并发 stop/target terminal-owner golden、输入 Order 数组重排 metamorphic、重新哈希后的 snapshot/checkpoint 语义篡改均已锁定。该纵切认证现有受限多 Order 状态闭包，不宣称通用多 pending matching 或任意 runtime cancel/amend。
- M3-G1 已完成：单一 v12 cutover Receipt 内完成真实 successor fresh process、第二 v10 Response、双 Schedule match、actual reproducibility pair 与正式 `executeReplayDecisionHarness` economic admission；同一 Control Plane-bound Request、真实 Dataset hash、Trial Reservation hash 与 successor Lease 随后进入 `runReplayTrial`，Receipt hash 被同一 Decision Evidence Timeline、Result Fingerprint 与 committed Artifact 绑定，第二次运行从 Artifact 幂等复读。长链测试锁定 distinct PID/process identity/authority lineage、canonical inner Response exact parity、fresh Control Plane spawn-boundary revalidation、CAS attempt orphan fail-closed 与 retry 不重启进程。R4.145–R4.151 的 exit-70 未激活 artifact 不被回写；cutover 显式绑定既有 activated artifact，并对新 Transport/Command/Intent/Capsule 再 revalidate 后 spawn。历史 v11/v9 Receipt 继续可读，新的 golden 以 v12/v10 为权威路径；成熟度升为 M3。
- 已收敛：停止以单个零实例合同推进 `R4.152+`，R4.151 保持 evidence-chain 冻结点；M3-G1 的 Functional/Evidence/Cutover 七门已在一个 bounded change set 内同时通过。v9/v10 切换由单一 adapter 完成，没有逐个升级 Result/Artifact/Checkpoint；下一项功能工作必须先定义新的 bounded milestone 与可机读完成门禁，不恢复 phase-number/schema treadmill。
- 已补：Replay R4.151 从 exact durable R4.150 Capsule 与 R4.149 Intent 物化 generation-specific spawn-boundary Revalidation Request、Control Plane Receipt 的 durable copy 与 Replay Binding，但不启动 worker。审计确认现有 Control Plane Request/Receipt schema 和 `revalidateReplaySpawnBoundary` producer 已是 generation-neutral，无需新增 CP 合同或放宽权威边界；Replay 先将 Capsule-bound Request canonical CAS 落盘，才可调用窄 `revalidate(request)` port，首份合法 Receipt 再以 Request key 固定唯一 durable slot，最后写 Binding。Receipt 必须证明 current Attempt 仍为 `running`、Attempt/ordinal/worker/generation/Lease hash/expiry 与 successor Capsule 完全一致，CP clock-bracketed read start 严格晚于 R4.149 `intent_issued_at`；pre-Intent/equal-time evidence fail closed。Binding 只记录 Capsule/Intent/Request/Receipt exact hash 与 canonical file SHA-256 closure，不递归嵌 lineage。Request 已写但 CP 未返回可重试；Receipt 已写但 Binding 未写时不再调用 CP；完整 retry 也不重采 clock。测试锁定 missing parents、stale chronology、CP port call-once、Request/Receipt/Binding/Capsule/Intent tamper 与零进程权限。当前 successor Lease/Envelope/Transport/stdio/probe/artifact-bound Transport/Execution Admission/Dispatch Claim/Command/Intent/Capsule/Revalidation Request/Receipt/Binding 均为 `1`，probe processes=`5`；worker process/Request Frame/decode/second Response/second Schedule/pair/Harness Receipt 均为 `0`。Binding 只授予 `single_immediate_attempt_bound_process_start_candidate`，`process_start_evidence=none`，blocker 从三项降为两项，成熟度仍 M2。
- 已补：Replay R4.150 从 exact durable R4.149 Process Launch Intent 物化 generation-specific Authority Capsule，但不做 spawn-boundary revalidation、不启动 worker。Capsule 的唯一直接父证据是 R4.149 regular canonical file；registry 校验 exact key/hash/self-hash 并记录 file SHA-256，不再复制第一 lineage Capsule 全量嵌入 Intent 的递归结构。内层 artifact-visible environment value 严格沿用 activated-stdio 已认证的七字段 schema，由 committed successor Intent 唯一派生 Transport/artifact/Envelope/Command/Intent/logical Request/Worker Request hashes，不接受 caller field、本地时间或 registry metadata；canonical JSON 原文与 `capsule_hash` 分别冻结，外层 `record_hash` 不进入环境值。每个 exact Intent 只有一个 create-or-identical Capsule slot，跨 Command/Intent/Attempt/generation 复用禁止。测试锁定 missing/tampered R4.149 parent、七字段 binding、canonical/hash、retry、Capsule file tamper 与全部零进程权限。当前 successor Lease/Envelope/Transport/stdio/probe/artifact-bound Transport/Execution Admission/Dispatch Claim/Command/Intent/Capsule 均为 `1`，pre/post Command Lease observation/receipt/clock 各为 `1`，probe processes=`5`；spawn revalidation/worker process/Request Frame/decode/second Response/second Schedule/pair/Harness Receipt 均为 `0`，blocker 从四项降为三项，`process_launch_authority=not_granted_until_fresh_spawn_boundary_revalidation`，成熟度仍 M2。
- 已补：Replay R4.149 从 exact durable R4.148 Command Admission 物化 generation-specific Process Launch Intent，但不创建 Capsule、不做 spawn-boundary revalidation、不启动 worker。Intent 必须绑定一套严格晚于 Command issued_at 的 fresh Control Plane active Lease observation、immutable registry read receipt 与 authority-internal dual-sample clock attestation；current Lease hash、Attempt/worker/generation 与 Command 完全一致，intent_issued_at 取 post-Command read completion，valid_before 继续等于 Command/Lease expiry。审计发现 R4.148 只携带 artifact hash、不携带 runtime executable evidence，因此 Intent 以 R4.148 为主父证据，并以 exact durable R4.147/R4.146 作为 Transport bounds 与 stdio runtime/version/executable hash 的窄支撑父证据；只记录三份 canonical file SHA-256/hash closure，不重嵌旧 lineage。Intent natural key 固定每个 exact Command 最多一个实例，换 clock/evidence 必须 same-key conflict；Intent 仅冻结未来 Capsule/launcher 输入，明确 `process_launch_authority=not_granted_until_capsule_and_fresh_spawn_boundary_revalidation`。测试锁定 missing parent、post-Command chronology、runtime/artifact/Lease binding、retry、zero-process authority，以及 Intent/R4.148/R4.147/R4.146 任一 durable file tamper。当前 successor Lease/Envelope/Transport/stdio/probe/artifact-bound Transport/Execution Admission/Dispatch Claim/Command/Intent 均为 `1`，pre/post Command Lease observation/receipt/clock 各为 `1`，probe processes=`5`；Capsule/revalidation/worker process/Request Frame/decode/second Response/second Schedule/pair/Harness Receipt 均为 `0`，成熟度仍 M2。
- 已补：Replay R4.148 从 exact durable R4.147 Execution Contract Admission 签发 lease-generation successor 的 generation-specific Dispatch Claim 与 Execution Admission Command，但不创建 Process Launch Intent、不启动 worker。新 claim 的 CAS natural key 精确绑定 R4.147 parent、logical Request、Attempt/worker 与 successor generation；同 key 不同 claimant/claimed_at fail closed，claim 只提供本地排他性。Command 必须同时绑定 fresh Control Plane active Lease observation、immutable registry read receipt 与 authority-internal dual-sample clock attestation；其 issued_at 取 clock read completion、valid_before 取当前 Lease expiry，且 observation 必须严格晚于 claim。Command 只授权 exact successor Process Launch Intent creation，Worker Request 仍为 `not_granted/not_invoked`，Transport activation、Intent/Capsule/revalidation/process/frame/Response/economic authority 均未开放。实现继续采用 thin direct-parent hash closure：R4.147 regular canonical file 的 key/hash/self-hash/file SHA-256、claim、Command 与 aggregate 各自 immutable CAS，read/retry 检查 parent/child/aggregate tamper，不递归重嵌入旧 lineage。测试锁定 fresh generation identity、missing parent、官方 observation/receipt/clock chronology、claim conflict、idempotent retry、三层 child tamper 与 parent tamper。当前 successor Lease/Envelope/base Transport/stdio/probe/artifact-bound Transport/Execution Admission/Dispatch Claim/Lease observation/registry receipt/clock/Command 均为 `1`，probe processes=`5`，但 Intent/Capsule/revalidation/worker process/Request Frame/decode/second Response/second Schedule/pair/Harness Receipt 均为 `0`；成熟度仍 M2。
- 已补：Replay R4.147 从 exact durable R4.146 Stdio Probe Admission 物化 lease-generation successor 的 artifact-bound Transport Contract 与 zero-instance Execution Admission Contract，但不签发 Command、不启动 worker。审计拒绝直接复用 R4.120/R4.121 的完整递归嵌入迁移 schema：它们把第一 Envelope lineage 重嵌入每个后继对象，套到 R4.146 后产生证据树体积与 canonical validation 的 O(depth²) 放大，也会把 `r4_120` 临时命名升级成长期制度。新 generation-specific v1 contracts 改用 exact durable hash closure，冻结 base Transport、stdio capability、negative Probe Receipt、successor Envelope、process artifact、frame schema/resource bounds、Attempt/Lease identity 与未来 Command required bindings；两份小型 child contract 及 aggregate 均 canonical/self-hashed。首次注册读取 direct R4.146 file、验证其 body self-hash，并记录 canonical file SHA-256；后继 read/retry 以 regular-file + key/hash + file SHA-256 检测 parent 篡改，再验证 child/aggregate CAS，不递归重放全部祖先。测试锁定 new/first artifact Transport 与 Execution Admission identity 均不同、stdio artifact hash 可相同但不授权复用、missing/tampered parent、child/aggregate tamper、retry 与全部零实例权限。当前 successor Lease/Envelope/base Transport/stdio/probe/artifact-bound Transport/Execution Admission=`1/1/1/1/1/1/1`，probe processes=`5` 但 worker process/Command/Intent/Capsule/revalidation/Request Frame/decode/second Response/second Schedule/pair/Harness Receipt 均为 `0`；成熟度仍 M2。
- 已补：Replay R4.146 从 exact durable R4.145 Transport Admission 重建 successor stdio capability，并以该 fresh capability 实跑新的五类 malformed-input negative probes。即使 successor 与 predecessor 的 stdio artifact bytes/hash 完全相同，capability identity 仍必须因 base Transport parent hash 不同而分离；相同字节只证明 deterministic rebuild parity，不允许复用旧 attestation。新的 Probe Receipt 精确绑定 successor capability，五个 child process 只消费 invalid UTF-8、空输入、多 frame、非 canonical JSON 与 schema-invalid frame，Worker Request frame/write/decode 均为 `0`；它们不是第二个 worker process，也不能计入 reproducibility member。registry 只接受 exact durable R4.145 parent，canonical CAS、create-or-identical、retry 不重采 clock、parent/binding/file tamper 均 fail closed。当前 successor Lease/Envelope/base Transport/stdio/probe=`1/1/1/1/1`，artifact-bound Transport/Execution Contract/Command/Intent/Capsule/revalidation/worker process/second Response/second Schedule/pair/Harness Receipt 均为 `0`；stdio 与 probes 只认证未激活 process surface，无 Command/process/Harness/Signal/Order/economic/Trial authority，成熟度仍 M2。
- 已补：Replay R4.145 从 exact durable R4.144 Envelope Admission 物化 lease-generation successor 的 zero-instance **base Transport Contract**，但不创建 stdio capability、Command 或第二进程。审计确认旧 `successor-transport-contract` 是 R4.120 的 decoder→stdio 工件迁移桥，完整嵌入第一 Envelope，不能被误当作 generation successor；新 Successor Execution Transport Admission 显式冻结复用边界：Code Admission/source bundle、logical Request/Response Contract、v10 Build Capability/decoder bytes、协议 schema 与资源上限可复用，凡 hash/语义嵌入旧 Envelope 的 base Transport、stdio capability、negative probe、artifact-bound Transport、Execution Admission/Command/Intent/Capsule/revalidation/process 均必须重建。通用 Transport registry 现在只在读到 exact durable R4.144 Admission 时接受 successor Envelope，缺 parent 或裸 Envelope fail closed；binding record 全量嵌入 predecessor/new Transport 并自哈希，canonical CAS、create-or-identical、exact-key tamper 已测试。另修正 stdio/activated-stdio immutable registry 读取不再隐式重编译，首次登记与显式 deterministic rebuild 仍保留编译认证。当前 successor Lease/Envelope/base Transport=`1/1/1`，stdio/probe/artifact-bound Transport/Execution Contract/Command/process/second Response/second Schedule/pair/Harness Receipt 均为 `0`；Transport 仅冻结 frame root，无 transport activation、Command/process/Harness/Signal/Order/economic/Trial authority，成熟度仍 M2。
- 已补：Replay R4.144 从 exact durable R4.143 Successor Lease Admission 物化 predecessor-linked successor Execution Envelope，但不创建 Command 或第二进程。现有 Envelope v1 已正式支持 `same_attempt_lease_generation_successor`，因此不升级 schema；Runner 从 R4.143 内嵌 R4.141 lineage 恢复 exact 第一 Envelope，以已接纳 successor Lease 和同一 Response Contract/logical Request 构造新 Envelope。新增 Replay-owned Successor Execution Envelope Admission 将 R4.143 Admission、第一 Envelope 与 successor Envelope 全量嵌入并自哈希，锁定同 Attempt/ordinal/worker、generation `+1`、immutable request/context/Reservation、exact predecessor hash 与 exact successor Lease hash；registry 使用 canonical JSON local immutable CAS，create-or-identical、missing durable parent、field/hash/regular-file tamper 均 fail closed。当前 successor Lease/Envelope=`1/1`，Command/Intent/Capsule/revalidation/process/second Response/second Schedule/pair/Harness Receipt=`0`；Envelope 只授权后续 fresh successor Command construction，无 process/Harness/Signal/Order/economic/Trial authority，成熟度仍 M2。
- 已补：Replay R4.143 将 exact durable R4.141 selection 正式接入 R4.142 Control Plane renewal producer，但止于 successor Lease admission，不启动第二进程。Replay 从 R4.141 内嵌第一 lineage 确定性派生 renewal Request，绑定 exact authority/Pair/first Schedule/first Envelope、logical/Worker/Replay Request 与 predecessor fencing token；Request 先以 canonical JSON 写入 local immutable CAS，再通过窄 `renew(request)` authority port 调用 Control Plane。CP SQLite adapter 在已有单事务 producer 上返回持久化 Receipt；Replay 对 Request、Receipt、predecessor/successor Lease、generation `+1`、Attempt immutable bindings 与全部 parent hashes 做全量校验，重新确认 durable parents 后写入不可变 Successor Lease Admission。CP 已提交但 Replay 未提交时，同一 Request 可幂等回收 Receipt；Replay admission 已存在时 retry 不再调用 CP；同一 natural key 改 expiry、parent/Receipt tamper、非 canonical/非 regular file 均 fail closed。当前正式 renewal Request/CP Receipt/successor Lease Admission=`1/1/1`，successor Envelope/Command/Intent/Capsule/process、second Schedule、pair、Harness Receipt 仍=`0`；Lease 仅可用于构造 fresh predecessor-linked lineage，无 process/Harness/Signal/Order/economic/Trial authority，成熟度仍 M2。
- 已补：Replay R4.142 在 Research Control Plane 增加 Successor Verification Lease Renewal Request/Receipt v1 与原子 producer，但不虚构 R4.141 已贯通。Request 固定 `second_reproducibility_member_same_attempt_successor_generation` 用途，绑定 successor-authority/Pair/first Schedule/first Envelope hashes、logical/Worker/Replay Request identity、Attempt/ordinal/worker、expected generation/Lease hash、minimum successor generation 与 proposed expiry；Request 本身无 renewal/process/Harness/economic authority。Control Plane 在单一 SQLite immediate transaction 内先按 source Request/contract create-or-identical，重验 active Attempt、Request hash 与 fencing token，使用 authority-internal clock 生成 heartbeat，执行 generation `+1` 后将 predecessor/successor Lease 全量嵌入 immutable Receipt 并插表；失败整体回滚，retry 不重采 clock，竞争 identity、stale generation、UPDATE/DELETE 均拒绝。Control Plane 只把 Replay parent 当 opaque hash，不反向导入 Replay lineage；Receipt 只授予 successor Lease，fresh Envelope/Command/Intent/Capsule/process 仍必需。当前 producer capability 已认证，但尚无从 exact durable R4.141 派生并消费的正式 Request/Receipt，故实际 successor lineage/second Schedule/pair/Harness Receipt 仍=`0/0/0/0`，成熟度不升 M3。
- 已补：Replay R4.141 物化 Worker v10 Successor Verification Authority Contract v1，正式选择 `same_attempt_higher_lease_generation`，但不越权续租、不物化第二 lineage。原因不是偏好：双进程复现是同一次 Attempt 的执行认证义务；Control Plane 新 Attempt 仅用于前一 Attempt terminal/expired 后的授权恢复，不能拿 retry ordinal 伪装独立双跑。Registry 只消费 exact durable R4.140 Pair Contract，绑定第一成员的 Envelope/Lease hash、Attempt/ordinal/worker/generation，并要求未来由 Control Plane 在单一权威事务中证明 Attempt 仍 current active、未取消/未 fencing 后续租；successor generation 至少为当前 `+1`，Trial/run/Reservation/Request/worker/claimed_at 与 logical Worker identity 全等，随后必须新建 predecessor-linked Envelope 及全新的 Command/Intent/Capsule/revalidation/process lineage。当前 successor Lease/Envelope/Command/Intent/Capsule 均为 null，lineage/second Schedule/pair/Receipt=`0/0/0/0`；测试锁定 missing durable parent、create-or-identical、selection/immutable requirements 与落盘 tamper，Signal/Order/economic/Trial authority 全为 none，成熟度不升 M3。
- 已补：Replay R4.140 物化 Worker v10 Reproducibility Pair Contract v1，但不虚构第二次运行。Registry 只消费 exact durable R4.139 admitted Schedule Admission，绑定第一成员的 logical Request、Worker Request/Response、Schedule、Request、process launch/dispatch/validation identities；合同冻结跨 pair 必须全等的 19 个 protocol/code/input/output bindings，以及必须不同的 13 个 Command/Intent/Capsule/process/dispatch/admission bindings。现有 Capsule 明确 single-use，禁止用同一 authority lineage 再 spawn 或重读旧 Receipt 充当第二成员；后继 authority 可采用 same-Attempt new generation 或 Control Plane-authorized new Attempt，但本阶段不提前选择。测试锁定 missing durable parent、create-or-identical、完整 requirement sets 与落盘 tamper。当前 first/second Response=`1/0`、required=`2`、pair=`0`、Harness Receipt=`0`，Signal/Order/economic/Trial authority 全为 none，成熟度不升 M3。
- 已补：Replay R4.139 物化 Worker v10 Authority Schedule Admission v1。Registry 只消费 exact durable R4.138 admitted Response Validation，并以其完整 spawn revalidation lineage 中 Control Plane `current_attempt_lease.request_hash` 校验传入 Request v30；随后由 Worker Context 的 sequence/time 唯一选择冻结 Schedule entry，重验 Context、Decision/Market/State snapshots 与 Request 的边界一致性，再比较 Worker claim 和 `replayDecisionOutputFor(Request, entry)` 的 canonical 投影。测试锁定真实 authority child 的 initial-order exact match、错误 Request hash、missing durable parent、retry 与落盘 tamper。成功只授予 `granted_exact_boundary_match`；当前独立 Response 实例仍为 `1/2`、Harness Receipt 为 `0`，不得把一次 Schedule match 伪装成 reproducibility pair，Signal/Order/economic/Trial authority 全为 none，成熟度不升 M3。
- 已补：Replay R4.138 物化 Worker v10 Authority Response Validation v1。Validator 只消费 exact durable R4.137 Dispatch Receipt，不接受 caller payload/time；纯函数要求 exit `0`、无 signal/transport error/stderr，并对 stdout 执行 fatal UTF-8、唯一 canonical JSON LF、Authority Response Frame exact-field/schema/self-hash、Transport/artifact/Envelope/Command/Intent/Request Frame echo，以及 inner Worker Response v10 Request echo/payload/self-hash校验。成功或 typed rejection 均按 Dispatch Receipt/policy create-or-identical；测试锁定 malformed UTF-8、trailing second frame、outer echo drift、stderr、missing durable parent、retry 与落盘 tamper。成功只证明 exact activated artifact decode Request、调用 Harness 并返回 typed claim；Request decode、Response Frame/read/validation=1，但 Schedule/Harness Receipt 尚未 admission，Signal/Order/economic/Trial authority 全为 none，成熟度不升 M3。
- 已补：Replay R4.137 物化 Worker v10 Authority Request Dispatch Attempt/Receipt v1。Runner 从 exact durable Process Launch Receipt 的完整 authority lineage 唯一重建 Request Frame v2，触碰 child stdin 前以 `Process Receipt hash + Frame hash` CAS 不可重放写槽；只有首次调用返回的 exact live session 可写 canonical JSON UTF-8 LF、关闭 stdin，并按冻结 timeout/response bound 排空 stdout/stderr等待退出。Attempt 无 Receipt 视为 child 可能已消费，禁止自动 rewrite；Receipt retry 不取 clock、无需 live handle。Receipt 用 canonical base64 暂存 exact raw bytes/count/hash 与 exit outcome，但明确不是最终 Artifact 格式；exit `0`、非空 stdout 也不构成 decode/Harness/Response 证明。测试锁定 exact Transport/Command/Intent/Worker Request frame binding、一次 write/close、opaque capture、durable retry、缺 parent 与 tamper 拒绝。当前 Request Frame/write Receipt=1，decode Receipt、Response Frame/read/admission=0，Decision/Signal/Order/economic/Trial authority 全为 none，成熟度不升 M3。
- 已补：Replay R4.97 冻结 non-economic Decision Observation Input Materialization v1。Data Adapter 完整重建 R4.96 binding lineage，校验 Dataset Manifest schema 与 Request identity，并为每个 boundary 复用正式合同生成 Decision Input Snapshot v1 与 Market Input Snapshot v1；首版只认证 supplemental requirement `none` 的空快照，closed-bar lookback 只取 admitted Projection 中末 N 根 `closed_bar`，未来 bar、interval/grid/terminal-time 漂移 fail closed。position-open State Snapshot 固定 `runtime_state_required_not_materialized`，raw dataset revalidation、Worker Request、Harness invocation、Runner 与经济 authority 均未开放。测试覆盖 closed-bar 正式物化、deterministic rebuild、Manifest identity/interval drift、future leakage、self-consistent snapshot substitution 与 authority field injection，成熟度不升 M3。
- 已补：Replay R4.92 冻结 portable non-economic Decision Observation Bundle v1。Bundle 内嵌完整 R4.91 Binding Set 与逐 boundary R4.89 Projection payload，要求 `projection_count == binding_count == schedule entry count`，按 binding sequence 一对一匹配 projection id/hash、as-of time、observation count、Payload View hash 与 Cut hash；Projection id/hash 唯一，payload/ids/hashes/observation-values hashes 分别折叠。Bundle 明示 payload 可携带但 authoritative rebuild 必须重验外部 parent lineage，不能把自包含 JSON 当成来源权威。测试覆盖 deterministic rebuild、实际 observation payload、omission、reorder、funding substitution+rehash 与字段注入。Decision Input compatibility 未声明，Harness/Artifact/Runner 未绑定，Decision/Signal/Order/economic authority 均为 none，成熟度不升 M3。
- 已补：Replay R4.93 由 Research Control Plane 签发 immutable Decision Observation Bundle Admission v1。签发函数不接受 caller 自报 lineage 摘要，而是重验 reserved Trial、Reservation v9、Request v30、已登记 Cross-source Ordering Admission、Wire v2、Request 的完整 frozen Schedule 与 portable Bundle；每个 Reservation/request/ordering/wire/bundle identity 只能 create-or-identical 一份 sidecar。测试锁定重放幂等、竞争 identity、Request/Reservation data drift 与 SQLite update 禁止。唯一 capability 为 `non_economic_decision_observation_audit`；当前只认证 `wire_identity_and_schedule_binding_only`，Projection 是否由完整 Cursor→Cut→View 父链正确推导仍为 `not_certified`，Harness/Runner/Decision/Signal/Order/economic authority 均为 none，Request/Reservation/Result/Artifact/Checkpoint/Simulator 版本不变，成熟度不升 M3。
- 已补：Replay R4.94 冻结 Decision Observation Bundle Derivation Attestation v1。Replay 认证器不只检查 Bundle 内部 hash，而是对每个 boundary 重放 `Wire → non-economic Gate → Candidate Trace → Availability Cursor → Visibility Cut → PIT Payload View → Observation Projection → Schedule Binding → Bundle` 的既有 lineage validator，并强制所有 boundaries 共用同一 Wire/Gate/Trace/Cursor root；两条各自有效的 parent chain 混装也拒绝。Attestation 对 boundary 的 Cut/View/Projection/Binding、observation/future hashes 分别折叠，deterministic rebuild、mixed-root、parent substitution 与 authority field injection 已测试。对象仅为 `hash_summary_without_parent_payload_duplication`，独立复验仍要求外部 parents；R4.93 Control Plane Admission v1 仍固定 derivation `not_certified` 且未绑定该 attestation，Harness/Runner/Decision/Signal/Order/economic authority 均为 none，成熟度不升 M3。
- 已补：Replay R4.95 由 Research Control Plane 新签发 immutable Decision Observation Bundle Derivation Admission v1，而不修改 R4.93 receipt。签发读取 authoritative reserved Trial、Reservation 与既有 Bundle Admission，验证 R4.94 Attestation 的 schema/self-hash，并逐 boundary 对齐已准入 Bundle 的 Wire、Schedule、Binding、Projection、Cut/View、observation/future hashes；每个 Reservation/request/bundle admission/bundle/attestation identity 只能 create-or-identical。Control Plane 明示只做 `attestation_schema_hash_and_admitted_bundle_binding`，不反向导入 Replay Engine、不声称重放 parents，独立复验仍需外部 parent chain。测试锁定幂等、竞争 identity、self-consistent bundle substitution、字段注入和 SQLite update 禁止；Harness/Runner/Decision/Signal/Order/economic authority 均为 none，成熟度不升 M3。
- 已补：Replay R4.96 冻结 non-economic Decision Observation Harness Context Binding v1，而不修改正式 Harness Context v7/Worker v9/Receipt v11。Data Adapter 重验 Request v30、R4.95 Derivation Admission 与 admitted Bundle，将每个 frozen Schedule entry、Projection/Binding/Cut/View hash 与由 `Request + Schedule entry` 唯一派生的 Harness Context v7/hash 一对一绑定，并折叠 entry/context/projection hashes。该对象只证明 observation boundary 与 Harness identity/time 对齐，机器固定 `decision_input_materialization=not_certified`、所有 supplemental/market/state input 与 Worker Request compatibility `not_bound`、`harness_invocation=forbidden`；deterministic rebuild、Harness hash drift、self-consistent Context substitution 与 authority field injection 已测试，成熟度不升 M3。
- 已补：Replay R4.91 冻结 non-economic Decision Schedule Observation Binding Set v1。Set 内嵌并自哈希完整 R4.90 binding 序列，要求 `binding_count == frozen schedule entry count`、sequence 从 1 连续升序、每个 member 共用同一 Schedule v7/hash，且 binding id/hash、Projection hash 均不可重复；first/last decision time 与三组 member hash 独立折叠。Engine builder 必须按 Schedule 顺序接收恰好一份 binding input，成员再逐一通过完整 Wire→Cut→View→Projection→Binding lineage。测试覆盖 deterministic rebuild、双 boundary 完整覆盖，以及 omission、duplicate、reorder、cross-schedule mixing 与自洽重哈希攻击。Set 仍把 effect 视为 opaque label，Harness/Decision/Signal/Order/economic authority 均 forbidden/none，Runner、Checkpoint、Result、Artifact 未接入，成熟度不升 M3。
- 已补：Replay R4.90 冻结 non-economic Decision Schedule Observation Binding v1。Binding 只把外部冻结 `Decision Schedule v7 + hash` 的一个 selected entry 与 R4.89 Projection 引用相连，要求 schedule sequence 连续、decision time 严格递增、冻结 hash 不漂移，并强制 `projection.as_of_time == selected decision_time`；每个 boundary 因而拥有独立 causal observation prefix。selected effect 仅作为 opaque frozen label 绑定 entry hash，R4.90 不替代 Request-aware Schedule authority validator，也不解释或执行经济语义。对象 exact-field、自哈希并绑定 projection/payload-view/cut hash；测试覆盖双 boundary、确定性复读、时点错位、旧冻结 hash、effect substitution+rehash 与字段注入。Harness invocation、Decision/Signal/Order/economic authority 均 forbidden/none，Runner、Checkpoint、Result、Artifact 未接入，成熟度不升 M3。
- 已补：Replay R4.89 冻结 non-economic Decision Observation Projection v1。Projection 只消费通过完整 lineage 的 PIT Payload View，并按版本化 exact-field whitelist 输出 status、funding settlement、aggregate trade、bar-open、closed-bar 五类只读 observation。bar-open 只能包含 `open_time/open`；完整 OHLCV 只能来自 `closed=true` 且 `close_time=effective_time` 的 bar-range；status `observed_at`、funding timestamp、aggregate trade event/availability 必须与 record 双时钟一致。每条 observation 绑定 payload-record/value/payload/source-envelope hash，未来 payload access、Signal、Order、经济副作用均 forbidden。测试覆盖 bar-open 字段集、closed-bar 时点、迟到 status、空投影、range-field 注入与 funding rate substitution+rehash。Decision Input compatibility、Harness、Runner、Checkpoint、Result、Artifact 均未接入，成熟度不升 M3。
- 已补：Replay R4.88 冻结 Cut-bound PIT Payload View v1。Materializer 只接受通过完整 Wire/Gate/Trace/Cursor/Cut lineage 的输入，按 Cut visibility ordinal 一对一取回 Wire v2 typed payload；每条 record 绑定 transition id、Wire id、source/kind、effective/availability、ambiguity、payload hash 与 source-envelope hash。未来后缀只复制 transition count/id hash，`future_payload_materialization=forbidden`；空 Cut 生成 canonical 空 view。测试覆盖迟到 status 边界前后、空 view、确定性复读，以及篡改 funding payload 并重算所有 view-local hash 的攻击，后者由 Cut/Wire lineage 拒绝。View 固定 decision/economic authority none、Harness/Runner not bound，不进入 Request、Checkpoint、Result 或 Artifact，成熟度不升 M3。
- 已补：Replay R4.87 冻结 non-economic Visibility Cut v1。Cut 对任意 `as_of_time` 使用 `availability_at <= as_of_time`，复制原 Cursor 的完整 visibility prefix，并绑定全 Cursor cardinality、visible prefix hash 与 future transition count/id hash；因此未来事实不能提前进入，已可见事实也不能通过删项并重哈希隐藏。Cut 允许首事件前的空视图，记录 per-source count/last-id、delayed-visible count、latest availability 与 max effective frontier；但 payload 固定不物化，decision/economic authority 均为 none。测试覆盖迟到 status 边界前后、空前缀、确定性复读及闭世界 omission 攻击。该路径仍不接 Harness、Request、Runner、reference Engine、Checkpoint、Result 或 Artifact，成熟度不升 M3。
- 已补：Replay R4.86 冻结 availability-aware 双时钟 candidate cursor。R4.85 Candidate Trace 仍是按 `effective_time` 排列且不可改写的市场时间轴；Cursor 另以 `(availability_at, effective_time, effective-event ordinal, wire-event id)` 形成可见性时间轴。`availability_at > effective_time` 的事件固定标记 `delayed_historical_fact`，availability 前的 visibility cut 不含该事实，到达后也固定 `retroactive_execution=forbidden`、execution effect none。Cursor 自哈希绑定 Trace/Wire/Gate/Ordering lineage、两条 timeline、lag、source count/last-id；测试覆盖 500ms 迟到 status、边界前后视图、全 immediate parity、确定性复读及“重排并重哈希以提前揭示”攻击。该路径仍不接 Request、Runner、reference Engine、Checkpoint、Result 或 Artifact，成熟度不升 M3。
- 已补：Replay R4.85 冻结 Wire v2 pre-execution gate 与非经济 candidate reducer。Gate self-hash 绑定 Wire Manifest、Ordering Attestation、ambiguity/limitation hash，只放行 `non_economic_schedule_trace`；`economic_exact_trigger` 在同刻跨源碰撞时以 `rejected_resolution_limited` 拒绝，无碰撞时也以 `rejected_economic_consumer_not_certified` 拒绝，避免把 deterministic source-rank tie-break 或 declared timestamp exact 升格成 venue/execution exact。Candidate reducer 与 reference Engine 隔离，只输出逐事件 payload/key/ambiguity lineage、四源 observation count/last-id，碰撞成员固定 `deterministic_tie_break_only`，所有 execution effect 固定 none/forbidden。已补 collision/exact deterministic replay、rehash overclaim、ambiguity lineage tamper 与经济 gate 拒绝测试；Request、Reservation、Runner、Simulator、Checkpoint、Result、Artifact 与 legacy SourceEvent 均不变。
- 已补：Replay R4.84 实现 Projection-bound Wire v2 materializer 与受限 legacy parity。Data Adapter 先用调用方提供的 Instrument Status、Funding、Aggregate Trade、OHLCV typed collections 重建完整 R4.80 Ordering Attestation，要求其 canonical hash 与 R4.81 Admission、R4.82 Projection 绑定对象全等，再按 native id 一对一内联 payload 并复核 payload/source-envelope/event cardinality/ordered-envelope hash；缺失、额外、篡改或重排均 fail closed。新增 self-hashed `Legacy Shared Event Schedule Parity Attestation v1`，以真实旧 Engine `buildReplaySourceEvents` 输出认证 Instrument Status、Funding、bar-open/bar-range 的 `kind + effective_time + status snapshot id` correspondence；Aggregate Trade 固定 Wire-only，legacy payload、EventKey 与跨源同刻顺序 parity 明确 `not_asserted`。Manifest 仍保留 `legacy_semantic_parity=not_certified`、Runner not bound、economic authority none；Request、Reservation、Engine consumer、Result、Artifact、Checkpoint 与 Simulator 均未改版。
- 已补：Replay R4.83 冻结 parallel candidate SourceEvent Wire v2 contract，而未原地扩展 legacy SourceEvent。旧类型已进入 Result v43、Artifact `source-events.json`、Checkpoint source prefix 与 resume hash，原地加字段会重写既有证据身份。Wire v2 为 Instrument Status、Funding、Aggregate Trade、bar-open 与完整 closed bar 定义 canonical inline typed payload，保留 effective/availability、native identity 与 R4.80 ordering key；validator 从 wire 重建 Cross-source Envelope 并复核 payload/envelope/event-id hash。Manifest v1 绑定 Projection schema/policy/id/hash、Admission、Ordering Attestation、Reservation、Dataset、window、ambiguity/limitations 与 ordered envelope/event/payload hash，并固定 `parallel_epoch`、legacy unchanged/parity not certified、Runner not bound、economic authority none。同刻 Aggregate Trade sequence=7 与 OHLCV sequence=0 fixture 证明把 source rank 塞进旧 subphase 会因旧键先比较 sequence 而反转顺序。该阶段仅有 schema/validator；R4.84 承接 materializer 与受限 parity，Engine/Runner/Request/Result epoch 仍未开始。
- 已补：Replay R4.82 冻结 Admission-bound SourceEvent Projection Attestation v1，但不修改现有生产 `ReplaySourceEvent`。Data Adapter 同时重验实际 R4.81 Admission 与 R4.80 Ordering Attestation，逐项匹配 Trial/run、Reservation、Dataset、四源 collection、ordered events、ambiguity、resolution 与 limitation hash，再对每个 ordered envelope 一对一投影，保留 source/projected kind、native id/sequence、effective/availability、payload/source-envelope hash 与完整 cross-source ordering key。发现现有 SourceEvent 缺 `availability_at/payload_hash/source_rank` 和 `aggregate_trade` kind，且其 EventKey 先比较 source sequence，不能用 event subphase 无损承载 source rank，因此 projection 固定 `hash_only`、`production_source_event_compatibility=not_asserted`、`economic_authority=none`。同刻 resolution-limited 与错峰 declared-exact fixture、Admission drift、availability lineage tamper、deterministic self-hash 已锁定；Request v30、Runner、Engine、Result v43、Artifact v45、Checkpoint v22、Simulator v16 均不变。
- 已补：Replay R4.81 冻结 Control Plane Reservation-bound Cross-source Ordering Admission v1，而未修改 Reservation v9 或 Runner。签发读取并验证实际 R4.80 Ordering Attestation，要求 Trial 仍 reserved、Reservation 半开窗口有效、既有 Aggregate Trade Evidence Admission ref/hash 完全匹配、provider certification 在新签发时点仍有效，且 cross-source window 等于 aggregate coverage window、aggregate collection hash 等于 coverage events hash。Admission 从 Attestation 派生四源 collection、ordered events、ambiguity、limitations hash，并从 Reservation 继承 Dataset ref/hash 与 status schedule/provenance；每个 Reservation/aggregate admission 只允许 create-or-identical 一份不可变记录。scope 固定 `pre_integration_cross_source_ordering_only`，`economic_authority=none`，外部完整性仍为 `not_verified`。已补真实四源 Attestation 跨 Plane fixture、CAS/SQL immutability、hash/window/source/certification cutover/overclaim 测试。Request v30、Runner、Result v43、Artifact v45、Checkpoint v22、Simulator v16 与经济 EventKey 不变；Funding/OHLCV 分项哈希仍是 sidecar decomposition，production SourceEvent merge 尚未开始。
- 已补：Replay R4.80 冻结 pre-integration 跨源 EventKey/Ordering Attestation，而未把新键偷接到 Runner。Cross-source Envelope v1 分离 `effective_time` 与 `availability_at`，分别保留 instrument status effective/observed、funding event time、aggregate trade trade/available time 与 OHLCV open/close visibility。EventKey Policy v1 先验证各源 native order，再按 effective time、semantic phase、source rank、native sequence、stable id 形成确定性全序；source rank 只是 tie-break。不同源同刻且缺 venue-global sequence 时必须生成 ambiguity group 并标 `resolution_limited`，不能用 stable id 猜历史先后。Ordering Attestation v1 自哈希绑定 source collection/content/event hash、半开窗口、limitations，并固定 `pre_integration_ordering_evidence_only + forbidden_until_runner_contract_binds_attestation`。已补四源 collision、effective/availability 分离、确定性复读、native gap/symbol/window/tamper/overclaim 测试。Request v30、Reservation v9、Runner、Result v43、Artifact v45、Checkpoint v22、Simulator v16 与现有经济 `ReplayEventKey` 不变；venue 全局序号、bar/trade 原始 source-link、funding 外部完整性和 production merge 仍未完成。
- 已补：Replay R4.72 将“GTC Limit 到不可变数据边界仍未成交”从 R4.70 的临时 deterministic failure 收敛为权威零成交完成证据。Source Reducer 以 nullable entry/terminal transition 返回 EOD；Result v39 新增 `entry_outcome=filled|unfilled_at_data_end`。未成交分支保留 submit/activate、active Order 与完整 resting resolution chain，不伪造 cancel/expire；`fills/positions/margin_snapshots/liquidation` 为空，Ledger 只有 initial/ending cash，Equity v2 用 `never_opened + position_event_id=null` 表达零仓位，Journal v5 不预留 collateral，Metrics 为零交易/零 PnL。Simulator v12、Artifact v41 已同步；Runner 首次发布与幂等复读交叉验证 outcome/Fill/Position，clean/resume Result hash 完全一致。持单 delisting 仍 typed-fail，IOC/Cancel/expire OrderEvent 与 authority 未开放。已补 engine clean/resume、防篡改、空账本/Equity/Journal/Metrics、Artifact commit 及 149 项 Replay 定向回归。

- 已补：Replay R4.71 消除 R4.70 `full_fill_capacity` 的裸 Request 自报。新增 self-hashed Liquidity Capacity Attestation v1，冻结 symbol/base-asset unit、静态 full-fill ceiling、校准窗、observed/available time、source ref/hash、opaque derivation policy hash，并永久声明其不是 event-depth/queue proof。Dataset Manifest v11 携带 attestation；Request v26 Limit lane、Trial Reservation v9、Result v38 Fingerprint 与 Artifact v40 分别绑定同一 hash，Market lane Reservation/Fingerprint 固定 `null`。Adapter 在 Engine 前拒绝 attestation 缺失、`available_at > signal_time`、symbol/capacity/hash 漂移；独立 `liquidity-capacity-attestation.json` 支持幂等复读，Checkpoint v18 经 Request/Manifest hash 间接冻结且 clean/resume parity 不变。Simulator v11 撮合与账本算法不变，所有 OHLCV Limit Fill 继续标 `ohlcv-limit-queue-unobserved / resolution_limited`。已补 self-hash tamper、PIT future leakage、Reservation binding、Fingerprint/Artifact 与端到端 Limit 认证。production 数据源、校准算法、外部 completeness/authenticity、真实 depth/queue/partial 仍未完成。

- 已补：Replay R4.70 将 R4.69 的 pre-entry Limit GTC 子集接入权威执行链，而未把整个 primitive 升格为通用 matching。Request v25 以 tagged `entry_execution` 冻结 market 或 `limit/gtc/limit_price/liquidity_model/full_fill_capacity`；Limit signal 时 submit+active，next-open 起逐 `bar_open|bar_range` 生成 self-hashed resolution。open marketable 或 strict-cross 仅在 capacity 内全成，实际 Fill 经 adverse slippage 后仍以 limit 截断；exact touch 保持 resting。Fill EventKey 严格晚于 decisive SourceEvent，stop/target 再于 phase-90 激活，禁止用已消费同一 OHLC boundary 触发新保护。Checkpoint v18 保存 active entry、resolution prefix、nullable transition；pending cancel/resume 与 clean run Result hash 一致。Result v37/Fingerprint 绑定 `pending_order_resolutions_hash`，Artifact v39 必交 `pending-order-resolutions.json`；EOD 未成交与持单期间 delisting 分别返回 non-retryable `limit-entry-unfilled-at-end-of-data` / `instrument-delisted-with-pending-entry`，均无 Result/Artifact。golden/contract、跨 bar resting→strict-cross、limit price bound、OrderEvent→Fill→Position→Ledger、checkpoint parity、artifact idempotent replay 与 unfilled typed failure 已锁定。Simulator v11 对所有成功 Limit Fill 明示 `ohlcv-limit-queue-unobserved / resolution_limited`；capacity 仍只是冻结模型上限。IOC、Stop pending、Cancel OrderEvent、真实 queue/depth partial、多订单 allocation 未完成。
- 已补：Replay R4.69 冻结首个通用 pending-order pre-integration primitive，而未把 Limit/TIF 偷渡进当时的 certified Runner。新增 self-hashed `Pending Order Resolution v1` 与 Engine resolver：只消费一个已激活 `limit` GTC/IOC 或 `stop_market` GTC、一个 `bar_open|bar_range` OHLC observation 和可选 Cancel EventKey；唯一 liquidity model 为 `ohlcv-cross-through-full-fill-bounded-v1`，quantity 必须不超过冻结 capacity。Limit open-marketable 取不劣于 limit 的 observed-open improvement，strict-cross 取 limit reference，但两者均因 queue 不可见标 `resolution_limited`；touch 零 fill且 limited，未触达才 `exact_under_ohlc`。Stop gap 取 observed open，range trigger 取 trigger reference。Cancel 严格早于 observation 胜、晚于 modelled fill 败、晚于确定 non-fill 胜；同 ordinal 和 touch-before-cancel 都是 `unresolved`。R4.69 本身不创建经济事实；其可执行范围以后续 R4.70 为准。
- 已补：Replay R4.68 将 startup recovery 接到唯一可执行的 Control Plane claim-side admission gate。新增 `research.replay-attempt-admission`：同一 existing authority DB session 内，先按 R4.67 全量 discovery/inspection/ack，再调用既有 `claimReplayAttempt`；强制 `recovered_at <= claimed_at`，并重验返回 Lease 的 Attempt/worker、Trial/run、Reservation/hash、Request、claim/expiry binding。malformed outbox、authority drift 或时间倒置均 zero-claim，输出不含 DB/artifact path；standalone recovery 仍不 claim，Replay Runner 仍只消费 Lease、不读 DB。仓库不存在 worker supervisor，因此本阶段没有伪称进程部署闭包：调用方仍须把 artifact root 绑定到同一已停止 worker pool，且 valid recovery 在后续 claim 校验失败时保持已提交；这是一条 ordered fail-closed gate，不是 filesystem/SQLite 分布式事务。J04、Result/Artifact/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.67 将 R4.66 从 library seam 接到真实 Control Plane-owned startup job。新增 `research-control-plane/replay-recovery` 与 agent-facing `research.replay-cancellation-recovery`：要求显式 existing CP DB、existing local Artifact Store root 与 registration time，校验 authority schema 后组合 Runner discovery 与 SQLite pending/already inspection；不初始化错误 DB、不 claim Attempt、不运行 Engine。Discovery Recovery Result 推进至 v2，以 portable namespace identity/record/Observation hash 替代本机绝对 namespace/outbox ref；job response 同样禁止泄漏 DB/artifact path。空 store、malformed outbox fail-before-registry-mutation、missing/wrong DB、CLI path guard 已认证。该 job 必须由部署层置于 fresh worker admission 前，但本阶段不擅自接入 J04 research loop、不固定 daemon/cron、remote transport、GC/quarantine 或 SLA。Result/Artifact/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.66 闭合 local cancellation outbox 的启动发现与 exact cancellation-generation lease rehydration。Outbox Record 升至 v2，新增完整 current lease snapshot 与 logical idempotency-key hash；Local Artifact Store 只读枚举 canonical 两级 Attempt tree，先全量验证 record canonical bytes/self-hash、lease/outcome/Observation hash、derived namespace 与 symlink fence，再发生任何 Control Plane 调用。Control Plane recovery inspection 只裁决 exact Observation 为 `pending` 或 `already_registered`：前者 no-poll/no-Engine ack，后者不重复投递。legacy v1 不猜测迁移，仍只能走显式 bound helper；misplaced namespace、linked tree、authority hash drift fail-before-delivery。Result/Artifact/Fingerprint/Simulator/经济语义不变；remote listing、retention/GC、quarantine、跨存储事务与 startup/stop SLA 未解决。
- 已补：Replay R4.65 修复 local cancellation outbox 将 constructor lease generation 隐式固定为最终 generation 的缺口。Outbox namespace 继续按 Attempt identity 打开，但 constructor lease 只冻结 Trial/run/request/reservation、Attempt id/ordinal/worker 与 generation floor；每次 persist 新增 exact current lease snapshot，并要求 prepared outcome/Observation/commit 的 generation 与 lease hash 全等。真实 coordinator 测试锁定 boundary 1 generation `2→3` renewal、boundary 2 才收到 cancellation，仍在原 namespace pre-terminal 提交 generation 3 record 并 ack；重启用 generation 3 recovery-first 不 poll、不重跑，generation 2 stale invocation fail-before-poll/ack。rollback、worker/Attempt 漂移和用旧 lease 持久化新 outcome 均拒绝。Outbox schema/Result/Fingerprint/Simulator/经济语义不变；namespace discovery 与 exact invocation rehydration 仍未解决。
- 已补：Replay R4.64 将 durable cancellation recovery 提升为 coordinator admission rule。每次 durable invocation 在 Runner validation、authority poll 与 Engine 前先 load bound outbox；存在 pending record 时只 ack immutable Observation，不进入 fresh run。新增 invocation fence 交叉核对 Request hash/run、Trial Reservation、Attempt id/ordinal/worker、完整 lease snapshot hash/generation、commit producer/record/Observation hash；cross-run/cross-attempt 漂移与 tamper 均 fail-before-poll。测试用已过期 invocation time、`cancel_requested=true`、空 bars 证明 restart-first 路径不执行 Runner，并锁定 cross-binding/tamper 时 poll/ack 均为零；显式 recovery helper 仍兼容。该阶段不解决 Attempt namespace 自动发现或 Request/Reservation/lease 重建，调用方仍须 rehydrate 完全相同的冻结 invocation；不固定 recovery index owner、retention/GC、remote transport 或 SLA。Replay wire/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.63 将 cancellation outbox 从 Run Outcome 返回后的投递准备前移到 Engine source-boundary 的 pre-terminal handoff。Runner 抽取唯一 canonical authority-cancellation outcome constructor；durable coordinator 在 directive/lease 校验通过后用同一构造结果 immutable-CAS outbox，并要求其 bytes 与 Runner 随后返回的 terminal outcome 完全一致，再执行 ack。测试以 persistence 时 diagnostic checkpoint 尚存在、终态后仅余 outbox 锁定 `poll → persist → Engine cancel/cleanup → ack`；无效/过期 directive 不写 outbox，持久化失败仍执行 authority cancel、返回 exact cancelled outcome 并禁止 ack，restart recovery 继续不 poll、不重跑 Engine。该顺序覆盖 outbox commit 后、Runner 返回前的进程崩溃恢复，但不虚构 Control Plane SQLite 与 local filesystem 的分布式事务；poll 返回至 local commit 仍有不可原子化的极短窗口。Replay wire/Fingerprint/Simulator/经济语义不变，retention/GC、remote store/transport、SLA/watchdog 仍未固定。
- 已补：Replay R4.62 落地 Runner-owned local durable cancellation outbox。Durable coordinator 在 Observation 产生后、Control Plane ack 前，先通过 certified Attempt-local Artifact Store 提交固定名、自哈希 Outbox Record v1；记录绑定 request hash、run、Attempt/lease generation、poll count 与完整 authority-cancelled Run Outcome。落盘失败抛 typed error 且不 ack；ack 失败携带 Outbox Commit。重启恢复只 load/重验 record 后重投 Observation，不 poll、不重跑 Engine。相同 persist 保留首次 `persisted_at`，竞争内容、byte/hash tamper 拒绝；成功 ack 后记录仍保留，使 record 已提交后的崩溃窗口收敛为 Control Plane create-or-identical redelivery。Durable Coordination Result v1/Outbox Commit 是 operational evidence，不是 Result/Artifact Manifest、Checkpoint Receipt 或 Resume authority。已补 persistence-before-ack、restart recovery、no-poll/no-replay、tamper、first-write-wins 与 unsafe-ack suppression 测试。Observation 生成至 outbox commit 之间仍有不可消除的进程指令窗口；未固定 retention/GC、remote store/transport、stop SLA 或 watchdog。
- 已补：Replay R4.61 将 cancellation acknowledgement 的“可重试”从说明提升为可执行合同。Runner 新增 `acknowledgeReplayCancellationOutcome`：只接受带 self-hashed Observation v1 的 authority-cancelled Run Outcome，复用原 poll count，以新 registration attempt 调注入 port；它不 poll、不重跑 Engine，也不生成第二份 Observation。typed error 进一步保存 attempted `registered_at` 与原始 cause，失败不能伪装为 registered。SQLite adapter 对相同 Observation 的重复 ack 保持 create-or-identical，并保留首次成功 `registered_at`/latency；竞争内容继续拒绝。该闭包只认证进程内恢复，不声称 durable outbox、进程崩溃恢复、跨进程 delivery 或 SLA。Replay Result/Artifact/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.60 落地 transport-neutral cancellation coordinator reference path。Runner 新增 injected `poll/acknowledge` port 与 Coordination Result v1：每个完整 source boundary 先 poll 当前 lease，无取消才委托既有 renewal callback；receipt-bound cancelled outcome 返回后登记 Observation。ack registry 失败抛出携带完整 cancelled outcome/poll count 的 typed error，允许重试 immutable observation，不伪报成功。Control Plane 新增结构兼容 SQLite adapter 与只读三段 latency projection；数值只测 `recorded→observed→registered`，不是 SLA。authority cancel 不再发布 resumable checkpoint/diagnostic commit，并删除 attempt-local diagnostic 文件；普通 cooperative cancel/resume 不变。已补 injected-port poll/ack、typed ack failure、lease-window miss、latency projection、no-resume/no-orphan diagnostic tests。未固定 push/poll/lease-response/IPC/网络，未实现强制抢占、watchdog 或通用空目录 GC。Replay Result/Artifact/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.59 冻结 Attempt Cancellation observation/acknowledgement seam。Control Plane 新增 read-only SQLite reference directive，只在 caller lease 与 stored cancellation 的 Trial/run/request/reservation、Attempt/worker/ordinal/generation 全等且仍未过期时返回 `cancel + original receipt`，不续租。Runner 在完整 source boundary 校验 receipt self-hash、时间与 active lease hash，以 Run Outcome v35 返回 self-hashed Observation v1；Control Plane create-or-identical append-only registry 再核对 terminal Attempt并保存首次 `registered_at`，拒绝 `recorded_at > observed_at`、`observed_at > registered_at`、竞争 ack、binding/generation 漂移及 update/delete。已补 contract hash/type、directive lease window、Runner receipt binding/stale generation、Outcome no-partial、registry idempotency/immutability 测试。Observation 只证明 cooperative worker 已看到取消并返回，不证明跨进程发送时刻、强制抢占或 stop-latency SLA；取消后 checkpoint 无 Receipt/Resume authority，R4.60 已进一步停止发布并删除其 diagnostic 文件。Result/Artifact/Fingerprint/Simulator/经济语义不变。
- 已补：Replay R4.58 冻结 Control Plane emergency cancellation authority。Reservation Cancellation v1 绑定完整 Reservation hash 与 Trial/run，以 `recorded_at <= effective_at < expires_at` 的 append-only 收据永久阻断生效后的新 claim；生效前已 active Attempt 与其幂等 delivery 不被暗中撤销。Attempt Cancellation v1 独立绑定 Trial/run/request/reservation、Attempt/worker/ordinal 与精确 lease generation，收据和 Attempt terminal `cancelled` 原子提交；stale generation、terminal/竞争取消拒绝，旧 lease 后续 renew/finalize/new checkpoint authority 失效。停止当前运行且禁止 retry 必须显式写两份收据。Replay wire/Fingerprint/Simulator/经济语义未变，Runner 复用 cooperative cancellation port 且不发布 partial Result/Artifact。已补 hash/type/time、CAS/immutability、scheduled boundary、active idempotency、stale generation、terminal fence 与竞争测试。R4.59 已承接 reference observation/ack seam；跨进程 transport/SLA、强制进程抢占和 orphan artifact GC 仍未补。
- 已补：Replay R4.57 冻结 Control Plane provider certification 的非追溯 rotation/revocation。Certification v1 保持不可变；新增 self-hashed Termination v1 与不可变 create-or-identical registry，每份认证至多一个 `revoked`/`superseded` 终止，`recorded_at <= effective_at` 禁止 backdate。Supersession 要求 successor 已注册、同一 provider 且在 cutover 可准入；有效准入窗缩为 `[certified_at, min(valid_until, effective_at))`。只阻断 cutover 起签发的新 Reservation；此前 Reservation、后续 claim/Attempt/Result/Artifact 不被追溯改写。Replay wire/Fingerprint/Simulator/Checkpoint/经济语义未变，且无 latest-wins 查询。已补合同 hash/timestamp/type、registry CAS/immutability/单终态、rotation/revocation、cutover 前后 admission 与旧 Reservation schema-valid 测试。既有 authority 的显式停止已由 R4.58 承接；外部签名 capability receipt 仍未补。
- 已补：Replay R4.56 冻结 instrument-status acquisition receipt/capability fence。Market Data Store 新增 Acquisition Receipt/Attempt v1 与 exact response BLOB：request identity、transport/capability、HTTP status、failure class/retryable、payload ref/hash/bytes/count、attempt chronology/hash 和 terminal outcome 以 create-or-identical CAS 持久化，失败/429/invalid response 不丢失。Source Batch v2 / Archive v3 强制绑定 receipt id/hash，并在 commit 时核对 stored payload、venue/symbol/window/watermark/raw hash/count；只有成功的 `historical_event_archive + offline_import` 可进入 batch，current snapshot 被双重拒绝。新增 Binance USDⓈ-M read-only `exchangeInfo` collector，支持 bounded retry 与 acquisition-id 幂等，但只出具 `current_snapshot_only`、`external_authenticity=not_verified`。Provider v4 / Evidence v4 因 Archive v3 输入升级而需重新认证；Replay wire/Simulator/Checkpoint/经济语义未变。已补 429→success、invalid-body non-retry、exact payload readback/tamper、receipt CAS、current→history 禁止、archive admission 和 provider cross-plane 测试。未补历史 websocket/官方 archive 回填、TLS/venue 签名留存、断线 completeness、外部历史穷尽证明。
- 已补：Replay R4.55 建立 imported instrument-status archive 的仓库内来源闭包。Market Data Archive v2 新增 Source Batch Manifest v1、raw content hash/count、批次序号/coverage/previous-hash chain 与 Completeness Audit v1；审计强制 `batch_window_continuity` 和 `external_completeness=not_verified`，只证明导入批次无 gap/overlap、hash/record/window 闭合。修订以同 scope、单后继 append-only supersession 追加，旧 archive/hash/result 不变，未偷设“latest wins”。Provider v3 / Evidence v3 输出 archive/audit/batch-chain/supersession hash，能力/build 改版后必须重新由 Control Plane 认证；Replay wire、Simulator、Checkpoint 和经济语义未变。已补 batch gap/hash-link/overclaim、CAS/readback、correction/orphan/branch、provider deterministic/tamper/cross-plane 测试。未补 venue 签名、collector 原始响应留存、源系统穷尽证明、修订选择政策。
- 已补：Replay R4.54 冻结 Control Plane provider certification admission。新增 self-hashed `Instrument Status Provider Certification v1` 与不可变 create-or-identical 注册表，绑定 certifier/policy、半开有效期、provider capability/build、normalization、允许的 source/completeness；Trial Reservation v8 只能按注册 hash 读取并在 `certified_at <= issued_at < valid_until` 发放，调用方不能自签。Market Data provider 要求认证 ref/hash 且 capability 必须等于自身固定值；Instrument Status Provenance v2、Dataset Manifest v10、Request v24、Reservation v8、Result v36 Fingerprint、Artifact v38 与 Run Outcome v34 贯通同一收据。Runner 在 Engine 前交叉验证 Dataset ref/hash/capability、Request 与 Reservation 内嵌认证。已补 registry CAS/immutability/unknown admission、provider binding、Contract/Adapter/Runner/Fingerprint/golden/cross-plane fixture。Simulator v10、Checkpoint v17 与经济语义不变；未补 venue 签名/外部 archive 穷尽证明、认证撤销/轮换、collector 或 halt/delisting settlement。
- 已补：Replay R4.53 落地 Market Data immutable instrument-status archive/provider 接缝，未改变 Replay wire 或 Simulator。`market-data-store` 新增 Archive v1 元数据/事件表：event sequence、effective/observed time、coverage/finality、source/content/archive hash 经 `BEGIN IMMEDIATE` create-or-identical CAS 原子提交，同 id 内容漂移拒绝。`market-data.instrument-status-provider` 只读 archive，要求 coverage anchor、严格递增且交替的 `trading/halted` transition、finality watermark 覆盖 `coverage_end` 与 requested window containment，再以固定 build manifest / Normalization Policy v1 生成连续半开 epochs、Provenance v1、Provider Capability/Evidence v1。已补 archive 幂等/冲突/顺序/终局、provider deterministic/tamper/window、CLI read-only 与输出通过 Replay Request v23 / Dataset Manifest v9 的跨域认证。未补 venue 自动采集/签名/外部穷尽审计、Control Plane provider registry admission、halt/delisting settlement。
- 已补：Replay R4.52 冻结 instrument-status producer authority 与 completeness attestation。`Instrument Status Provenance v1` 由 `market-data-products` producer domain 出具，绑定 producer/source owner/source kind、normalization、coverage、source observation/production time、raw record count/ref/hash 与 derived schedule hash；Dataset Manifest v9 内嵌，Request v23、Trial Reservation v7 与 Result v35 Fingerprint 独立绑定 provenance hash。只有覆盖 Replay window 的 `venue_status_event_archive` 可声明 `complete_history`；current/periodic snapshot 即使全部观测为 trading 也只能是 `current_snapshot_only`。Artifact v37、Run Outcome v33 同步推进；Checkpoint v17 与 Simulator v10 不变。已补 Contract/Adapter/Reservation/Fingerprint/golden/cross-plane fixture 认证。未补 production archive/provider、外部 completeness certification、status collector、halt/delisting settlement，也未从缺 bar 推断 status。
- 已补：Replay R4.51 将 instrument trading status 提升为独立 PIT authority。`Instrument Status Snapshot v1` 以连续半开 `trading/halted` epochs 写入 Dataset Manifest v8，Request v22、Trial Reservation v6 与 Result v34 Fingerprint 分别绑定 schedule hash；`current_snapshot_only` 不得伪证历史 halt。所有 decision/frozen executable boundary 与 bar 必须处于 trading。缺 bar 只有在 complete schedule 证明 `[previous.close,next.open)` 全程 halted 且下一 open 已 resumed 时才可跨越；普通缺口仍按 R4.50 typed-fail。halt/resume 进入 phase-`00` consumed source prefix，delisting 同刻仍先行；停牌期间 protection 保持 active、禁止 market/strategy Fill，但 exact Funding/Mark 继续入账和观察风险，恢复首个真实 open 按 observed gap 执行。停牌中 maintenance breach 返回 `maintenance-margin-breach-while-halted` 与 `not_simulated` observation，不造 liquidation Fill、Result 或 Artifact。Checkpoint v17 锁定 cancel/resume parity；Artifact v36、Run Outcome v32、Simulator v10 同步推进。未补自动历史状态采集、halt settlement、delisting settlement 或 venue 真实强平重建。
- 已补：Replay R4.50 将“连续 bar 之间观测到的价格跳空”与“执行窗口内整根时间网格缺失”拆成不同语义。Simulator v9 仍对前者执行 `worse_open`；后者绝不合成 bar、forward-fill 或跨越未知区间继续执行。冻结的 earliest executable bar 缺失时，Data Adapter 在任何 Fill 前返回 `dataset-grid-gap-in-execution-window`；持仓到达缺口时，Engine 在前一根已观测 bar close 后、任何后续 Funding/Mark/open 与 checkpoint 前返回同一 typed `data_integrity` failure。`ReplayDataGapFailureEvidence` 自校验 gap kind、精确起止、interval 与 missing count；Run Outcome v31 禁止携带 partial Result/Artifact。terminal 若已在缺口前发生，未来缺口不进入 consumed prefix，Result 不变；cancel/resume 不能绕过 continuity fence。严格 closed-bar lookback 的 gap 或 stale terminal bar 仍在 Harness 前拒绝。该协议不修复行情、不推断停牌，也不把数据缺失误标为 OHLC `resolution_limited`。
- 已补：Replay R4.49 将 R4.48 的集成 economic oracle 扩展为 Python `Decimal` 跨语言认证，未改变 production schema/version。Certification-only `OHLCV Economic Oracle Request/Response v1` 以 canonical decimal string 经 stdin/stdout 传递向量；Python 独立执行 buy-ceil/sell-floor 滑点价、signed gross floor、fee ceil 与 net，不导入 TypeScript decimal/accounting。Bun 经仓库 Python resolver 提交 48 条 path，Python Decimal、test-local TS BigInt 与 production Evidence 的四个经济字段逐字符串一致；Python unittest 锁定 long/short 手算 golden、协议保序/唯一 id，Bun 另锁非规范 decimal 的 typed `input_invalid`。这排除 fixture 范围内的同语言自证，不把 Python 变成 Replay backend，也不证明完整 Result、平台可移植性或真实 venue cost fidelity。
- 已补：Replay R4.48 用独立实现认证 R4.47 economic envelope，未推进任何 production schema/version。新增 certification-only `OHLCV Economic Oracle Fixture v1`，冻结 zero-cost fine-grid、cost-aware fine-grid、fractional-bps coarse-grid 三套 quantity/cost/increment profile；test-local BigInt rational oracle 独立计算 buy-ceil/sell-floor 滑点成交价、signed gross PnL floor、fee ceil、net contribution 与 envelope 聚合，不导入 Replay accounting/decimal。8 条 ordered-price trace × 3 profile × 2 path 全部 parity，ordered actual path containment、canonical non-improvement、long/short collision 手算 golden 和 densification metamorphic 已锁定。该结果只排除当前 TypeScript production 算法的同实现自证，不证明真实 venue 成本、tick path、完整 equity interval 或跨语言 parity。
- 已补：Replay R4.47 将 `resolution_limited` 从布尔标签升级为自哈希 Economic Impact Envelope。OHLCV Resolution Evidence v3 的每条 P1/P2 path 以同一实际 entry basis、active remaining quantity、冻结 fee/slippage、price/settlement increment 和 Numeric v3 计算 simulated execution price、gross realized PnL、exit fee 与 net terminal contribution；envelope 给出 min/max、span、canonical contribution 与 canonical-to-best shortfall。Result v33 Metrics 聚合 limited count/span/shortfall，Artifact v35/Run Outcome v30 发布；Runner 用 Request/Dataset/entry Fill 重算并把 canonical economics 与实际 terminal Fill price/fee 交叉绑定。open gap/single touch 的 span 为零，collision 的 long/short 成本方向、价格缩放 metamorphic、policy tamper 和 Fill tamper 已锁定。该指标明确排除共同 entry fee、funding、已实现 partial PnL，不是完整 counterfactual equity、真实路径概率或晋级评分。
- 已补：Replay R4.46 将 OHLCV terminal evidence 从“价格 bracket”升级为 `Active Protection Evidence v2`：每条证据绑定 `protection_generation`、post-mutation remaining quantity、stop/target Order id、trigger 与独立 protection hash。Engine 初始双保护为 generation 1；唯一 stop replacement 或 partial-fill 后原子 resize 推进到 generation 2，并写入 Checkpoint v16。Result v32/Artifact v34/Run Outcome v29 的 runner 在首次发布与幂等复读时交叉验证 SourceEvent、submitted/activated/triggered OrderEvent、terminal Fill、数量及 generation/order-id 组合；即使重算 protection/evidence/checkpoint hash，也不能让 terminal 回指旧 stop/target。未改变 Simulator v8 的成交、路径或账本语义，也未把 generation 2 外推为 multiple amendment 制度。
- 已补：Replay R4.45 以 certification-only `OHLCV Oracle Fixture v1` 校验 R4.44 simple-bracket two-path evidence，不增加 runtime capability 或 wire epoch。8 条 synthetic ordered-price fixture 覆盖 long/short、open gap、single touch 与 high-first/low-first collision；已知轨迹的首个 terminal crossing 必须落入 P1/P2 对应 outcome，canonical stop 的 raw terminal PnL 不得优于 oracle。相同 OHLC 的 high-first/low-first 轨迹可产生相反 terminal owner，但 Replay evidence 保持相同并标记 `resolution_limited`；在每段内插入中间观察不改变 OHLC、oracle 结果或 semantic envelope。该认证证明当前包络对 simple bracket 的 soundness，不代表 tick replay、真实成交重建、完整 intrabar path 枚举或 generic matching。
- 已补：Replay R4.44 将 simple-bracket OHLCV 路径不确定性从非结构化 limitation 升为自哈希 `OHLCV Resolution Evidence v1`。每次 stop/target 终止均绑定 source EventKey、bar OHLC、active bracket、P1=`open-high-low-close`、P2=`open-low-high-close`、两条 path digest、canonical path 与选择政策；open gap 和单触点标记 `exact_under_ohlc`，stop/target collision 标记 `resolution_limited / stop_target_order_ambiguous`，并保留 stop-first 作为当前 simple-bracket 的较差 canonical 结果。Result v31、Artifact v33、Run Outcome v28 与 Fingerprint 发布并重验独立 `ohlcv-resolution-evidence.json`；篡改路径后即使重算 evidence hash 仍因 path digest/语义约束被拒绝。未改变 Simulator v8 的经济执行语义，也未扩展为通用多订单 OHLC resolver、limit queue 或真实 intrabar reconstruction。
- 已补：Replay R4.43 收口 partial Fill 后的 terminal owner 组合与 checkpoint 语义防篡改。重建后的 stop、target、exact liquidation、EOD 均只消费 `abs(post-fill position)`；stop/target/liquidation 全平，EOD 不造 Fill、保留 open Position 并取消当前双保护。long/short lane 锁定相同 transition 结构，Runner 贯通 post-partial stop/target、exact-risk full liquidation、open-marked EOD 与已有 final strategy exit；同刻 pending final decision 由更早 terminal owner 写 `not_reached_terminal`。Engine Checkpoint v15 除自哈希外，还校验 event sequence、每个 Order 与最后 OrderEvent、partial Intent/Fill、重建 protection 的确定 order id/type/side/quantity/trigger；即使攻击者重算 checkpoint hash，篡改 trigger 仍拒绝。未增加 wire capability，不扩展 multiple partial、partial liquidation 或真实 liquidity matching。
- 已补：Replay R4.42 将 R4.41 draft 收敛为首条 executable partial-reduce 纵切。Request v21 / Schedule v5 认证一次 opposite-side market reduce-only fixed quantity，必须 increment-aligned、严格留下 open Position，且与 stop replacement 互斥；可选后续 final full exit。Simulator v8 在 eligible open 维持 exact risk/stop/target 优先，partial Fill 后于同一 source boundary 依次取消旧 stop/target，再以 `abs(post-fill position)` 和原 trigger 重建双保护。Result v30 的 Position/Ledger 记录 partial fee/realized PnL，后续 Funding、Margin、State Snapshot v3 使用剩余数量；terminal preemption 会取消 pending partial。Checkpoint v14 保存 partial Order/Fill 与 current bracket，clean/resume Result hash 一致。Artifact v32、Run Outcome v27、Timeline v8、Boundary v7、Context v6、Worker v7、Registry v7、Capability/Receipt v9 完成 epoch 推进。认证只覆盖模型内 fixed quantity 全成，不代表真实流动性 partial fill、multiple partial 或通用 matching。
- 已补：Replay R4.41 先冻结部分减仓的非可执行 draft seam，不提前声称 certified。`Partial Reduce Intent Draft v1` 仅允许一次 opposite-side market reduce-only `fixed_quantity`，数量必须小于初始仓位并留下 open Position；决策必须晚于入场可执行时点，成交也必须严格晚于决策。Protection Draft Policy 冻结在 partial Fill 后的同一 source boundary 取消旧 stop/target，再以 `abs(post-fill position)` 为唯一数量 authority、保留当前 trigger 价重建全量双保护；v1 draft 禁止与 stop replacement 组合，但允许后续一次 final full exit。该 draft 常量未加入 `REPLAY_CERTIFIED_CAPABILITIES`，也未加入 Request/Decision Schedule；Reservation 若要求 `partial-reduce-v1-draft`，Runner 在 Engine 前返回 unsupported 且不发布 Result/Artifact。
- 已补：Replay R4.40 认证 protective-stop replacement 的 long/short 方向对称与 OHLCV 歧义语义。镜像 fixture 锁定 short stop 只能向下收紧；replacement 后同根 bar 同时触及当前 stop/target 时，long/short 都以 stop-first 作为保守 canonical path，并必须输出 `ohlcv-stop-target-collision / resolution_limited`，不得声称 bar 内真实路径。以固定中心价镜像后，entry/stop Fill 价、net/realized PnL、return、trade count 与 OrderEvent phase/subphase 全部 parity。本阶段不增加 schema/version，也不把 stop-first 临时包络升级为一般 OHLC 路径制度。
- 已补：Replay R4.39 不扩大 wire capability，补齐止损收紧后的唯一终止 owner 组合认证。`replace -> full strategy exit` 路径锁定 Harness 下一决策必须看到新 active stop，exit fill 后只取消 replacement stop 与 target，旧 stop 保持已取消；Checkpoint 同时保存 replacement stop 与 submitted exit，clean/resume Result hash 一致且不重跑已消费决策。`replace -> exact liquidation` 路径锁定 phase-15 按 current stop cancel、target cancel、forced submit/activate/fill 顺序取得唯一 terminal Fill。新 stop 自身的后续 gap fill 已由 R4.38 覆盖；本阶段没有新建第二套 terminal arbitration 或升级为通用 OCO engine。
- 已补：Replay R4.38 认证一次全仓保护性止损仅收紧替换。Schedule v4 最多冻结一个 `authorized_protective_stop_replace`；Intent v1 绑定旧/新 trigger、opposite-side stop-market、reduce-only、`full_open_position` 与 `tighten_only_cancel_then_submit`。Engine 在非终止 closed-bar 边界以固定 EventKey 顺序取消旧 stop，再提交/激活新 stop；新 stop 若已被当前 close 穿越则拒绝，不回溯制造 same-bar fill。State Snapshot v2、Checkpoint v13 保存当前 stop/target 权威状态，测试锁定 cancel→submit→activate、后续 gap fill、terminal-before-decision、checkpoint tamper 拒绝与 resume parity。未开放止损放宽、target 改动、通用 amend/cancel-replace、连续 trailing 或部分保护量。
- 已补：Replay R4.37 认证第一条 effect-bearing position-open 决策纵切。Schedule v3 最多冻结一个末位 `authorized_reduce_only_exit`；Exit Intent v1 仅允许 opposite-side / market / reduce-only / full-open-position，并绑定 closed-bar signal 与严格更晚 earliest open。Harness 必须从 Market/State Snapshot 复算同一 Intent；Engine 在决策边界提交 strategy-exit Order，Checkpoint v12 保存 pending Order，open 上按 `exact risk -> stop gap -> target gap -> strategy exit` 执行。测试覆盖正常全平、terminal not-reached、stop-gap 抢占取消、Intent/State binding 与 cancel/resume parity；未开放 partial reduce、add/reversal、cancel/replace 或多 strategy order。
- 已补：Replay R4.36 认证第一条 position-open 只读决策纵切。Schedule v2 可在唯一入场授权后冻结 `no_action`；Engine 只在非终止 closed-bar source boundary 生成自哈希 Position/Cash State Snapshot并调用双 fresh subprocess Harness。stop/target/liquidation 同刻优先时写 `not_reached_terminal`，不事后补算；Checkpoint v11 内嵌已消费 Timeline，resume 不重跑已提交 post-entry Harness。当前仍不支持 effect-changing 持仓决策、动态 supplemental join、cancel/replace、加减仓或第二笔订单。
- 已补：Replay R4.35 移除“Timeline 最后一项就是入场授权”的位置假设。Adapter、Harness Context、Engine、Artifact writer/reader 与 Fingerprint/Checkpoint 派生字段现在都按唯一 `authorized_initial_order / authorized_order` 语义定位经济入口；完整 Timeline hash 仍覆盖全部 boundary。当前 schedule 仍拒绝持仓后 decision，直到运行时 Position/Cash State Snapshot、terminal-before-decision 与 resume prefix 合同完成，不能用事后重放补证。
- 已补：Replay R4.34 新增 Control Plane-frozen `Decision Schedule v1`。pre-entry 多 boundary 只允许 `no_action* -> authorized_initial_order`；每个 boundary 独立重建 hash-bound closed-bar PIT Snapshot、执行两次 attested Harness，并把 tagged output、Context、Boundary、Entry 与 Timeline 全链自哈希。Replay 不得增加决策时点，Engine 会对全部 entry 重算数据输入。当前不支持持仓后 decision、动态 supplemental join、cancel/replace、加减仓或第二笔订单。
- 已补：Replay R4.33 冻结 `Decision Market Input Requirement v1`，从 Dataset Manifest/Data Hash 已覆盖的 OHLCV 生成严格 closed-bar lookback Snapshot；Harness Worker 改用不含预填 Order 的白名单 Context，并以双 fresh subprocess 重算 Order。lookback 不足、gap、future-visible、Order 泄漏、输出漂移及 requirement/snapshot/artifact/checkpoint hash 漂移均拒绝。当前只认证单次 initial decision，不能外推为滚动信号或通用策略 runtime。
- 已补：campaign validation 必须与 discovery 保持 locked holdout embargo；默认按 `max(max_hold_bars, factor lookback, funding interval)` 换算。
- 已补：campaign 每个 hypothesis 必须带 `thesis_certificate`；缺 edge 类型、行为假设、参与者、regime、失效条件、成本敏感度、候选 universe 或 negative controls 时零 trial 停止。
- 已补：replay provenance 输出 `temporal_contract`，覆盖 closed-candle reference、availability、lookback start、label end、universe selection 与 supplemental report availability；strategy review 会把缺 temporal contract 的 replay evidence 判为 legacy/stale。
- 已补：R&D batch 输出 `statistical_report`，记录完整 trial universe、accepted/rejected、winner、OOS/effective sample、edge margin、deflated edge probability 与四时间块 CSCV/PBO；统计未决或 PBO 失败时不会把 winner 标成 ready。
- 已补：R&D candidate 与 strategy promotion 的 OOS gate 增加 raw/effective sample 与净 edge margin，不再只看正收益。
- 已补：`shadow -> live-small` 只接受 review-derived shadow attribution；手工填 `execution_attribution` 不能补齐 live-small gate。
- 已补：strategy review 输出 replay -> shadow -> live-small decay diagnostics；shadow 相对 replay 的 avg_r 保留率过低时阻断 live-small，并归因为 execution/reality decay。
- 已补：strategy review 输出 `cost_model_feedback`，把 review-derived fee / slippage / funding / total cost drag 反灌成 per-trade R 值与 unknown-size capacity bucket，供下一轮 replay cost stress 使用。
- 已补：forward holdout 测试版输出冻结候选 hash、状态和下一步动作；主数据与 benchmark / supplemental 数据都必须是机器可读 `frozen_at` 之后的闭合样本，缺 `frozen_at` 的真实 artifact 会被拒绝。
- 已补：calibration panel 明示 `survivor_only`，并支持通过外部归档 manifest 合入 inactive / delisted symbol；没有可靠归档输入时仍不得声称 survivorship robust。
- 已补：R&D replay / panel / campaign / forward holdout / shadow tracker 读取 manifest 时支持把迁移前 `data/*-panel-*` 路径安全解析到当前 `tmp/panels/*`，并加回归测试；旧 artifact 可复读，但新产物仍应写 repo 相对 `tmp/panels/...`。
- 已补：panel R&D 单候选时 `cross_candidate_asset_shuffle_v1` 不再显示 `passed=true`；状态仍是 `not_applicable`，避免误读成 panel-level negative control 通过。
- 已补：forward holdout 在全部阻塞原因只是主数据 / benchmark 尚未晚于 `frozen_at` 时，`next_action` 改为等待下一根闭合 K 线并刷新 manifest，而不是误报“修数据覆盖”。
- 已补：新增 `research.data-split`。新 hypothesis 开研前可把 OHLCV manifest 物理切成 discovery / validation / locked_holdout 三个独立 manifest，并自动按 max hold / feature lookback / funding interval 留 embargo；locked holdout 在策略合约冻结前不再需要靠人脑“记得别看”。
- 已补：`research/rd-artifact-summary` 可自动识别普通 R&D loop 与 panel R&D artifact；普通 loop 摘要会暴露 `failure_summary`、`reliability_gate`、候选 R/OOS 指标与 blocker，避免 no-promote 被误读为空结果。
- 未补：按订单 notional / ADV / depth 的 capacity 与 market impact 分桶、White Reality Check / Hansen SPA 完整实现、可靠 delisted 历史数据源。

## 当前测试状态

- 2026-07-13 23:20 CST，手动跑一次 J04 R&D supervisor：
  - 初始入口：`rd-supervisor --supervisor-job` 可自动初始化 `data/rd_state.db`，但空 `next_hypothesis_queue` 立即停止为 `data_or_tool_blocked`，只给出 `marketability_score_v1` queue seed 建议，未自动落 queue。
  - 为完整验证链路，手动用 `research.data-split` 把 `.cache/outline-audit/btc-usdm-20260423/manifest.json` 切为 discovery / validation / locked_holdout：216 / 72 / 72 根 4H，embargo 30 根；locked holdout 未打开。复盘修正：这里不应为适配 420 根缓存而降低 `min_segment_rows`，正确动作是先用 `ohlcv-fetch` 补足 OHLCV，再按默认门槛切分。
  - 手动生成并 validate `btc-4h-tsm-manual-2026-07-14` hypothesis contract，转成 ready queue item 后跑 supervisor；产物 `tmp/artifacts/strategy-rnd/rd-program-btc-4h-tsm-manual-2026-07-14-20260713162000.json`。
  - 策略质量：`time_series_momentum_v1` 单候选，`trial_count=1`、`accepted_count=0`、`sample_count=3`、`avg_r=-0.586246`、`total_r=-1.758737`、`PF=0.120632`、`maxDD=2R`；blocked by `R-SAMPLE-SIZE / R-EXPECTANCY / R-PROFIT-FACTOR`，结论是 `no_promote`，不得进入 validation / shadow。该结果首先说明本次数据准备不合格，其次才说明该粗糙 TSM 假说没有 edge。
  - 流程问题统计 6 个：缺 DB 时 read 报底层 `unable to open database file`；空 queue 只推荐不补队列；标准位置缺可直接研发的 manifest，需从 `.cache` 找且源 manifest 含本机绝对路径；为跑通流程降低 `min_segment_rows`，而不是先补 K 线；designer contract -> queue -> state 仍需人工桥接；queue item 默认 `mode=loop` 时即使带 `validation_manifest_path` 也只跑 discovery loop。
  - 优化点统计 6 个：read 缺库错误应分层；supervisor 可在空 queue 时自动调用 designer 或明确输出可执行 patch；准备一组 repo-relative smoke-test manifests；数据不足时先用 `ohlcv-fetch` / `ohlcv_store` 补足再切分，禁止降低研究门槛；designer `queue_item` 可支持直接生成 state update patch；带 validation manifest 的 ready item 默认走 campaign，除非显式 `mode=loop` 且说明会忽略 validation。
  - 开发过程复盘：系统成功把失败写回 `rejected_mechanisms / universe_lessons / artifact_refs`，但第二轮又因“无 ready hypothesis”把总状态置为 `data_or_tool_blocked`；这适合暴露问题，不适合无人值守连续开发。下一轮应先生成一条全新的市场机制假说，而不是给本次 TSM 加过滤器。
- 2026-07-10 19:31 CST，`S-ALT-4H-HIGH-BETA-SHORT-MOMENTUM` 按现行 `research.data-split` 重跑 validation：10 资产、validation 段、固定 STC short momentum candidate；结果 `outcome=no_promote`，pooled `sample_count=364`、`avg_r=0.021334`、`total_r=7.765575`、positive assets `5/10`，blocked by `PANEL-BREADTH / PANEL-COST`。Locked holdout 未打开；策略保持 `draft`。
- 2026-07-09 19:45 CST，新增三条 liquid-alt 机制检查：
  - `vol-compression-alt-validation-2026-07-09`：VCB long 三变体全部 `no_promote`；原始 `VCB-L-30-120` 在 8 资产 panel 上 `total_r=-32.522326`、3/8 资产正， blocked by breadth / cost / catastrophic / asset-shuffle。
  - `relative-capitulation-reversion-long-2026-07-09`：BTC 弱势里做相对输家多头回归失败；三变体全部 pooled negative，最差 `RRV-L-BTCWEAK-180-1R-RC` 为 `total_r=-199.35362`、0/8 资产正。
  - `relative-btc-weak-overstrong-short-2026-07-09`：`RRV-S-BTCWEAK-180-1R-RC` 有机制但不过 broad-universe gate；`sample_count=1322`、`avg_r=0.048568`、`total_r=64.207004`、7/8 资产正，panel asset-shuffle 通过，但 `TRXUSDT` 触发 `PANEL-CATASTROPHIC`（`total_r=-28.993152`、`max_drawdown_r=41.550344`）。
- 已新增 draft policy：`S-ALT-4H-BTC-WEAK-RELATIVE-WINNER-REVERSION-SHORT`。它只记录受限假设，不提供 shadow / promotion evidence；任何排除 TRX-like 资产的版本都必须作为新 hypothesis 在 fresh panel / forward holdout 上验证。
- 2026-07-09 20:35 CST，完成两条后续诊断：
  - 路径 fallback 修复验证：旧 `alt-panel-competition-input-2026-07-08.json` 仍含 `data/calibration-panel-*` 路径，修复后可直接复跑并得到原始 panel summary。
  - BTC 弱势相对赢家 short 风险修复诊断：0.5R BE 仍被 TRX catastrophic veto，1R BE 更差；非 TRX 外部 panel 上原始候选通过当前 gate（`total_r=7.844068`，4/5 资产正），但 SOL 为负且 panel 已被看过，只能作为机制支持。
- 2026-07-09 22:10 CST，BTC 强势相对赢家 short draft 做冻结候选外部检查：
  - `relative-reversion-btc-strong-fresh-check-2026-07-09`：`BCH/LTC/ATOM/NEAR/APT`，固定 `RRV-S-BTCSTRONG-NONMEME-120-1R-RC-FROZEN`，`sample_count=399`、`avg_r=0.063149`、`total_r=25.196466`、4/5 资产正、无 panel gate blocker。
  - `ATOMUSDT` 为负（`total_r=-5.157863`），OOS 与 cost stress 均为 false；单候选 panel asset-shuffle 不适用，不能把这轮解释成 panel negative control 通过。
  - forward holdout 以 `frozen_at=2026-07-09T14:00:00Z` 运行，全部被 `HOLDOUT-NOT-FORWARD / HOLDOUT-SUPPLEMENTAL-NOT-FORWARD` 阻塞；最新闭合 K 线是 `2026-07-09T12:00:00Z`，下一步是等下一根冻结后 4H 闭合 K 线并刷新资产与 BTC benchmark manifest。
- 2026-07-10 09:05 CST，开研 BTC 4H volatility compression breakout long：
  - 先跑 `research.data-split`：discovery `2019-09-08 -> 2023-09-04`，validation `2023-10-07 -> 2025-06-05`，locked holdout `2025-07-09 -> 2026-07-08`，embargo `200` 根 4H；locked holdout 未打开。
  - campaign `btc-4h-vcb-rd-2026-07-10-a` 预声明 6 个 long VCB candidate；discovery 结果 `accepted_count=0`、`outcome=no_promote`，未消耗 validation。
  - 主要 blocker：`R-PROFIT-FACTOR` 6/6、`RND-OOS-EFFECTIVE-SAMPLE` 6/6、`R-EXPECTANCY` 5/6；`reliability_gate.decision=reject_hypothesis`。下一步不能给旧候选事后外挂过滤器；若继续使用过滤 / 资产选择 / 风控改造，必须作为新市场机制或新 candidate strategy hypothesis 预声明。
  - artifact：`tmp/artifacts/strategy-rnd/btc-4h-vcb-rd-2026-07-10-a.campaign.json` 与 `tmp/artifacts/strategy-rnd/btc-4h-vcb-rd-2026-07-10-a-H-BTC-4H-VCB-LONG-001-discovery.json`；R&D ledger 已登记到 `data/data_catalog.db`。
- 2026-07-10 10:10 CST，开研 BTC 弱势下 high-beta alt 相对弱者 short continuation：
  - `relative-loser-continuation-btc-weak-2026-07-10-a`：6 资产 high-beta panel，4 个候选全部 `no_promote`；`RWM-S-BTCWEAK-180-1R` 有局部苗头（`sample_count=1024`、`total_r=19.099307`、asset-shuffle 通过），但仅 2/6 资产正，blocked by breadth / cost / catastrophic。
  - `relative-loser-continuation-btc-weak-broad-2026-07-10-a`：8 资产 broad liquid-alt 泛化检查仍 `no_promote`；同一 `RWM-S-BTCWEAK-180-1R` pooled `total_r=58.744121`、5/8 资产正、asset-shuffle 通过，但 blocked by OOS / cost / catastrophic，`NEAR/LTC` 亏损与多资产 drawdown 过重。
  - 结论：该机制不是“调参即可用”；若继续，必须作为新 hypothesis 明确 universe / risk owner / market-state filters，而不是排除亏损资产后复用同一 holdout。

## 外部校准

- White 的 Reality Check 重点不是“不要搜索”，而是承认时间序列里数据复用几乎不可避免，必须检验“搜索出来的最好模型是否真的优于基准”。见 [White 2000](https://www.ssc.wisc.edu/~bhansen/718/White2000.pdf)。
- Harvey/Liu/Zhu 对 factor zoo 的结论很硬：大量因子挖掘后，传统 `t > 2` 不够，新因子通常要更高门槛。见 [NBER w20592](https://www.nber.org/system/files/working_papers/w20592/w20592.pdf)。
- Bailey/Lopez de Prado 系列把问题落到 DSR/PBO：Sharpe / 回测表现要同时修正多重测试、样本长度、非正态与选择偏差。见 [DSR](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551) / [PBO](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2326253)。
- 实盘差异主要来自 reality model：fee、fill、slippage、capacity、market impact 与 broker/live reconciliation。见 QuantConnect [reality modeling](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/key-concepts)、[slippage](https://www.quantconnect.com/docs/v2/writing-algorithms/reality-modeling/slippage/key-concepts)、[reconciliation](https://www.quantconnect.com/docs/v2/writing-algorithms/live-trading/reconciliation)。
- 参数优化如果在同一历史段反复找最优再回测，本质就是 look-ahead。见 QuantConnect [optimization parameters](https://www.quantconnect.com/docs/v2/writing-algorithms/optimization/parameters)。

## 当前强项

- `calibration -> R&D -> locked holdout -> evidence -> shadow -> live-small` 的链路边界清楚；R&D ledger 和 strategy evidence 分离，避免失败搜索污染交易事实。
- campaign 有全局 `<=10` trial budget；locked holdout 重复使用会被 `strategy-rnd-ledger` 拦住。
- replay 已固定 next-open、stop-first、gap 更差开盘、双边 fee/slippage、funding coverage gate、fingerprint freshness。
- candidate 必须过 side-flip / entry-lag negative control；panel R&D 有 asset shuffle negative control；promotion gate 会挡住缺 funding、panel negative control、robustness、shadow attribution 的证据。
- 文档明确禁止自动升格和 holdout 失败后继续调参，这是正确的研究纪律。

## P0 缺口

1. **Holdout 只查不重叠，缺 embargo。**（已补）
   `rd-campaign-runner` 只要求 discovery / validation manifest 时间范围不重叠；若 validation 紧贴 discovery，仍可能泄漏持仓标签、indicator lookback、funding/market state。
   整改：新增 `RND-HOLDOUT-EMBARGO`，要求两段之间至少隔开 `max(max_hold_bars, max_feature_lookback, funding_interval)` 对应时间；带 factor report 时 embargo 还要覆盖 factor lookback。

2. **升 shadow 的统计门槛偏低。**（已补基础门槛）
   当前 locked holdout `sample_count >= 10` 且正收益/PF `>=1.05` 可以过 replay 层。对 4H swing 可接受为“影子候选”，但不能证明 edge；在现在的市场里，10 笔很容易被 regime 和执行误差吞掉。  
   整改：增加 `effective_sample_count`、bootstrap 下置信界、DSR/PBO 或等价保守统计；同时要求净 edge 超过成本模型误差缓冲，比如 `avg_r_lower_ci > 0` 且 `net_edge > 2 * execution_error_band`。

3. **Shadow 执行归因还不够自动。**（已补 gate 来源约束）
   roadmap 已标“下一块”：从真实 shadow order/event 自动汇总 fee、slippage、funding。只要这里依赖人工填报，`shadow -> live-small` 就可能被乐观归因放行。  
   整改：live-small 前只接受由 shadow order、mark/last、funding event、exchange fill/reconcile 自动生成的 attribution；人工 notes 只能解释，不能补齐 gate 字段。

## P1 缺口

- **strategy thesis certificate 已有基础 gate。** 当前已强制 campaign 预声明 edge 类型、市场行为假设、参与者、适用 regime、失效条件、成本敏感度、候选 universe、negative controls；后续还可把预期持仓/换手与对应 negative control 的覆盖关系做得更细。
- **Panel survivorship 已有基础防线。** 当前 20 个可交易资产 panel 会明示 `survivor_only=true`；若提供可靠 inactive / delisted manifest，可合入 calibration suite 并取消 survivor-only 标记。剩余缺口是真实归档数据源与 listing-age-aware universe。
- **Panel artifact 路径边界已补读路径兼容。** 旧 artifact 中仍可见 `data/calibration-panel-*` / 绝对路径痕迹；当前已在读路径加兼容，后续仍要求新产物落在 `tmp/panels/` 且输出 repo 相对路径。
- **Reality model 已有反馈闭环基础。** 当前已有 replay -> shadow/live decay 诊断与 `cost_model_feedback`；下一步应把 review 里的订单 notional / ADV / depth 接入，形成真实 capacity / impact 分桶。继续不伪造 maker 队列成交概率。
- **统计报告已进入最小正式版。** 当前 full-trial report 已避免只看 winner，并加入 deflated edge probability 与四时间块 CSCV/PBO；后续要继续补 White Reality Check / Hansen SPA 和更严格的 DSR / min track record length。

## 实用流程判定

可投入使用的范围：

- 用 calibration suite 判断数据、成本、funding、panel 是否可靠。
- 用 campaign 做少量预声明假设验证。
- 把 locked holdout 通过者送入 shadow，禁止直接 live-small。
- 用 forward holdout 测试版验证冻结候选在 `frozen_at` 后真实闭合样本上的信号表现；结果只能进入 shadow/review 判断。
- 用 R&D shadow tracker 持续跟踪 forward 信号；关闭后生成 review draft，再决定是否整理为正式 strategy evidence。
- R&D tracker 需按 [rd-event-chain-design.md](../architecture/rd-event-chain-design.md) 补 entry / observation / exit / review_draft 事件链；否则 review 只能看终点，无法区分 entry、exit、regime、execution 归因。
- 用失败 ledger 指导回到数据、成本、regime、样本或假设层，不继续加 trial。

暂不应投入使用的范围：

- 自动连续挖策略。
- 单资产漂亮回测升格。
- 未自动归因的 shadow 样本升 live-small。
- 看完 holdout 后改参数/过滤器继续使用同一 holdout。

## 最小整改队列

1. 接入可靠 delisted / inactive 历史数据源，并形成 listing-age-aware calibration universe。
2. 补 White Reality Check / Hansen SPA 与更严格 DSR / min track record length。
3. 接入订单 notional / ADV / depth，形成 capacity / impact 分桶。
