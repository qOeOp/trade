summary: Resolve one quarantined historical R&D V1 research goal
description: Read-only exact-identity resolver for historical V1 custody; fresh V1 submission is disabled.
lock: "!inline f/trade/product_edge/research_goal_v1.script.lock"
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  order:
    - action
    - request_identity
  properties:
    action:
      type: string
      enum:
        - RESOLVE
    request_identity:
      type: string
  required:
    - action
    - request_identity
tag: rd-product-edge
