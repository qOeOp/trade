[package]
name = "vibe-observability"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
description = "Read-only, rebuildable status projections for VibeTrader observability"
repository.workspace = true

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dev-dependencies]
rstest = { workspace = true }
