{
  "$id": "trade.protocol.domain-outbox-envelope.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_id", "message_id", "source_domain", "rail", "payload_ref", "created_at"],
  "properties": {
    "schema_id": { "const": "trade.protocol.domain-outbox-envelope.v1" },
    "message_id": { "type": "string", "minLength": 1 },
    "source_domain": {
      "type": "string",
      "enum": [
        "orchestration-ops",
        "policy-risk",
        "portfolio-execution-state",
        "market-data-products",
        "exchange-gateway",
        "live-decision-planning",
        "live-execution-control",
        "research-strategy-development",
        "governance-review-compliance",
        "artifact-knowledge"
      ]
    },
    "target_domain": {
      "type": "string",
      "enum": [
        "orchestration-ops",
        "policy-risk",
        "portfolio-execution-state",
        "market-data-products",
        "exchange-gateway",
        "live-decision-planning",
        "live-execution-control",
        "research-strategy-development",
        "governance-review-compliance",
        "artifact-knowledge"
      ]
    },
    "rail": { "type": "string", "minLength": 1 },
    "payload_ref": { "type": "string", "minLength": 1 },
    "idempotency_key": { "type": "string" },
    "created_at": { "type": "string", "minLength": 1 }
  }
}

