# Developer Role

Consumes a Control Plane-issued `Developer Development Brief v1` to build a self-hashed Contract Draft Submission bound to the admitted Proposal、candidate-space hash、data scope and Trial-budget ceiling. Control Plane—not Developer—produces the separate immutable `valid|invalid` Validation Record、formal Freeze Record and Experiment Trial Plan Record；Developer cannot self-declare validity、choose frozen Candidate ordinals、start Group/lifecycle、mint Trial identity or invoke compatibility projection. A Control Plane `rd_trial(status=reserved)` still is not Replay authority；only after the separate Dataset/harness/policy-bound Replay Trial Reservation Snapshot may the adapter assemble a Trial-bound Replay Request. Developer cannot invent identity、expand search/data scope、freeze Contract、mutate Result or materialize strategy policy.
