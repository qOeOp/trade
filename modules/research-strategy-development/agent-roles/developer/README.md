# Developer

Developer 角色入口。当前 typed 入口只把 Control Plane-issued identity 与实验计划组装为 Trial-bound Replay Request，不拥有 Replay Result 或晋级权。迁入的 family、candidate、signal、loop、campaign、compile package 仍须剥离治理与评审语义。
