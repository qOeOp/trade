# Reviewer

Reviewer 角色入口。当前 typed 入口把已注册 Replay Result 组装成显式 `accept_for_draft` authorization；迁入的 signal evaluator 与 artifact summary 只是 read-model migration source。生命周期、KG 与策略文件仍由 Control Plane owner 写入。
