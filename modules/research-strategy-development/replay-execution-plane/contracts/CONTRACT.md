# Replay Execution Plane Contracts

M4-P26 adds an explicit `Portfolio Fixed Partial` successor over the certified single-Lane Replay Result. Terminal, owner-keyed Accounting and 1–8 full-flat cycle Evidence/Manifest bind the immutable partial intent, Lane Result/Artifact, remaining quantity, generation-2 protection, cash/collateral, exposure and historical/current risk split. Default materialization rejects partial; one fixed reduce must fully fill and leave a nonzero Position, original isolated collateral remains reserved until full-flat, and lower utilization authorizes no entry. P15–P25 remain authoritative; dynamic/repeat partial, reentry, cross-margin, real liquidity and Fast are excluded.

M4-P25 adds one end-to-end `Portfolio Protective Stop Cancel` successor family: target-only terminal、risk-degraded state、owner-keyed Accounting and 1–8 cycle consolidated Evidence/Manifest. It consumes zero-or-one frozen final `cancel_active_stop_preserve_target` per Lane；the historical admission stop-risk remains hash-bound and reserved, while current active-stop bounded risk becomes null for an open cancelled Lane. Cancel creates no cash/Journal posting and releases no risk；only committed target/liquidation full-flat may release the reservation and admit a successor cycle. Default Portfolio materialization still rejects the Schedule；P15–P24 and their hashes remain authoritative.

M4-P24 adds one end-to-end `Portfolio Strategy Exit Cancel` successor family: bracket-preserved terminal、owner-keyed Accounting and 1–8 cycle consolidated Evidence/Manifest. It consumes zero-or-one frozen pending full-position strategy exit/cancel pair per Lane, binds the original exit time and cancel intent, preserves generation-1 stop/target, and makes the former exit unreachable. Default Portfolio materialization still rejects the Schedule；P15–P23 and their hashes remain authoritative.

M4-P23 adds one end-to-end `Portfolio Protective Take Profit Cancel` successor family: cancel-aware terminal、owner-keyed Accounting and 1–8 cycle consolidated Evidence/Manifest. It consumes zero-or-one frozen initial full-position target cancel per Lane, preserves the initial full stop, keeps decision-boundary target priority and makes the former target unreachable afterward. Excluded former-target terminal hashes、long/short owner、cash/NAV、cycle-local hashes and one sequence opening equity are hash-bound；stop cancel、repeat mutation、partial、reentry、cross-margin and Fast remain forbidden.

M4-P22 adds one end-to-end `Portfolio Protective Take Profit Replacement` successor family in a single change set: terminal Evidence/Manifest、owner-keyed Accounting and 1–8 cycle consolidated Evidence/Manifest. It consumes zero-or-one frozen full-position reduce-only initial-target replacement per Lane；the decision bar remains generation 1, generation 2 starts only on a later market boundary, and the initial stop is preserved. Long/short target gap/range、old-target race、stop-first collision、exact liquidation、cash/NAV and cycle roll-forward are hash-bound. P15–P21 remain immutable；repeat replacement、cancel、stop mutation、partial、reentry、cross-margin and Fast remain forbidden.

M4-P21 adds `Portfolio Protective Stop Replacement Cycle Sequence Evidence/Outcome/Artifact Manifest v1` as a new successor without changing P17/P18. One immutable 1–8 cycle Plan/Reservation is materialized into cycle-local P19/P20 commits；only each committed P20 Trial Balance may seed the next cycle. The sequence binds replacement-aware owner/generation sets、child evidence/manifests、one global cash chain、one opening-equity Journal and a consolidated Trial Balance through eleven manifest-last payloads. Runtime expansion、free amend、target mutation、partial、reentry、cross-margin and Fast remain forbidden.

M4-P20 adds `Portfolio Protective Stop Replacement Terminal Accounting Evidence/Outcome/Artifact Manifest v1` as a read-only successor to committed M4-P19. Ledger、Journal and Trial Balance use the P19 replacement-aware record hash and owner—including `replacement_protective_stop`—as their source key, bind the P19 manifest/evidence plus the Risk Result recovered through the embedded P15 lineage, exclude preempted terminals and post-terminal Funding, and reconcile exactly to P19 cash/collateral/unrealized/NAV. Eight immutable payloads precede one accounting manifest. P16 and P17/P18 remain immutable and do not consume this evidence.

M4-P19 adds `Portfolio Protective Stop Replacement Terminal Evidence/Outcome/Artifact Manifest v1` as a successor to committed P15. It accepts at most one frozen `authorized_protective_stop_replace` per Lane, requires tighten-only、full-open-position、stop-market、reduce-only semantics, preserves the target, and binds the predecessor P15 record/evidence/manifest plus Risk Result. A replacement decided at bar close becomes active only after that bar's market range；a terminal on the same boundary remains generation 1. Generation-2 gap/range resolution, terminal owner, economics and fingerprint are republished through six manifest-last payloads. P15–P18 remain immutable and do not yet consume this successor.

M4-P18 adds `Portfolio Protective Terminal Cycle Sequence Accounting Evidence/Outcome/Artifact Manifest v1` as a read-only successor to committed M4-P17 plus every hash-bound cycle P16 Artifact. It preserves each P16 Ledger/Journal entry and hash, removes only cycle 2+ local `opening_cash` from the consolidated view, and proves one sequence opening equity、global cash roll-forward、balanced Trial Balance and final P17 cash/NAV. Nine fixed payload roles use manifest-last commit；source/read/projection/publication failure returns no consolidated accounting Evidence. P7/P12/P13 and cycle-local P16 remain immutable.

M4-P17 adds `Portfolio Protective Terminal Cycle Sequence Result/Outcome/Artifact Manifest v1` without modifying M4-P12/P13. One immutable Control Plane Sequence Reservation is deterministically projected into each declared cycle authority；every cycle commits real P9 Integrated、P14 Mark/Risk、P15 terminal-owner and P16 accounting Artifacts. Only the committed P16 Trial Balance `ending_available_cash` may seed the next cycle, and every predecessor must be protective-terminal full-flat. The sequence Result binds every child evidence/manifest hash and a five-payload manifest-last commit；mid-sequence failure publishes no sequence Result. Runtime append, stop mutation, partial and reentry remain forbidden.

M4-P16 adds `Portfolio Protective Terminal Accounting Evidence/Outcome/Artifact Manifest v1` as the read-only accounting successor to committed M4-P15 evidence. It binds the exact P15 manifest/evidence and Risk Result, gives every economic posting a terminal-record owner, distinguishes generic `terminal_trading_fee` from liquidation fee, and requires a single-opening-equity double-entry Trial Balance equal to P15. Preempted terminals and post-terminal Funding are immutable exclusion sets. Eight fixed payload roles use manifest-last commit；retry is create-or-identical and failure returns no accounting Evidence.

M4-P15 adds `Portfolio Protective Terminal Evidence/Outcome/Artifact Manifest v1` as the economic successor to committed M4-P14/P9 evidence without rewriting either. Its authority is limited to one simultaneous initial Allocation cycle with no reentry and the immutable initial stop/target already frozen in each Trial Request. One Lane has exactly one terminal owner; funding phase 10 precedes exact liquidation phase 15, which precedes OHLCV protection/strategy exit phase 20, while same-open protection precedes strategy exit. Single touch/gap is exact; stop-target collision is resolution-limited and conservatively stop-first. The Result rebuilds wallet, collateral, NAV and exposure/risk release, but does not yet certify a successor Journal.

M4-P14 adds `Portfolio Mark Risk Revaluation Evidence/Outcome/Artifact Manifest v1` as a read-only successor to the committed M4-P9 Integrated Portfolio Artifact. It binds every transition to the authoritative Risk queue, revalues exact-Mark gross/net exposure, preserves the frozen-stop admission risk budget, and reports prospective mark-to-stop drawdown separately. Dynamic cap breaches are observation-only. An exact Mark crossing the initial frozen stop is `resolution_limited` and cannot synthesize a Fill because Portfolio terminal stop ownership is not yet integrated. Eight immutable payload roles use manifest-last commit; source, aggregate, hash or publication drift yields no revaluation evidence.

M4-P13 adds `Portfolio Cycle Sequence Accounting Evidence/Outcome/Artifact Manifest v1` as a read-only successor to the committed M4-P12 Artifact. It preserves every cycle-local P7 entry/hash, adds only sequence wrappers with global order, includes exactly one cycle-1 opening-equity posting in the consolidated Journal, and requires the consolidated Trial Balance to reconcile the final Sequence cash/NAV. The companion Artifact has eight fixed roles and a manifest-last commit marker；artifact read drift, repeated opening equity, cash-chain drift, unbalanced posting or publication failure yields no consolidated evidence.

M4-P12 adds `Portfolio Cycle Sequence Plan/Result/Outcome v1` without changing the historical P10/P11 schemas. One Plan binds the Control Plane sequence Reservation and an ordered 1–8 element array of Integrated/Allocation/Risk Plan hashes. Result wraps unchanged per-cycle transitions with numeric `cycle_index`, exact cycle records and one cash/time chain；runtime append, missing/reordered cycles, non-flat predecessor, cash drift and same/earlier successor time fail closed. Artifact roles remain fixed while the cycle evidence payload is dynamic, so cycle count cannot create a schema treadmill.

Owns the versioned Replay request, Dataset Manifest, instrument-accounting spec, market input, fill, ledger, journal, trial-balance, result, artifact, and fingerprint contracts.

## Certified Result v50 / Simulator Policy v22 capability

- Instrument-status producer implementation and normalization policy each bind both a version and a SHA-256 content hash; version labels alone are not reproducibility authority.
- One asset and one isolated position lane.
- Closed-candle signal with earliest execution at the next bar open.
- Signal-time market submission remains inert until the eligible `bar_open` SourceEvent synchronously drives activation, entry fill, and bracket activation; same-time phase-`10` funding sees the `t-` flat position.
- Append-only submitted/activated/triggered/partial-or-full-filled/cancelled/rejected order events with a strictly increasing per-order sequence and nondecreasing event time.
- Every order event carries the total-order key `(event_time, boundary_phase, source_sequence, event_subphase, stable_event_id)`; equal or backward keys are rejected.
- Trial Reservation Snapshot v9 is owned and issued by Research Control Plane. It binds the half-open new-claim window `[issued_at, expires_at)` plus Request v36 risk/spec/status/decision/entry-execution authority and nullable liquidity-capacity-attestation hash. Runner requires Dataset provenance, Request and Reservation to name the same authority before execution. Replay does not query or mutate Trial/provider registry state.
- Replay Attempt Lease v1, Attempt Cancellation/Observation v1, Checkpoint Receipt v2 and Resume Authorization Snapshot v1 are Control Plane-owned runtime boundary contracts. Artifact v52 persists Decision Evidence Timeline v13、OHLCV Resolution Evidence v5、Pending Order Resolution v4 and Order State Snapshot v1; Run Outcome v35 carries current fencing、commit/failure state and optional cancellation/ambiguity evidence. Result/Fingerprint bind path economics plus status schedule/provenance/provider-certification identity while Checkpoint v29 remains the pre-derived resume payload. Attempt execution cancellation/checkpoint identity does not alter Result v50 economic fingerprint；contract-owned order Cancel、GTD expiry and scheduled decision evidence do.
- Artifact Store Capability v1 freezes one Attempt-scoped port contract: immutable create, full-content SHA-256 collision identity, strong read-after-write, and Manifest-last publication. The certified local snapshot requires hard-link create-if-absent plus temporary unlink. The contract-valid remote target requires conditional put with `If-None-Match: *` and abort/expiry of uncommitted uploads, but it is not an executable capability until an adapter passes the same parity and crash certification.
- Dataset Manifest v11 carries ordered PIT authority and an optional self-hashed Liquidity Capacity Attestation v1. Limit/Stop pending execution requires its symbol/capacity/hash to match Request v36 and Reservation v9, and `available_at <= signal_time`; GTD additionally requires `expires_at` to equal one supplied executable bar close. Result v50 Fingerprint and Artifact v52 bind it. The attestation is a static conservative ceiling with source/derivation provenance, not event-depth or queue-position proof. Gap, coverage, authority or hash drift is a pre-event failure.
- R4.77 adds pre-integration `Aggregate Trade Event v1` and `Aggregate Trade Coverage Attestation v1`. Events require one symbol、strictly increasing contiguous aggregate-trade ids、non-decreasing trade time、`available_at >= trade_time` and positive price/quantity. The attestation freezes one half-open window、first/last id、count、source/events hash and `external_completeness=not_verified`; binding rejects id gaps、hash drift and out-of-window events. These types are not Dataset Manifest/Request authority yet and do not alter certified capabilities.
- R4.80 adds pre-integration `Cross-source Event Envelope v1`、`Cross-source EventKey Policy v1` and `Ordering Attestation v1` for Instrument Status、Funding、Aggregate Trade and OHLCV boundaries. The key is `(effective_time, semantic_phase, source_rank, native_sequence, stable_event_id)`；`availability_at` remains a separate causal-visibility field. Source rank is only a deterministic tie-break. Equal effective timestamps across independent sources without a venue-global sequence force `resolution_limited` and explicit ambiguity groups；the attestation is forbidden as economic/Runner authority until a later wire contract binds it.
- R4.82 adds `SourceEvent Projection v1` and its self-hashed Attestation. It binds the actual Control Plane Ordering Admission and Ordering Attestation, then preserves every ordered envelope one-to-one as hash-only lineage: source/projected kind、native id/sequence、effective/availability time、payload/envelope hash and the complete cross-source key. Scope is pre-integration, economic authority is none, and production `ReplaySourceEvent` compatibility is explicitly not asserted because that schema lacks Aggregate Trade and the required provenance/order fields.
- R4.83 adds parallel candidate `SourceEvent Wire v2` plus Manifest v1 instead of mutating the legacy SourceEvent embedded in Result/Checkpoint hashes. Each wire event carries one canonical typed payload and reconstructs its R4.80 Envelope; payload、envelope、event-id and ordered collection hashes must close. The Manifest binds Projection/Admission/Ordering/Reservation/Dataset identity and fixes `parallel_epoch`、legacy unchanged/parity not certified、Runner not bound and no economic authority. Those authority claims remain unchanged after R4.84 materialization and limited schedule parity.
- R4.84 adds `Legacy Shared Event Schedule Parity Attestation v1`. It may certify one-to-one kind/effective-time/status-snapshot correspondence for Instrument Status、Funding and OHLCV boundaries materialized through Wire v2. It permanently excludes Aggregate Trade from the legacy side and does not assert payload、EventKey or cross-source same-timestamp order parity. The attestation remains non-economic and cannot bind Runner capability.
- R4.85 adds `Wire Pre-execution Gate v1` and `Candidate Trace v1`. The gate admits only `non_economic_schedule_trace`; `economic_exact_trigger` is rejected for either cross-source `resolution_limited` ordering or an uncertified economic consumer. Candidate Trace preserves per-event ambiguity-group hashes and deterministic-tie-break limitations while fixing every execution effect to none. Neither object is Request、Runner、Result or Artifact authority.
- R4.86 adds `Availability Cursor v1` and `Visibility Transition v1`. The immutable effective timeline remains bound to Candidate Trace; a separate visibility timeline is ordered by availability time and preserves the originating effective ordinal. Delayed events become visible only at `availability_at`, are classified as historical facts, and carry no retroactive execution effect. Cursor authority is non-economic and not Runner-bound.
- R4.87 adds `Visibility Cut v1`. An as-of cut must contain the complete availability-ordered prefix and hash the excluded suffix identities, so neither future inclusion nor omission of an already visible event can be hidden by rehashing. The cut exposes identity lineage only, carries no payload or decision authority, and is not a Harness、Runner、Result or Artifact input.
- R4.88 adds `PIT Payload View v1`. Each record is the exact typed Wire v2 payload for one Visibility Cut transition and binds its transition id、Wire id、payload hash and source-envelope hash. Future transitions remain identity-only and cannot materialize payloads. The view has no decision/economic authority and is not Harness- or Runner-bound.
- R4.89 adds `Decision Observation Projection v1` with a versioned exact-field whitelist. Bar-open exposes only open time/price; full OHLCV requires a closed-bar payload; status、funding and aggregate-trade chronology must bind visibility metadata. Projection records bind their source payload-record hash but have no Decision Input compatibility、Signal、Order or economic authority.
- R4.90 adds `Decision Schedule Observation Binding v1`. It structurally verifies an externally frozen Schedule v7/hash, binds one selected entry/hash, and requires its decision time to equal the Projection as-of time. The selected effect is an opaque label, not re-authorized or executed here; exact fields and lineage bind Projection、Payload View and Cut while Harness、Decision、Signal、Order、economic and Runner authority remain absent.
- R4.91 adds `Decision Schedule Observation Binding Set v1`. The set contains exactly one ordered R4.90 binding per frozen Schedule entry, rejects duplicate binding/projection identities and cross-Schedule members, and independently hashes members and their binding/projection hashes. It proves closed-world observation coverage only; effect semantics、Harness、Decision、Signal、Order、economic and Runner authority remain absent.
- R4.92 adds `Decision Observation Bundle v1`. It embeds the complete Binding Set plus exactly one full Projection payload per member and cross-checks projection id/hash、as-of time、observation count、Payload View and Cut lineage. Embedded payload is portable but external parent lineage remains mandatory for authoritative rebuild; Decision Input、Harness、Artifact、Runner、Decision、Signal、Order and economic authority are not granted.
- R4.93 adds Control Plane-owned `Decision Observation Bundle Admission v1`. The immutable sidecar binds the authoritative reserved Trial、Reservation v9、validated Request v30、registered Ordering Admission、Wire v2、complete Schedule binding and Bundle hashes. Its sole consumer capability is non-economic observation audit; `parent_lineage_validation=wire_identity_and_schedule_binding_only` and `projection_derivation_compatibility=not_certified` prevent the receipt from becoming Harness、Runner、Decision、Signal、Order or economic authority.
- R4.94 adds Replay-owned `Decision Observation Bundle Derivation Attestation v1`. Certification replays the full `Wire → Gate → Trace → Cursor → Cut → View → Projection → Binding → Bundle` lineage for every boundary and requires one common Wire/Gate/Trace/Cursor root. The portable object is a hash summary, so independent verification still requires external parent objects. Control Plane Admission compatibility remains `not_bound`; the attestation grants no Decision Input、Harness、Runner、Decision、Signal、Order or economic authority.
- R4.95 leaves the Replay attestation unchanged while Control Plane adds a separate Derivation Admission v1. That receipt may bind the attestation schema/hash and its boundary identities to an admitted Bundle, but it does not make Control Plane a second parent-chain certifier or grant Replay execution authority.
- R4.96 adds exact-field, self-hashed `Decision Observation Harness Context Binding v1`. It binds every admitted Bundle boundary to the canonical Harness Context v7 derived from Request v30 plus its frozen Schedule entry, including identity、decision time、seed and strategy-policy hash. It does not revise Context v7、Worker v9 or Receipt v11；Decision Input materialization is uncertified, all Worker inputs/requests are unbound, Harness invocation is forbidden, and no Decision、Signal、Order、economic or Runner authority is granted.
- R4.97 adds exact-field, self-hashed `Decision Observation Input Materialization v1`. Every boundary carries formal Decision Input Snapshot v1 and Market Input Snapshot v1 hashes: supplemental materialization is limited to the canonical empty `mode=none` case, while market lookback is selected only from admitted closed-bar observations and must remain closed、contiguous、interval-aligned and terminal at decision time. State Snapshot v3 remains null with runtime-state-required status where applicable；raw dataset validation、Worker Request、Harness invocation、Decision、Signal、Order、economic and Runner authority remain outside this contract.
- R4.98 adds exact-field, self-hashed `Initial Signal Supplemental Input Materialization v1` without changing R4.97. It binds Request v30、Dataset Manifest v11、the complete supplemental revision stream and the authorized initial Schedule entry；requires every fact to match exactly one frozen requirement；selects the last revision with `availability_at <= signal_time`；checks minimum count/freshness；and embeds the formal Decision Input Snapshot v1 plus future-revision lineage. It is a separate Dataset-bound input channel, not a fifth Market Wire source, and certifies neither rolling supplemental joins nor Worker/Harness/Runner/economic authority.
- R4.99 adds exact-field, self-hashed `Decision Worker Input Assembly v1`. It consumes one R4.96 Context Binding and exactly one same-Request materialization source. R4.97 supplies empty Supplemental plus Market snapshots；R4.98 supplies non-empty initial-signal Supplemental only, so the Assembly explicitly records `incomplete_market_snapshot` instead of accepting an unbound Market snapshot. Position-open entries remain `incomplete_runtime_state_snapshot`. Source bundle、build attestation、invocation identity and Worker Request stay null/unbound；the object has no Harness、Decision、Signal、Order、economic or Runner authority.
- R4.100 adds exact-field, self-hashed `Decision Market Input Materialization v1`. It binds every R4.96 Context boundary to its admitted Observation Projection and formal Market Snapshot, validating Request market requirement、Dataset ref/data/symbol/timeframe and complete Context lineage while deliberately not inspecting supplemental facts or requirement mode. Equivalent market parents produce the same Snapshot under empty and non-empty supplemental Requests, but Request、Context and materialization lineage remain distinct. It does not revalidate raw data or authorize Worker/Harness/Runner/economic effects.
- R4.101 adds immutable `Decision Worker Input Assembly v2` without rewriting v1. It requires one same-Request/context R4.100 Market parent plus exactly one R4.97 empty or R4.98 initial-signal Supplemental parent. The R4.97 path must prove its embedded Market Snapshot exactly matches R4.100 before R4.100 becomes the selected source；the R4.98 path can now form a complete non-executable tuple. Position-open still lacks runtime State. Complete never means Worker Request、Harness invocation、Decision/Signal/Order/economic or Runner authority.
- R4.102 adds exact-field, self-hashed `Position-open State Input Materialization v1`. Engine binds one existing formal Decision State Snapshot v3 to the matching Request/Schedule、R4.96 Context entry and complete SourceEvent prefix. The wrapper performs no economic recomputation and does not attest that an arbitrary snapshot was authentically produced by a certified runtime；independent prefix validation still requires the external complete prefix. R4.101 Assembly v2 remains unchanged and does not consume this object yet；Worker Request、Harness invocation、Decision/Signal/Order/economic and Runner authority remain absent.
- R4.103 adds immutable `Decision Worker Input Assembly v3` without rewriting v2. Engine embeds one self-valid v2 containing at least one incomplete position-open entry and exactly one schedule-ordered R4.102 parent for every such entry；Request、Trial、Candidate、Context and entry hashes must agree before every tuple becomes `complete_non_executable_build_unbound`. Validation covers embedded parent schema/hash and cross-binding only；full R4.101/R4.102 upstream lineage and SourceEvent-prefix replay still require external parents. No Worker Request、Harness invocation、Decision/Signal/Order/economic or Runner authority is created.
- R4.104 adds immutable `Decision Worker Input Assembly v4`. Runner binds one complete R4.103 v3 to the full matching R4.96 Context Binding、Harness Source Bundle v1 and Build Attestation v2；the Runner builder must deterministically rebuild the source under the attested Bun runtime and require byte-identical attestation/artifact before publishing `complete_non_executable_build_bound`. The contract independently checks embedded schema/hash and cross-binding, while independent source-to-artifact revalidation still requires an external rebuild. Registry admission、invocation identity、Worker Request、Harness execution、Decision/Signal/Order/economic and Trial Runner authority remain absent.
- R4.105 adds immutable `Decision Harness Code Admission v1`. Runner validates the fixed Registry Capability, resolves the v4 bundle hash, and freezes the capability、lookup value、complete returned registration and embedded v4 only when Source Bundle and Build Attestation are canonical-exact. This is an unsigned process-local interface observation, not durable registry authority：instance identity is unavailable、future lookup is not proven, and invocation identity、Worker Request、Harness execution、Decision/Signal/Order/economic/Trial authority remain absent.
- R4.106 adds immutable `Decision Harness Invocation Identity Set v1`. Every complete v4 tuple derives the exact existing Worker Request v9 six-field canonical hash through one shared pure function. The ID is logical input identity shared by the reproducibility subprocess pair—not process、attempt or retry identity. Legacy v9 does not directly hash Context；the Set records that limitation while binding Context through embedded parents. Worker Request remains null/forbidden and no Harness or execution authority is created.
- R4.107 adds immutable `Logical Request Identity Upgrade v1 / Identity Policy v2`. The next Worker epoch ID directly hashes target protocol/schema、Context、Code Admission and all phase-required snapshot hashes；legacy v9 ID is retained only as a migration alias. Attempt lease is intentionally excluded because renewal would destabilize logical identity；a future execution envelope must bind attempt/worker/lease separately. Worker v10 remains an identity target only—no v10 Request/Response or execution authority exists yet.
- R4.108 adds exact-field, self-hashed `Worker Request v10` plus immutable `Materialization v1`. Each Request embeds complete Context、Decision Input、Market Input and phase-required nullable State entities, revalidates their hashes and v2 logical ID, and binds Code Admission、bundle/artifact and the v9 compatibility alias. Attempt/process/lease fields are outside the whitelist. Every Request remains non-executable with no admission、envelope or transport；v9 execution is unchanged and Response/Harness/Trial/economic authority remains absent.
- R4.109 adds immutable zero-instance `Worker Response v10 Contract v1`. The response wire exact-echoes Request self-hash、Code Admission and every logical/input hash, and self-binds typed DecisionOutput、trace and Response hashes. A structurally valid response remains an `unadmitted_worker_claim`, not Schedule evidence. The Contract embeds R4.108 but contains zero responses；execution envelope、process/Harness Receipt、transport and all Decision/Trial/economic authority remain absent while v9 execution stays unchanged.
- R4.110 adds immutable `Attempt-bound Execution Envelope v1`. It keeps the Control Plane Replay Execution Request hash distinct from Worker Request v10 self-hash, and binds one logical Request to one exact Attempt/worker/ordinal/lease generation. Same-Attempt renewal requires a predecessor-linked higher-generation successor；cross-Attempt retry starts a new root while preserving logical identity. Worker authority is not process identity, dispatch freshness is unproven, and transport/Response/Harness/Decision/economic authority remains absent.
- R4.111 adds immutable `Dispatch Lease Admission v1`. It binds one R4.110 Envelope to a Control Plane current-Lease observation and admits freshness only when Attempt/worker/generation/hash match exactly and `heartbeat_at <= observed_at < lease_expires_at`. Renewal requires a successor Envelope before readmission；retry requires a new root. The observation is not an external clock attestation or an actual dispatch/process/transport receipt, and all Response/Harness/Decision/economic authority remains absent.
- R4.112 adds immutable `Dispatch Lease Authority Binding v1` and a local wire view of the Control Plane-owned Current Attempt Lease Observation Receipt v1. The Binding exact-matches receipt hash/time/Lease/Attempt/worker/generation to R4.111 Admission without importing Control Plane source types. R4.113 persists the unchanged Control Plane receipt create-or-identical；Replay contract does not claim registry-read provenance. External clock proof、actual dispatch/process/transport and all Response/Harness/Decision/economic authority remain absent.
- R4.114 adds immutable `Dispatch Evidence Registration v1`. It embeds the complete R4.112 Binding and derives one natural registry key from Attempt、generation and logical Request. First registration must remain inside the observed Lease window；the object declares local CAS durability and restart revalidation while keeping `dispatch_claim=null` and `dispatch_occurrence=not_materialized`. It is not Control Plane registry-read proof、external clock proof、process/transport/Response or economic authority.
- R4.115 adds immutable `Local Dispatch Claim v1`. It binds an actually re-read R4.114 Registration to a self-hash-verified post-registration Observation of the same exact Lease, then records first-claimant-wins local CAS exclusivity. The claimant is opaque and not process-attested；the claim remains `dispatch_occurrence=not_materialized`, can lose delivery after claim-before-dispatch crash, and is not process/transport/Response or economic authority.
- R4.116 adds immutable `Process Launch Attempt/Receipt v1`. The CAS Attempt binds an actually re-read Claim、post-claim exact-Lease Observation、attested runtime/artifact、fixed argv/env and zero-request EOF probe policy before spawn. The Receipt types local child-start or pre-start failure and binds PID context、exit/signal and output hashes. PID and Runner clock are not external attestations；an orphan Attempt is indeterminate/no-relaunch, and even a started probe carries zero Worker Request bytes and grants no dispatch、transport、Harness、Response or economic authority.
- R4.117 adds immutable `Transport Activation Gate v1`. It exact-binds the durable Process Launch Receipt、Claim/Envelope、target v10 Request and Build Attestation, then deterministically blocks because the artifact is attested v9、the Request remains non-executable/not-invoked and the completed probe is not a live dispatch process. Silent v10→v9 projection is forbidden；frame/write-receipt counts remain zero, no frame schema is predesigned, and future versioned capability/process evidence requires a new gate key.
- R4.118 adds immutable `Worker v10 Build Capability v1`. It embeds the exact R4.105 Code Admission and attests a separately generated Bun ESM decoder module with generated-source、runtime、artifact and v9-parent hashes. The decoder surface is one in-memory plain object and checks the Request v10 exact whitelist、protocol/schema and closed execution markers；full semantic validation remains Runner-owned. The capability explicitly has no stdio loop、frame、process/request/decode/dispatch occurrence、Harness invocation or authority and cannot mutate the R4.117 blocked gate.
- R4.119 adds immutable zero-instance `Worker v10 Single-request Transport Contract v1`. The certified mode fixes one fresh process per logical Request、one canonical-JSON UTF-8 LF Request frame followed by stdin EOF、one Response frame followed by process exit, with no pool、keepalive or multiplex. The inner Request `artifact_hash` remains the legacy v9 Code Admission migration anchor；the outer frame binds the R4.118 v10 `process_artifact_hash` through an exact Code Admission、Source Bundle and legacy-artifact bridge explicitly marked as non-long-term taxonomy. The current artifact remains a decoder module without a stdio loop；process/frame/write/read/dispatch counts are zero and no Response、Harness or economic authority is granted.
- R4.120 adds immutable `Worker v10 Stdio Process Capability v1` and `Negative Probe Receipt v1`. The capability exact-binds R4.119、its decoder/Source Bundle/runtime and a byte-distinct successor Bun artifact whose bounded stdin loop enforces EOF、UTF-8、single LF、no trailing frame and JSON parse boundaries. R4.119 still binds the decoder hash, so any parseable input exits `transport_activation_not_granted` before decoder invocation until a successor Transport Contract exists. The Receipt covers five fresh-process non-frame probes—empty、invalid JSON、missing LF、multiple frames and oversized—with exact exit/output hashes；it records local process observations but zero Worker Request frames、write receipts、decodes、dispatches、Responses、Harness calls or economic authority.
- R4.121 adds immutable zero-instance `Worker v10 Successor Transport Contract v2`. It exact-binds the R4.120 stdio artifact as the new outer process artifact while preserving the legacy v9 Request anchor、R4.118 decoder predecessor and immutable R4.119 contract. Its complete blocker set starts with Request v10's own closed execution/transport markers, then requires fresh Lease、Attempt-bound process、Request frame/write/decode and Response read/admission evidence. Five negative-probe processes remain diagnostic parents, not admitted instances；all successor instance/receipt counts are zero, activation is blocked, and no Harness or economic authority exists.
- R4.122 adds immutable zero-instance `Worker v10 Execution Admission Contract v1`. It chooses a separate Attempt/Lease-generation-bound Execution Admission Command instead of mutating Request v10 or creating Request v11 solely to flip authority. Request v10 remains a non-executable logical payload and its closed markers retain their original meaning；the future command is the executable object and must bind Request/logical identity、Attempt/worker/generation、durable Claim、issuance-time Lease observation、stdio artifact and Transport Contract. Renewal/retry command reuse is forbidden and future Response must echo command/request hashes. The current Contract binds only exact R4.121 evidence, records nine pre-issue blockers and zero command/process/frame/Response instances, and grants no dispatch、Harness or economic authority.
- R4.123 adds immutable `Worker v10 Execution Admission Pre-issue Bundle v1`. It exact-binds the R4.122 Contract、durable local Dispatch Claim and a strictly post-claim, pre-expiry Control Plane Lease observation, then proves both Claim and transport lineages name the same Request/logical id、Attempt/worker/generation、stdio artifact and Transport Contract. This closes the missing-Claim and missing-current-Lease evidence blockers only. The observation remains a wire view with caller-supplied UTC and no registry-read provenance or independent clock attestation；seven blockers remain, command/process/frame/Response counts stay zero, and no dispatch、Harness or economic authority exists.
- R4.124 adds the local wire view of Control Plane-owned `Lease Observation Registry Read Receipt v1` and immutable `Worker v10 Execution Admission Registry Provenance v1`. The Receipt binds an immutable registry row plus exact current active Attempt/Lease read in one Control Plane transaction；Replay exact-binds it to the durable R4.123 bundle without importing the Control Plane store. This closes registry-read provenance only. Caller-supplied UTC remains non-attested, six blockers remain, and command/process/frame/Response/dispatch/Harness/economic authority stay zero.
- R4.125 adds the local wire view of Control Plane-owned `Dispatch Clock Attestation v1` and immutable `Worker v10 Execution Admission Clock Attestation Binding v1`. The Control Plane samples wall and monotonic clocks before and after the registry/current-Attempt read without accepting caller time；Replay exact-binds the receipt to durable R4.124 provenance. This closes local caller-independent clock evidence only. It is not signed TSA/NTP/remote trusted time；five blockers remain and every executable/economic instance stays zero.
- R4.126 materializes immutable `Worker v10 Execution Admission Command v1`. Its natural key is Request/logical id + Attempt/ordinal + worker + Lease generation, while the payload exact-binds Claim、current Lease observation、registry/clock evidence、stdio artifact、Transport and the R4.122 authority Contract. A different valid clock receipt for the same generation collides instead of issuing a second command；renewal/retry require a new command. Command count is one, but process intent/receipt、frame、Response、dispatch、Harness and economic counts remain zero with four blockers.
- R4.127 materializes immutable `Worker v10 Process Launch Intent v1` without process start. One Command owns one intent natural key；the payload binds a strictly post-command Control Plane clock-bracketed current active Attempt read plus exact successor stdio artifact/runtime/argv/environment/resource bounds. The intent is non-executable and explicitly requires another spawn-boundary expiry/cancellation/fencing revalidation. Command/intent counts are one, process/frame/Response counts remain zero, and three blockers remain；the terminal v9 EOF probe is not reusable successor evidence.
- R4.128 materializes immutable `Worker v10 Process Launch Readiness Gate v1` and denies launch. The Gate proves the intent-bound R4.120 artifact terminally rejects every parseable frame before decode and that Frame v1 lacks Command/Intent binding and Command echo. Remediation requires versioned activated artifact、authority frames、Transport、Command and Intent；R4.119–R4.127 remain immutable. Gate count is one, process/frame/Response counts stay zero, the three downstream blockers remain, and four cutover findings are explicit.
- R4.129 materializes immutable `Worker v10 Authority Frame Build Contract v1` without building an artifact. Request Frame v2 binds Transport/artifact/Envelope/Command/Intent/Worker Request；Response Frame v2 must echo Command/Intent/exact Request-frame/Worker Request before admission. Inner Request/Response v10 remain immutable payload/result objects and gain no hidden execution authority. The activated build order is fatal UTF-8 + one canonical JSON LF frame → outer authority validation → v10 decode → Harness invocation → validated v10 response wrapped by exact authority echo. The contract consumes the exact durable R4.128 Gate, forbids reuse of old artifact/Frame v1/Command/Intent, and records zero activated artifact、successor Transport/Command/Intent、process and frame instances.
- R4.130 materializes immutable `Worker v10 Activated Stdio Capability v1` and one byte-distinct artifact without execution authority. Self-consistent Frame hashes are insufficient；the artifact requires one canonical JSON spawn authority capsule carrying expected Transport/artifact/Envelope/Command/Intent/logical Request/Worker Request hashes and compares every outer field before v10 decode. The future Intent freezes capsule derivation while the future Process Receipt must bind the observed capsule hash, avoiding both attacker-controlled self-assertion and Intent self-hash recursion. Missing/malformed capsules terminate before stdin/Harness at fixed exits 71/72. Build closure and bytes are certified, but no synthetic valid-authority probe is admitted；successor Transport/Command/Intent、capsule/process/frame/Response instances remain zero with six blockers.
- R4.131 materializes immutable `Worker v10 Authority Transport Contract v3` without issuing downstream authority. The exact durable R4.130 Capability、its byte hash、Frame v2 schemas、old Envelope/Request and immutable R4.121 predecessor are closed into one canonical object. Capsule static bindings come from v3；Command hash is added after future Command commit；Intent hash is derived only after future Intent commit and is never stored in that Intent payload. A future Process Receipt must exact-bind capsule、Command、Intent、artifact、runtime、spawn-boundary revalidation、Transport and Worker Request before any Response admission. Authority Transport count is one；new Command/Intent、capsule、process/frame/Response remain zero with five blockers.
- R4.132 materializes immutable `Worker v10 Authority Execution Admission Command v2` without Intent or dispatch. It consumes exact Authority Transport v3 plus a Control Plane clock-bracketed registry/current-Attempt read whose start is strictly later than predecessor Intent issuance；old Command time is not relabelled as fresh authority. One natural key binds Transport、Request、Attempt、worker and Lease generation, so alternate valid clock evidence cannot issue a second Command. The payload binds activated artifact、Frame v2、Envelope、Claim and current Lease but cannot embed its own hash；the launcher adds committed Command hash to the future capsule. New Command count is one；new Intent、capsule、process/frame/Response remain zero with four blockers.
- R4.133 materializes immutable `Worker v10 Authority Process Launch Intent v2` without capsule materialization or process start. It consumes the exact durable Authority Command v2 plus a Control Plane clock-bracketed current-Attempt read whose entire interval starts strictly after Command issuance, then freezes Authority Transport/artifact、Bun executable、fresh-process argv/private cwd、`0500` materialization、fixed base environment、Frame v2/resource bounds and post-commit capsule derivation. One Command owns one Intent slot, so alternate valid clock evidence collides. Because the capsule must bind the final Intent hash, the Intent payload declares the derivation rule but cannot contain that hash；the launcher may add it only after the exact Intent commit. New Intent count is one；capsule、spawn revalidation、process/frame/Response remain zero with four blockers including explicit `authority_capsule_not_materialized`.
- R4.134 materializes immutable `Worker v10 Authority Capsule v1` without spawn-boundary revalidation or process start. The outer record consumes only the exact durable Authority Intent v2；its inner environment value contains exactly the seven activated-artifact hash fields, while canonical JSON bytes and their SHA-256 are frozen separately from registry metadata and record hash. No caller field、local commit time or self-hash enters the environment value. One Intent owns one create-or-identical capsule slot. Capsule count is one；spawn revalidation、process receipt、process/frame/Response remain zero and three blockers remain.
- R4.135 materializes immutable `Worker v10 Authority Spawn Boundary Revalidation v1` without process start. A durable capsule produces one Control Plane-owned Request challenge；the Receipt binds that challenge and an authority-clock-bracketed direct current-Attempt transaction matching Attempt、worker、Lease generation/hash/expiry. Replay accepts one Receipt per Request natural key, so alternate valid clock evidence collides. The Receipt is one immediate spawn-transition candidate, not process evidence, and preserves the post-read cancellation/fencing race. Request/Receipt counts are one；process receipt、process/frame/Response remain zero with two blockers.
- R4.136 materializes immutable `Worker v10 Authority Process Launch Attempt/Receipt v1` and one fresh local child, without frame write. The durable Attempt is an at-most-once slot over exact R4.135 evidence；only its CAS creator may verify the Bun executable、materialize and hash-check the `0500` activated artifact、and spawn it with a fresh private cwd、fixed no-inheritance environment and exact capsule. Receipt binds full authority lineage、PID、local spawn-observed time、artifact/argv/cwd/environment hashes and process instance id while stdin remains open with zero bytes. Failure/orphan never auto-relaunches；a durable Receipt cannot recover the ephemeral live handle. Process Attempt/Receipt/live-instance counts are one；frame/Response remain zero with one blocker. Local spawn observation is neither kernel/remote attestation nor post-spawn current-Attempt proof.
- R4.54 adds Control Plane-owned Provider Certification v1 admission and binds one receipt across Dataset Manifest、Request、Reservation、Runner and Fingerprint. Replay validates the resulting closure but does not import the provider implementation, write its store, or treat certification/source finality as venue signatures/external completeness proof.
- R4.57 adds Control Plane-owned Provider Certification Termination v1 without changing Replay wire. Revocation/supersession only fences Reservation issuance at or after its non-retroactive `effective_at`; a previously issued Reservation and its later Attempt lease remain frozen authority. Replay neither queries lifecycle state nor applies a mutable "latest certification" rule.
- R4.58 adds Control Plane-owned Reservation/Attempt Cancellation v1 without changing Replay request/result wire. Reservation cancellation fences only future claim admission; Attempt cancellation fences one exact active lease generation. Runner observes cancellation through its existing external execution-control callback and publishes no partial Result/Artifact; Replay never queries Control Plane tables directly.
- Dataset Manifest v11 also declares one immutable supplemental revision stream and the canonical hash of `Supplemental Requirement Set v1`. Decision Schedule v11 and Timeline v13 bind every exact decision time/sequence/effect, closed-bar Market Snapshot, optional open-position State Snapshot, and input/build/runtime evidence. The certified pending-entry sequence may include one `authorized_entry_cancel` matching Request's immutable GTC Limit v1 or Stop-market v2 Cancel Intent；its phase is `pending_entry` and State Snapshot must be null. Position-open sequence may remain no-action、use one tighten-only stop replacement、one fixed-quantity partial reduce plus optional final full exit、one pending strategy-exit cancel，或以最后且唯一 mutation 取消 initial target/stop sibling并保留另一侧 full-position protection，或替换 initial target 且保持 stop 不变。Replay cannot add times, change effects, combine mutually exclusive mutations, alter triggers during partial resize, or widen quantity.
- Normalized `instrument_delisted`、`instrument_halted`、`instrument_resumed`、`funding`、`mark`、`bar_open` 与 `bar_range` SourceEvents use the same key space；same-time ordering is policy/status selection then phase `00 -> 10 -> 15 -> 20 -> 90`，delisting sequence precedes status transition，Result v50 persists the consumed prefix only。Supplemental facts form a signal-time input view, not market execution SourceEvents. Mark is risk/valuation evidence; only an exact maintenance breach may create the system forced-liquidation lane.
- Conditional trigger evidence binds the OHLC source class and observed trigger price.
- Market entry and full reduce-only stop or take-profit exit, with sibling cancellation and fill-to-order traceability.
- One optional final strategy exit is opposite-side market, reduce-only, and full-open-position. It is submitted at the closed-bar decision and can activate/fill only at its frozen strictly later bar open. Exact risk, stop gap, and target gap win at the same open; terminal-before-decision becomes not-reached, while a submitted losing exit is cancelled.
- One optional protective-stop replacement is opposite-side stop-market, reduce-only, full-open-position, and tighten-only. At a non-terminal closed-bar decision, the old active stop is cancelled before the replacement is submitted and activated. The replacement must remain on the protective side of the target and must not already be crossed by the decision close; it cannot retroactively trigger inside that bar.
- Composition certification does not add a new wire capability: after replacement, the replacement stop is the sole stop authority. A later strategy-exit Fill cancels that stop and the target; an exact maintenance breach cancels the current stop, target, and any pending strategy exit before the forced Order; a stop Fill cancels the target and pending strategy exit. Every path permits exactly one terminal Fill, and checkpoint resume preserves the same owner/order transitions without re-evaluating committed position-open decisions.
- Direction-symmetry certification mirrors prices around a fixed center: long stop tightening upward maps to short stop tightening downward; mirrored entry/stop Fills produce equal signed PnL/return and identical OrderEvent phase/subphase sequences. If the post-replacement OHLC range touches both current stop and target, both sides select the stop-first conservative path and must publish `ohlcv-stop-target-collision` with `resolution_limited`. This is the certified simple-bracket envelope, not proof of the actual intrabar path or a generic multi-order OHLC resolver.
- OHLCV Resolution Evidence v5 binds each stop/target terminal to SourceEvent/bar、`bracket|stop_only|target_only` protection mode、both Order statuses、P1/P2 outcome and cost-aware economics. Every path records frozen-policy adverse execution price、gross realized PnL、exit fee 与 net terminal contribution；the impact envelope records min/max/span/canonical shortfall and explicitly excludes common cashflows. Single-sided protection makes the cancelled sibling unreachable in both paths. Runner recomputes it from Request、Dataset accounting and entry Fill, then binds canonical price/fee to the actual terminal Fill. Artifact v52 publishes it separately. It is not a full counterfactual equity interval or path probability.
- A terminal market SourceEvent synchronously drives the certified exit-order lane; trigger/fill/sibling-cancel EventKeys remain causally bound to that source boundary.
- Conservative stop-first same-bar collision; stop/TP gap triggers at the observed bar open, then applies adverse slippage.
- `Pending Order Resolution v4` is self-hashed. Request v36/Simulator v22 certify pre-entry `limit + gtc|ioc|gtd` 与 `stop_market + gtc|gtd`。Limit 保持 open-marketable/strict-cross bounded-full-fill、touch 不成与 queue limitation；Limit/Stop-market GTD `expires_at` 必须严格晚于 earliest executable 且精确等于 supplied executable bar close，到期 bar 先解析 open/range，Limit strict-cross 或 Stop trigger 胜，确定 non-fill/non-trigger 才在 phase-90 expired。Stop open gap 取 observed open，range trigger 取 trigger reference，Fill 再施加不利滑点。Stop range 触发 bar 若同时触达保护价，`Stop Entry Same-bar Path Ambiguity v1` 继续证明触发后路径不可判定，Run Outcome typed-fail 且无 Result/Artifact；expiry 不得掩盖它。GTC 数据边界仍未成交/触发时以 active Order 和 `unfilled_at_data_end` 完成；IOC 仍只属于 Limit earliest-open。ordered resolution chain enters Checkpoint v29、Result v50 and Artifact v52.
- `ReplayEntryCancelIntent v1` 继续冻结 pre-entry GTC Limit compatibility；v2 只冻结 pre-entry GTC Stop-market。二者均由不可变 Experiment Contract 拥有，且 `requested_at=signal_time`、`effective_at>earliest_executable_time`、target 必须匹配 pending Order。Schedule v11 可选 `authorized_entry_cancel`，必须逐字段/哈希匹配同一 Intent。Harness Context v9 的 `pending_entry` 只消费 effective closed-bar Snapshot，State Snapshot 为 null；更早 Fill 以 Fill EventKey 记 not-reached且不运行 Harness。range SourceEvent phase `20` 先解析 Limit/Stop，Cancel/TIF phase `90` 后生效：Limit strict-cross 或 Stop trigger 胜；确定 non-fill/non-trigger 时 Cancel/GTD expiry 胜。未冻结运行时 Cancel、IOC/GTD Cancel、FOK、任意 intrabar GTD、amend/cancel-replace、multi-order volume allocation 与真实 partial liquidity remain unsupported.
- `Protective Stop Cancel Intent v1` 只允许 Schedule v11 的最后且唯一 position mutation；目标固定为 run-scoped initial stop，必须在 runtime 同时证明 stop/target active、数量与 full open Position 对齐。closed-bar risk/open/range terminal 先于 phase-90 Cancel；成功后只把 stop 置 cancelled，target 保持原 id/trigger/quantity 并进入 `target_only`。cancel bar stop Fill 胜并使 Decision not-reached；former-stop touch 不可达，target、exact liquidation 与 EOD 保持有效。不得与 target cancel、stop replace、partial reduce、strategy exit/cancel 或 entry cancel 组合，不开放无保护持仓与通用 runtime cancel。
- `Take-profit Replace Intent v1` 只允许 Schedule v11 的最后且唯一 position mutation；目标固定为 run-scoped initial target，冻结原/新 trigger、full-position reduce-only、stop-preserved 与 cancel→submit→activate。runtime 必须证明 stop/target active、数量与 open Position 对齐，新 target 不等于旧值、仍位于 stop 另一侧且按 closed-bar mark 尚未触发。bar/risk terminal 先于 phase-90 Replace，故 Replace bar 原 target Fill 胜且 Decision not-reached。成功后原 target 以 `take-profit-repriced` 取消，stop id/trigger/quantity 不变，新 target 使用 generation-scoped id；former-target touch 不可达。不得多次 reprice、组合其他 mutation、重挂已触发 target 或外推为通用 amend。
- `Independent Lane Batch Plan/Result/Outcome v1` 是 M4-P1 外层编排合同，不改变 Request v36/Result v50/Artifact v52。Plan 至少冻结两个按 `lane_id` canonical 排序、run/symbol 唯一的 child Request/Reservation/Lease hash 与预分配 cash；Runner 必须调用正式 `runReplayTrial`，每个 Lane 独立 cash、isolated margin、Order State、checkpoint 与 Artifact。Result 只做 child evidence sum，并固定声明不是 spendable Portfolio NAV、无 rebalancing/cross-margin/global priority。任一 child 未完成或证据不全时 Outcome failed、Result null、不得发布 partial Batch Result；输入重排不改变 hash。该合同不授权 shared portfolio、跨 Lane matching/netting/liquidation 或隐含 symbol priority，M4-P1 完成不使整体成熟度高于 M3。
- `Shared Initial Capital Batch Result/Outcome v1` 是 M4-P2 外层证据合同，消费 Control Plane `Shared Initial Capital Reservation v1` 和 M4-P1 Plan。Reservation 必须把一个 settlement-asset 初始资金池完整静态分配给当前 child Trial Reservations，并冻结无并列 explicit priority；Replay 按 priority 调用正式 child Runner，但 child evidence 仍按 canonical lane identity 输出。Shared Result 绑定 Reservation/Plan/independent Result hash、唯一 initial cash 与 aggregate ending/net；任一 child 未完成则 Result null。该能力只消除 double-counted initial cash，不授权 runtime shared wallet、cash release/reuse、cross-margin、同刻 Fill 竞争、跨 Lane liquidation 或 concurrent matching，整体成熟度仍为 M3。
- `Runtime Shared Wallet Plan/Result/Outcome v1` 是 M4-P3 的独立 entry-only Portfolio 合同，不修改 M4-P2 或 child Result。Control Plane Runtime Reservation 冻结唯一本金与 priority、禁止静态 Lane allocation；Plan 绑定 child Request/Reservation/Lease identity。Runner 只接收同 settlement asset、isolated、market next-open；Engine 将 earliest bar-open 合并为 `event_time -> authority priority` 全局队列，按 `required_available_cash=isolated_collateral+entry_fee` 顺序准入并在每个 Fill 后提交共享钱包。Result 绑定 queue、Fill、open Position、拒单与 `settled=initial-fee`、`available=settled-reserved` 守恒；输入/Engine failure 必须 Result null。它不认证 exit、funding、liquidation、cash release、cross-margin、完整 Portfolio Artifact 或 Fast parity，整体成熟度仍为 M3。
- `Runtime Shared Wallet Lifecycle Plan/Result/Outcome v1` 是 M4-P4 的新 epoch，不改写 M4-P3 的 entry-only limitation。Control Plane Lifecycle Reservation 冻结 `exit_release_before_entry_admission_then_control_plane_priority`；Plan 在 child identity 上另绑定 nullable full-exit time/Intent hash，且至少一 Lane 有 frozen exit。Engine 把 entry/full-exit bar-open 合入 `event_time -> role(exit=1,entry=2) -> authority priority` 队列；exit 必须一次提交 realized PnL、exit fee 与全部 isolated collateral release，下一 entry 才可读取 available cash。Result 绑定 event cash chain、open/closed Position、entry/exit Fill、拒单、unreached exit 与资金守恒；输入/Engine failure 必须 Result null。该合同只认证 same-settlement、isolated、market next-open、full-position exit，不认证 funding、liquidation、partial、cross-margin、完整 Portfolio Artifact 或 Fast，整体成熟度仍为 M3。
- `Runtime Shared Wallet Funding Plan/Result/Outcome v1` 是 M4-P5 的新 epoch，不改写 M4-P4 的 no-funding limitation。Control Plane Funding Reservation 冻结 exact-event-time/t-minus Position 与 `funding_before_exit_before_entry_then_control_plane_priority`；Plan 对每 Lane 绑定 funding event count/hash、settlement increment 和既有 child/exit identity。Runner 只接受严格递增 UTC exact events 与 `funding_availability=event_time`；Engine 复用 Numeric Policy v3，把 phase-10 signed cashflow 在 phase-20 exit/entry 前提交。Result 绑定完整 funding event set、applied/not-reached、long/short side、钱包链和资金守恒；遗漏、重复、输入或 Engine failure 必须 Result null。该合同不认证 risk/liquidation、partial、cross-margin、borrow、完整 Portfolio Artifact 或 Fast，整体成熟度仍为 M3。
- `Runtime Shared Wallet Risk Plan/Result/Outcome v1` 是 M4-P6 的新 epoch，不改写 M4-P5 的 no-liquidation limitation。Control Plane Risk Reservation 冻结 `funding -> complete exact Mark/risk -> forced full liquidation -> strategy exit -> entry -> priority`；Plan 对每 Lane 绑定完整 Mark/Funding event set、risk/status epoch、Numeric increments 与成本参数。Runner 只接受 `complete_grid`、event-time exact Mark、完整 instrument-status history、unit linear multiplier 和 isolated margin；Engine 先以 Lane-attributed fee/funding、unrealized PnL、maintenance tier 生成 risk observation，再由 exact breach 取得 terminal ownership，以 trigger-Mark adverse slippage 全平并原子提交 realized PnL、trading/liquidation fee 与 collateral release。halted breach、isolated deficit、缺 Mark/epoch、输入或 Engine failure 都不得发布部分 Result。该合同不认证 cross-margin、partial liquidation、borrow、insurance/ADL、交易所真实 liquidation trade、完整 Portfolio Journal/Artifact 或 Fast，整体成熟度仍为 M3。
- `Runtime Shared Wallet Portfolio Evidence/Artifact v1` 是 M4-P7 的只读投影合同，不是新撮合或 Risk Result epoch。Ledger 串联全局队列中的 settled cashflow；Journal/Trial Balance 以 available wallet 与 isolated collateral 为独立资产账户，对平 fee/funding/realized/unrealized/liquidation 与 NAV；Fingerprint 绑定 Control Plane authority、所有 Lane Request/Reservation/Lease、冻结 data/risk/cost source set 及全部派生证据。Artifact 以 11 个 immutable payload 加 manifest-last commit marker 发布；只有完整自洽 manifest 才权威，无 manifest 的中断 payload 不构成部分 Result。该合同不授权 exposure/risk-budget allocation、Review/晋级、策略落盘、cross-margin、partial liquidation、borrow、真实 liquidity 或 Fast，整体成熟度仍为 M3。
- `Portfolio Allocation Plan/Result/Outcome v1` 是 M4-P8 的独立 entry-allocation epoch，不改写 M4-P3–P7。Control Plane Allocation Reservation 冻结 gross、absolute-net、portfolio risk 与逐 Lane risk caps、唯一 priority、计量策略和拒绝优先级；Plan 精确绑定 child Request/Reservation/Lease、market next-open entry、frozen protective stop、collateral、cost 与 Numeric increments。Engine 必须先按同一 execution time 收集完整 candidate set，再依 `lane risk -> cash -> gross -> absolute net -> portfolio risk` 逐项判定，整个 Allocation Cycle 完成后才能产生 phase-20 Fill；不缩量，long/short 可对冲 net 但不能抵消 gross。Risk amount 固定为 entry 至 adverse stop 的价格损失加 entry/stop round-trip fee，不是动态 VaR。Result 绑定 candidate-set、每项 hypothetical/allocated state、拒绝理由、wallet/exposure/risk chain 与 Fill；输入重排不改变证据，priority 改变可确定性改变胜者，失败或篡改不得发布部分 Result。该 epoch 尚未并入 exact funding/risk/liquidation 生命周期与 Portfolio Artifact，也不认证 exit、dynamic exposure、cross-margin、partial fill、borrow、真实 liquidity 或 Fast，整体成熟度仍为 M3。
- `Integrated Portfolio Plan/Result/Outcome/Artifact v1` 是 M4-P9 successor orchestration，不签发新经济 authority。Plan 精确绑定 Control Plane Allocation/Risk Reservations、P8/P6 Plan、唯一共同 initial entry time 和相同 child authority set。P6 entry 只有在逐字段匹配 P8 Allocation Event 时才能 filled/rejected，并继承 Allocation fill hash/reason；统一 state chain 对每个 P6 queue event 串联 wallet、fixed gross/net exposure 与 frozen-stop risk，full strategy exit/liquidation 才释放后两者。集成 Fingerprint/manifest-last Artifact 同时绑定双 Reservation/Result、P7 accounting evidence、state chain 与 limitations。任一阶段失败时 Result/Risk Result/Artifact 均不发布。该合同只认证一个 initial Allocation Cycle 后的生命周期，不开放 re-entry/reallocation、dynamic exposure、partial、cross-margin、borrow、真实 liquidity 或 Fast，整体成熟度仍为 M3。
- `Portfolio Reallocation Plan/Result/Outcome/Artifact v1` 是 M4-P10 的受限 successor，不是一般化循环调度器。它消费 Control Plane `Replay Portfolio Reallocation Reservation v1`，只接受已提交且自哈希有效的前序 Integrated Result/Manifest、零 open Position、零 gross/net/risk 以及实际完成归零的 full exit/liquidation transition；cycle 2 event 必须严格更晚。opening cash 只能是前序 `ending_available_cash`，然后复用 P8 的完整 candidate-set、priority、caps 与 full-fill-or-reject 语义。七个 payload 后最后写 reallocation manifest；失败不得发布 Result/Allocation Result/Manifest。该合同只认证第二次 Allocation，未认证其后 exact-risk lifecycle、cycle 3、partial-close reallocation、cross-margin、borrow、真实 liquidity 或 Fast，整体成熟度仍为 M3。
- `Two-Cycle Portfolio Plan/Result/Outcome/Artifact v1` 是 M4-P11 的无新 authority successor。它把 M4-P9 cycle-1、M4-P10 cycle-2 Allocation 与既有 P6 Risk Reservation/Result 闭成一条严格时序、wallet 连续的 state chain；cycle-2 entry 必须逐项继承 P10 Allocation fill/rejection，不得由 cash-only admission 复活。Fingerprint/十四个 payload 绑定两周期 Result/Manifest、cycle-2 双 Plan/authority/Result、P7 evidence 与统一 chain，manifest 最后提交。任一 input/Risk/Artifact failure 均不发布 Two-Cycle Result。该合同只认证恰好两个周期，不是 bounded-N scheduler、cycle 3 或通用 matching，整体成熟度仍为 M3。
- An observed open-price jump between contiguous bars is a market gap and retains `worse_open`. A missing interval-grid bar is unavailable evidence, not a price observation: missing frozen entry fails before Fill; a reached post-entry gap fails at the prior observed close before any later SourceEvent or checkpoint. `ReplayDataGapFailureEvidence` binds kind, exact bounds, interval, count, and `fail_before_unobserved_interval_effects`. Run Outcome v35 publishes this non-retryable `data_integrity` failure without Result/Artifact. A terminal consumed prefix ending before a future gap remains unchanged.
- A no-bar interval is exempt from that failure only when the complete PIT status schedule proves the entire half-open interval halted and the next observed open is already `trading`. Every decision/effective execution boundary and every supplied bar must resolve wholly to `trading`. During halt, active protection remains pending and Funding/Mark may still mutate ledger/risk; strategy/market fills are forbidden. An exact maintenance breach while halted is typed failure with a non-authoritative breach observation, never a synthetic liquidation Fill or partial Result.
- Per-fill fee/slippage and exact timestamp funding events.
- Every Fill carries the exact filled OrderEvent key; fee/realized-PnL ledger facts reuse the causal Fill key, funding reuses its SourceEvent key, and initial/ending snapshots use explicit phase-`70`/`100` checkpoint keys.
- Every certified Fill produces a post-fill Position Projection with the same EventKey. The Runner now permits one fixed-quantity partial reduce that leaves the Position open; its fee/realized PnL enter the causal ledger, later Funding and Margin use remaining quantity, and the next State Snapshot exposes the resized Position/protection. This does not certify exchange-liquidity partials, multiple partials, add/reversal, or wider matching.
- Dataset Manifest v11 binds the full-window linear-derivative accounting spec, its PIT provenance/status schedules, supplemental/capacity declaration, and an explicit Mark capability. `mark_coverage=none` requires no Mark data; `complete_grid` requires exact event-time availability, strict timestamp/source-sequence order, full endpoint/grid/count coverage, tick alignment, and content-hash binding. Partial or lagged Mark streams are rejected rather than treated as exact.
- Result v50 binds Margin v7、Journal v5、Equity v3、typed entry outcome、ordered pending-order resolutions、Order State Snapshot v1、Decision Timeline v13 and nullable liquidity-capacity-attestation hash. Request v36 freezes exact-risk liquidation、GTC/IOC/GTD Limit、GTC/GTD Stop-market、optional contract-owned pending-entry Cancel、one bounded initial-protection sibling Cancel or one bounded initial-target Replace；GTC data-boundary、IOC/GTD expiry and proven-nonfill Cancel are completed zero-execution Results, while Limit touch-before-cancel/expiry、Stop same-bar post-trigger path、OHLCV/halted maintenance breach remain Run Outcome v35 failures without Result.
- `rd-replay-number-v3` converts admitted finite number values into decimal coefficients, performs bps adjustment, notional fee, funding, linear PnL, weighted-average and return division as BigInt rationals, then quantizes once: quantity floors to step; buy execution price ceils and sell price floors to tick; fee expense ceils; signed funding/realized cashflow floors toward negative infinity; weighted average and return use half-away-from-zero at 12 decimal places. Stop/target, OHLC, initial cash, Fill, Position, Ledger and Journal facts must align. Shared language-neutral vectors are independently certified by Bun/BigInt and Python Decimal; this proves arithmetic-policy parity, not arbitrary JSON parser or unbounded-decimal transport equivalence.
- Deterministic Result Artifact and complete identity fingerprint, including numeric, journal, equity, and margin policy hashes/versions.
- Manifest-bound content hash, RFC 3339 UTC timestamps, explicit instrument lifecycle, universe survivorship, observed-through and bar/funding/mark availability policy.

At EOD, Simulator v22 marks an open Position without synthetic Fill；a still-pending GTC Limit or Stop-market publishes an authoritative zero-execution Result while preserving the active Order. An unfilled IOC publishes its TIF-owned expiry at the sole first-open observation；an unfilled/untriggered GTD Limit or Stop-market publishes expiry only at its frozen executable bar close after range resolution；a contract-cancelled GTC binds the exact post-range Cancel key、closed-bar valuation source and optional evaluated Harness receipt. A previously cancelled protection sibling remains cancelled while EOD cancels only the still-active sibling. None invents an external cancellation or settlement. Generic runtime cancel/amend/cancel-replace、IOC partial remainder、FOK、arbitrary intrabar GTD、stop loosening、multiple partials、真实 queue/depth partial、multi-entry/reversal、halt settlement、partial liquidation、cross/shared margin and fast mode remain unsupported.

## Certified fixed-quantity partial reduce

`trade.rd-replay-partial-reduce-intent.v1` freezes one opposite-side market reduce-only fixed quantity, strictly smaller than executable entry quantity and increment-aligned. At its strictly later eligible open, risk/stop/target retain priority. If not preempted, Simulator fills the partial Order at phase `20`, then at phase `90` cancels both current protection Orders and submits/activates replacements at unchanged trigger prices for exactly `abs(post-fill position)`, with no intervening SourceEvent. A terminal owner before execution cancels the pending partial Order. Schedule v5 rejects a second partial or any protective-stop replacement in the same run, but permits one later final full exit. Checkpoint v17 preserves pending/filled partial Order、partial Fill、rebuilt bracket generation 与 consumed halt/resume prefix，并以冻结 Intent、最后 OrderEvent 与确定 protection identity/trigger/quantity 重验其语义；clean/resume parity is mandatory.

The certified Manifest can prove frozen halt/resume history but still does not prove a delisting settlement price. An open position reaching delisting therefore produces a non-retryable typed failure with the terminal EventKey and publishes no partial Result; the engine never invents a terminal fill.
