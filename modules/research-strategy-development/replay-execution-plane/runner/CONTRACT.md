# Replay Trial Runner

Owns Trial-scoped Replay orchestration, Attempt authority validation, attempt-local idempotency, cooperative cancellation, resumable checkpoint handoff, typed failure, and durable Result Artifact commit.

Artifact Manifest v45 is the success commit marker. Runner 对 Result v43 的 OHLCV、pending-order、entry outcome、Decision Timeline v10 与 liquidity-capacity authority 在首次发布和幂等读取时重验。pre-entry Limit GTC/IOC 与 Stop-market GTC 必须有 PIT 可用且 Request/Reservation/Dataset 一致的 Capacity Attestation。filled 分支必须 terminal full Fill、Fill-after-source EventKey；Limit price 不劣于 limit，Stop 必须绑定 trigger/source/reference 与 `triggered → filled` OrderEvents。`unfilled_at_data_end` 必须全链 resting、Order active；`expired_unfilled` 只属于 Limit earliest-open IOC；`cancelled_unfilled` 必须绑定 matching contract intent、指定 `bar_range`、phase-`90` Cancel EventKey 与 cancelled OrderEvent。若 Schedule v7 含 `authorized_entry_cancel`，Runner 将其置为 pending runtime；effective range 到达后 Harness 必须输出相同 Intent，State Snapshot 为 null；更早 Fill 则 not-reached。Limit touch-before-cancel 与 Stop same-bar post-trigger protection path ambiguity 均为 deterministic-engine typed failure，无 Result/Artifact；后者额外携带 self-hashed path evidence。Run Outcome v35 completed 可携带四类权威 Result/Artifact。

Step Engine may emit Checkpoint v22 only after a complete source-event boundary. Besides existing authority/state it stores active Limit/Stop pending entry、ordered resolution prefix、Decision Timeline v10 and nullable entry transition；resume must not replay a consumed observation、Harness decision、trigger、Fill、expiry or contract Cancel。Runner durably publishes the Attempt/lease-generation/source-offset-bound payload and Diagnostic Checkpoint Commit v2；clean/resume Result hash 必须一致。SQLite reference execution cancellation 已认证；它与订单 Cancel 是不同 authority，remote transport 与 stop-latency SLA 尚未认证。

`replay-cancellation-coordinator` owns the transport-neutral Experiment Runner adapter: every complete boundary polls an injected authority port with the current lease, delegates ordinary renewal when no cancellation exists, and acknowledges a receipt-bound Observation after Run Outcome. The adapter returns Coordination Result v1 with poll count and typed acknowledgement status; acknowledgement failure preserves the exact cancelled Replay outcome、poll count、attempted registration time and original cause instead of pretending registration succeeded. `acknowledgeReplayCancellationOutcome` retries only that validated immutable Observation and never polls or reruns Replay; non-authoritative outcomes are rejected. That helper alone is only an in-process retry seam; durable recovery requires the outbox below. The SQLite implementation remains Control Plane-owned. Authority cancellation publishes no resumable checkpoint/diagnostic commit and removes attempt-local diagnostic files; this is not generic artifact-directory GC. Push/poll/lease-response transport and latency SLA remain deployment decisions.

`replay-cancellation-outbox` adds the certified local durable seam. Durable coordination uses the Runner-owned canonical cancellation-outcome constructor after validating the directive and current lease, then commits one fixed-name, self-hashed Outbox Record v2 through the certified Attempt-local Artifact Store **before returning `cancel` from the Engine source boundary** and before calling `acknowledge`; persisted bytes must equal the later Runner terminal outcome. V2 additionally persists the exact cancellation-generation lease and logical idempotency-key hash, so the record can reconstruct and hash-check its own Attempt namespace without Request/Reservation replay. Persistence failure is retained while the authority cancellation still terminates Engine, then raised as a typed error with the exact cancelled outcome and suppresses ack. Invalid/expired directives never reach the outbox. A restarted coordinator reloads and revalidates the record, then retries only Observation registration；it never polls or reruns Replay. Identical persistence preserves the first `persisted_at`; competing content and byte/hash tamper are rejected. Durable Coordination Result v1 carries the operational outbox commit, but neither record nor commit is a Replay Result/Artifact Manifest、Checkpoint Receipt or Resume authority. The record remains after successful ack so post-commit crash windows collapse to idempotent redelivery；poll 返回至 local commit 仍非跨存储事务，retention/GC、remote store adapter and transport remain undecided.

Durable coordinator admission remains recovery-first for a known invocation. In addition, the certified local store exposes deterministic, read-only two-level Attempt namespace discovery. `recoverDiscoveredReplayCancellationAcknowledgements` first scans and validates the entire set, requires every v2 record to map back to the namespace derived from its idempotency-key/Attempt hashes, then asks Control Plane whether the exact Observation is pending or already registered. Pending records are acknowledged without poll/Runner/Engine；already-registered records are reported without redelivery. Discovery Recovery Result v2 exposes only portable namespace/record/Observation hashes and Attempt generation；it never returns machine-local namespace or outbox refs. Any malformed tree、symlink、legacy v1 record、misplaced namespace、lease/outcome drift or authority hash conflict fails before delivery. V1 remains recoverable only through the explicitly bound legacy helper；there is no heuristic migration. Discovery is local-filesystem-only and does not define remote listing、retention/GC、quarantine or startup SLA.

The outbox namespace is Attempt-scoped, not initial-generation-scoped. Its constructor lease freezes the Attempt identity and a minimum generation; every persist additionally supplies the exact current lease snapshot. A later cancellation may therefore follow monotonic renewals in the same Attempt only when trial/run/request/reservation、attempt id/ordinal/worker remain unchanged, current generation is not below the constructor floor, and the prepared outcome lease hash/generation exactly match that current snapshot. V2 stores that cancellation-generation snapshot；discovery rehydrates it from immutable bytes and cross-checks Observation/outcome/hash before consulting Control Plane. The stale initial generation still fails the known-invocation path；generation rollback、worker replacement or cross-Attempt reuse remain invalid.

If the simulated full close leaves negative isolated collateral, Runner returns typed `liquidation-deficit-unsupported` with the breached snapshot, trigger observation, and `remaining_collateral`; it publishes neither Result nor Artifact and never invents insurance-fund, bankruptcy-price, or ADL facts.

An open position reaching manifest-bound delisting returns `instrument-delisted-with-open-position`, carries the phase-`00` terminal EventKey, is non-retryable, and commits no partial Result. A settlement price requires a separately frozen data contract; Runner does not synthesize one.

`dataset-grid-gap-in-execution-window` is a non-retryable `data_integrity` failure. Its self-validating evidence distinguishes `missing_earliest_executable_bar` from `open_position_grid_gap` and binds exact missing bounds/count. Runner publishes neither Result nor Artifact, whether the failure is raised by admission or source reduction. Retry is meaningful only after Control Plane freezes a different Dataset Manifest/Data Hash; it cannot reinterpret the same immutable input.

`maintenance-margin-breach-while-halted` is a non-retryable `deterministic_engine` failure. Runner preserves exact risk evidence but publishes no Result/Artifact. Resume uses Checkpoint v22 source prefix, so it cannot skip the halt、replay Funding or replay a pending-entry observation.

R4.104 adds **Runner-owned Decision Worker Input Assembly v4** as a pre-execution code-admission seam, not a Trial Runner capability. It embeds the complete R4.103 v3、matching full R4.96 Context Binding、Harness Source Bundle v1 and Build Attestation v2；the builder uses the existing deterministic Bun build path and requires byte-identical rebuilt attestation/artifact, so a merely self-hashed forged artifact is rejected. The published object still declares registry admission and invocation identity unbound, Worker Request/Harness invocation forbidden, and no Decision/Signal/Order/economic authority. Independent validation requires repeating the deterministic build；the object does not prove signed provenance、SBOM、OS sandbox or remote runtime compatibility.

R4.105 adds **Runner-owned Decision Harness Code Admission v1** without integrating Trial Runner execution. The builder validates the fixed process-memory Registry Capability, resolves exactly the v4 `source_bundle_hash`, requires the returned Source Bundle/Build Attestation to equal v4 code evidence, then publishes a deterministic receipt containing the full capability、registration and parent v4. The receipt deliberately records `registry_instance_id=null`、unavailable instance identity、`future_lookup_guarantee=not_proven` and unsigned process-local authenticity. A custom object can implement the same interface, so this is admission-time compatibility evidence—not durable/signed registry provenance, remote availability, invocation identity, Worker Request, Harness execution or any Trial/economic authority.

R4.106 adds **Runner-owned Decision Harness Invocation Identity Set v1** without creating a Worker Request. The builder consumes one complete R4.105 receipt and derives a schedule-ordered logical identity per v3/v2 tuple from the exact existing Worker v9 fields；the live Worker path calls the same pure derivation helper, eliminating formula duplication. The reproducibility pair intentionally shares one logical ID. PID、process instance、execution attempt and retry identities are not materialized；Context remains parent-bound but is not a direct member of the legacy v9 hash. The Set keeps Worker Request null、Harness invocation forbidden and grants no Trial/Decision/Signal/Order/economic authority.

R4.107 adds **Runner-owned Logical Request Identity Upgrade v1** without activating Worker v10. The builder projects each R4.106 entry into Identity Policy v2：target protocol/schema、Context、Code Admission、bundle/artifact and phase-required snapshot hashes are direct formula members；the v9 ID is migration metadata only. Attempt lease/ordinal/worker are excluded from logical identity so renewal and retry cannot perturb the same frozen request；they belong to a later execution envelope. The upgrade remains pre-worker evidence with null Request、no process/attempt identity、forbidden Harness invocation and no Trial or economic authority.

R4.108 adds **Runner-owned Worker Request v10 Materialization v1** without changing the live v9 Worker path. The builder projects every R4.107 identity onto the exact complete v2/v3 input entities and produces a field-whitelisted、self-hashed Request whose content hashes and logical ID are independently recomputed. Attempt/process/lease are deliberately absent；execution admission is not granted、envelope is null、transport is not invoked. Response、execution envelope、Worker/transport certification and all Harness/Trial/economic authority remain future gates.

R4.109 adds **Runner-owned Worker Response v10 Contract v1** without producing a Response. The builder embeds R4.108 and freezes the response field whitelist、ten-field Request echo、typed DecisionOutput/trace hashes and Response self-hash. The descriptor carries zero instances；even a schema-valid candidate is only an unadmitted Worker claim until execution-envelope、reproducibility、Schedule and Harness Receipt admission. Transport/Harness remain forbidden and the v9 Response/Receipt path is unchanged.

R4.110 adds **Runner-owned Attempt-bound Execution Envelope v1** without dispatch. The builder selects one R4.109 logical Request, binds both Replay Execution Request and Worker Request hashes to an exact Control Plane Attempt Lease, and distinguishes worker authority from OS process identity. Renewal requires immutable Attempt fields plus a higher generation and non-regressing heartbeat, producing a predecessor-linked successor；retry under a new Attempt produces a new root with the same logical ID. Lease freshness at dispatch、transport、Response/process receipts and all execution/economic authority remain future gates.

R4.111 adds **Runner-owned Dispatch Lease Admission v1** without dispatch. The builder compares an R4.110 Envelope with the Control Plane current Attempt Lease at one `observed_at`, requires exact Attempt/worker/generation/hash equality, and applies the half-open `[heartbeat_at, lease_expires_at)` window. A renewal invalidates the old Envelope for admission even before its old expiry；a retry requires a new root. This is freshness-only evidence from a Control Plane observation, not independent clock proof or process/transport/Response authority.

R4.112 adds **Runner-owned Dispatch Lease Authority Binding v1** without dispatch. Runner validates one official Control Plane Current Attempt Lease Observation Receipt, rebuilds R4.111 Admission from its Lease/time, then exact-binds both immutable parents. A renewed receipt fails against the old Envelope. R4.113 makes that Control Plane receipt durable and restart-readable without changing the Replay wire object；Runner still does not query Control Plane SQLite directly, so the object alone does not attest registry-read provenance. External clock attestation、process/transport/Response remain future gates.

R4.114 adds **Runner-owned local Dispatch Evidence Registry v1** without dispatch. It persists the complete Envelope→Admission→Authority Binding as canonical JSON under an Attempt/generation/logical-Request natural key using staged-file fsync、hard-link create-if-absent and directory fsync. Identical retries preserve the first registration；competing authority、non-canonical bytes and contract tamper fail closed；stateless reopen reads and revalidates the record. The registry returns no machine-local ref and grants no one-time dispatch claim、process/transport/Response or economic authority.

R4.115 adds **Runner-owned Local Dispatch Claim Registry v1** without process launch. Claim creation re-reads the exact durable Registration, requires a strictly later self-hash-verified Observation of the unchanged Lease and claims before expiry. One natural key admits only the first opaque claimant while the CAS file is preserved；identical retry is idempotent，competition、renewal drift、orphan/tampered evidence fail closed. Claim-before-dispatch crash can lose delivery and no reassignment is defined；claimant identity is not OS-attested and no dispatch occurrence、transport、Response or economic authority exists.

R4.116 adds **Runner-owned Process Launch Registry v1** without Worker Request dispatch. Runner re-reads the exact Claim and a newer same-Lease Observation, then commits one immutable Launch Attempt before spawning. The sole CAS creator materializes and hashes the embedded artifact, exact-matches the current Bun runtime, and starts it under fixed argv/env with zero stdin request bytes followed by EOF. Receipt records only local child/PID context、typed start/pre-start failure、exit/signal and output hashes；completed retry never respawns, while Attempt-without-Receipt is indeterminate and blocks automatic relaunch. PID reuse、Runner clock、local CAS and launch-invocation time are explicit limits；no transport admission、Harness invocation、Response or economic authority exists.

R4.117 adds **Runner-owned Transport Activation Registry v1** without frame construction or write. It re-reads the exact durable R4.116 Receipt and persists a create-or-identical blocked gate whose complete blocker set proves: attested artifact protocol is v9 while target Request is v10；Request execution/transport fields remain closed；the completed EOF probe is not reusable as a live worker. The registry rejects missing parents、blocker drift and canonical-byte tamper. Compatibility projection、frame、write receipt、dispatch、Harness、Response and economic authority remain absent；new capability/process evidence must use a new gate key.

R4.118 adds **Runner-owned Worker v10 Build Capability Registry v1** without changing the live v9 path or activating transport. The builder consumes the exact R4.105 Code Admission, writes the admitted Source Bundle plus one generated v10 decoder entrypoint into an isolated build root, requires exact Bun metafile closure and no residual imports, and attests a byte-distinct ESM artifact under the current Bun executable hash. Its sole exported API decodes one in-memory Request object and rejects field、protocol/schema or closed-marker drift；it has no stdin loop and never invokes the linked Harness entrypoint. Registration uses canonical local CAS、create-or-identical and restart validation；a merely self-hashed alternative artifact loses against deterministic rebuild. Process launch、frame、Request write/decode occurrence、dispatch、Response admission、Schedule/Harness Receipt and every economic authority remain absent, so R4.117 stays blocked until new process evidence exists.

R4.119 adds **Runner-owned Worker v10 Single-request Transport Contract Registry v1** without spawning or framing. It consumes the exact durable R4.118 Capability and R4.114 Execution Envelope lineage, freezes only the certified fresh-process lifecycle and canonical JSON UTF-8 LF Request/Response frames, and rejects pool、keepalive、multiplex or trailing bytes. The contract separates the inner Request's legacy v9 Code Admission artifact anchor from the outer frame's R4.118 v10 process artifact, requiring an exact lineage bridge explicitly scoped as migration-only. Registry create-or-identical/restart reads revalidate parents、canonical bytes and the bridge；frame candidates can be validated in memory while persisted process/frame/write/read/dispatch instance counts remain zero. No stdio worker、Response admission、Harness invocation or economic authority exists, and R4.117 remains an immutable blocked historical decision.

R4.120 adds **Runner-owned Worker v10 Stdio Capability and Negative Probe Registries v1** without valid Request dispatch. The deterministic builder consumes the exact durable R4.119 Contract, materializes its Source Bundle and R4.118 decoder plus one generated stdio entrypoint, requires exact Bun metafile closure/no residual imports, and attests a successor artifact distinct from decoder and legacy v9. Because R4.119 still binds the decoder artifact, the process rejects every parseable input before decode pending a successor contract. The probe registry runs five fresh local processes against only empty or structurally invalid/oversized bytes and persists exact input、PID-context、exit、stdout/stderr hashes；existing receipt retries do not respawn and tampered parents/bytes fail closed. These are diagnostic process observations, not Worker Request frames or write receipts；decode、dispatch、Response、Harness and all economic authority remain absent, and R4.117/R4.119 are not rewritten.

R4.121 adds **Runner-owned Worker v10 Successor Transport Contract Registry v2** without process admission or framing. The builder consumes the exact durable R4.120 Negative Probe Receipt closure, binds its stdio artifact as successor outer process identity, preserves the v9 logical anchor and decoder predecessor, and recomputes a deterministic nine-item blocker set. Registry create-or-identical/restart reads reject missing/tampered parent、artifact substitution、blocker drift、instance escalation and non-canonical bytes. Request v10 remains intrinsically closed, no current-Lease successor observation or Attempt-bound stdio launch exists, and every frame/write/decode/Response count remains zero；R4.117 and R4.119 stay immutable historical evidence and no Harness or economic authority is granted.

R4.122 adds **Runner-owned Worker v10 Execution Admission Contract Registry v1** without issuing an admission. The builder consumes the exact durable R4.121 Contract and freezes authority separation: Request v10 remains immutable non-executable payload；a future Attempt/Lease-generation-bound Execution Admission Command, not a Request marker mutation or authority-only Request v11, is the executable object. Command identity must bind Request/logical id、Attempt/worker/generation、durable Claim、issuance-time current-Lease observation、stdio artifact and Transport Contract；renewal/retry require a new command and Response must echo command/request hashes. Registry create-or-identical/restart reads reject missing/tampered parent、model drift、instance escalation and non-canonical bytes. Current command/process/frame/Response counts remain zero with nine explicit pre-issue blockers, so no dispatch、Harness or economic authority exists.

R4.123 adds **Runner-owned Worker v10 Execution Admission Pre-issue Registry v1** without command issuance. Builder closure-checks the exact R4.122 Contract against one durable local Dispatch Claim and a strictly later same-generation Control Plane Lease observation, including Request/Envelope/Attempt、successor stdio artifact and Transport Contract bindings. Registry re-reads both durable parents and uses canonical CAS create-or-identical/restart validation；claim-time observation、renewed generation、cross-lineage substitution、instance escalation and byte tamper fail closed. The bundle closes only the missing Claim and current-Lease evidence blockers；Control Plane registry-read provenance and independent clock remain absent, seven blockers remain, and command/process/frame/Response/dispatch/Harness/economic authority stay zero.

R4.124 adds **Runner-owned Worker v10 Execution Admission Registry Provenance Registry v1** without command issuance. Runner consumes the Control Plane receipt as a validated wire view, exact-binds its immutable registry row/current active Attempt evidence to the durable R4.123 bundle, and persists the result through canonical CAS create-or-identical/restart validation. Runner never reads Control Plane SQLite and does not manufacture Control Plane authority. Registry provenance is closed；caller-supplied UTC still lacks external attestation, six blockers remain, and every command/process/frame/Response/dispatch/Harness/economic instance stays zero.

R4.125 adds **Runner-owned Worker v10 Execution Admission Clock Attestation Registry v1** without command issuance. Runner validates the Control Plane dual wall/monotonic sample wire view, requires its embedded registry receipt to equal durable R4.124 provenance, and persists an exact canonical CAS binding. The caller cannot inject dispatch time into the Control Plane producer；however the source remains a local process clock, not signed TSA/NTP/remote trusted time. Five blockers remain and command/process/frame/Response/dispatch/Harness/economic instances stay zero.

R4.126 adds **Runner-owned Worker v10 Execution Admission Command Registry v1** without process launch. Runner requires exact durable R4.125 evidence and issues one canonical command per Request/Attempt/worker/Lease-generation natural key. Command time is the Control Plane clock-attestation completion, not registry commit time；a different valid clock parent for the same natural key fails closed. Worker Request v10 markers remain immutable, while the command grants only exact process-launch-intent creation authority. Command count is one；process intent/receipt、frame、Response、dispatch、Harness and economic counts remain zero with four blockers.

R4.127 adds **Runner-owned Worker v10 Process Launch Intent Registry v1** without spawning. Runner reads the exact immutable Command entry, binds one strictly post-command Control Plane clock-bracketed registry/current-Attempt receipt, and commits one canonical intent per Command. The intent freezes successor stdio artifact/runtime/argv/environment/cwd/resource bounds；alternate valid revalidation evidence collides instead of replacing it. It cannot bridge the observation-to-spawn race: a fresh spawn-boundary expiry/cancellation/fencing check remains mandatory. Command/intent counts are one, process/frame/Response/dispatch/Harness/economic counts are zero with three blockers, and legacy v9 probe evidence remains separate.

R4.128 adds **Runner-owned Worker v10 Process Launch Readiness Gate Registry v1** and still does not spawn. Runner reads the exact immutable Intent entry and persists a deterministic deny decision: its exact R4.120 artifact exits 70 before decoder for every parseable frame, while Request/Response Frame v1 cannot bind or echo the Execution Admission Command. Starting it would create another terminal probe, not a dispatch process. The required cutover is new activated artifact、authority frames、Transport、Command and Intent versions with no predecessor rewrite. Command/intent/gate counts are one；process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.129 adds **Runner-owned Worker v10 Authority Frame Build Contract Registry v1** and still does not build or spawn. Runner requires the exact immutable R4.128 Gate, persists one canonical contract, and validates Request/Response Frame v2 candidates without registering frame instances. Frame v2 closes the protocol shape gap by binding Command + Intent on input and echoing Command + Intent + exact Request-frame on output；inner v10 payload/result semantics remain unchanged. Any missing parent、echo drift、field drift、non-canonical persistence or tampering fails closed. Activated artifact、successor Transport/Command/Intent、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.130 adds **Runner-owned Worker v10 Activated Stdio Capability Registry v1** without successor authority or dispatch. Runner requires exact durable R4.129, rebuilds an exact source-bundle + decoder + generated authority-stdio closure, rejects residual imports, and persists one canonical byte-attested artifact capability. The artifact requires a canonical spawn authority capsule before stdin, compares every Frame v2 authority field against that external expectation, then may decode v10 and invoke Harness；missing/malformed capsule probes terminate at 71/72 and never form Request frames. Valid-authority probing is deferred until real successor Transport/Command/Intent exist. Activated artifact count is one；capsule/process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.131 adds **Runner-owned Worker v10 Authority Transport Contract Registry v3** without Command issuance or dispatch. Runner requires the exact durable R4.130 Capability by natural key/hash, closes its artifact and Frame v2 policy over the immutable old Envelope/Request lineage, and persists one canonical CAS Transport. It freezes launcher-side capsule derivation after future Command/Intent commits, fixed environment/no reuse, exact Response echo, and Process Receipt bindings；the Intent never embeds its own hash. Missing/tampered parent、artifact regression、field/count escalation、non-canonical bytes and competing evidence fail closed. Authority Transport count is one；new Command/Intent、capsule、spawn revalidation、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.132 adds **Runner-owned Worker v10 Authority Execution Admission Command Registry v2** without Intent creation or spawn. Runner requires exact durable Authority Transport v3 and validates a fresh Control Plane clock-bracketed current-Attempt read that starts after predecessor Intent issuance and still matches Request、Attempt、worker、Lease generation/hash. The canonical CAS natural key permits one Command per Transport/Request/Attempt/worker/generation；alternate valid clock evidence、old clock reuse、artifact/Lease drift、missing parent、non-canonical bytes and tamper fail closed. Command hash is a post-commit capsule input, never a self-reference in payload. New Command count is one；new Intent、capsule、spawn revalidation、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.133 adds **Runner-owned Worker v10 Authority Process Launch Intent Registry v2** without capsule materialization or spawn. Runner requires the exact durable Authority Command v2 and a fresh Control Plane clock-bracketed current-Attempt read whose complete interval begins after Command issuance, then exact-binds Authority Transport/artifact、Bun executable、fresh-process argv/private cwd、`0500` materialization、fixed environment、Frame v2/resource bounds and capsule derivation policy. The canonical CAS natural key permits one Intent per Command；alternate valid clock evidence、stale reads、artifact/Lease drift、missing parent、non-canonical bytes and tamper fail closed. The final Intent hash is a post-commit capsule input and never a self-reference in payload. New Intent count is one；capsule、spawn revalidation、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.134 adds **Runner-owned Worker v10 Authority Capsule Registry v1** without revalidation or spawn. Runner requires the exact durable Authority Intent v2, deterministically derives the seven-field canonical environment value from committed Transport/Command/Intent lineage, freezes its exact text and hash, and persists one create-or-identical outer record per Intent. Registry metadata、record hash、caller fields and local commit time cannot enter the artifact-visible capsule. Missing/tampered parent、field injection、hash drift、non-canonical bytes and persisted tamper fail closed. Capsule count is one；spawn revalidation、process receipt、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.135 adds **Runner-owned Worker v10 Authority Spawn Boundary Revalidation Request/Binding Registries v1** without spawn. Runner first requires the exact durable capsule, persists one capsule-bound Control Plane Request, then accepts a Receipt whose internal clock bracket directly revalidates the exact current Attempt/worker/Lease generation/hash/expiry. The binding natural key is the Request hash；a second valid Receipt、missing/tampered parent、Lease drift、field injection、non-canonical bytes and persisted tamper fail closed. The Receipt does not erase the read-to-kernel-start race. Request/Receipt/binding counts are one；process receipt、process/frame/Response/dispatch/Harness/economic counts remain zero.

R4.136 adds **Runner-owned Worker v10 Authority Process Launch Registry v1** and starts one fresh child without writing a frame. Runner re-reads exact durable Spawn Revalidation and Capsule, commits one non-replayable Attempt slot, verifies current Bun plus the activated artifact, then spawns with a private cwd、`0500` artifact、fixed no-inheritance environment、exact capsule and three open pipes. The success Receipt records full lineage、PID、local spawn observation、artifact/argv/cwd/environment hashes and process instance id；stdin stays open with zero bytes. Only the creator receives the ephemeral live session；Receipt retries do not read a clock or relaunch and cannot recover that handle. Failed/orphan attempts require manual recovery/fresh authority. Process Attempt/Receipt/live-instance counts are one；frame/Response/dispatch/Harness/economic counts remain zero.
