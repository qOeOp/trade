# Replay Accounting

Owns deterministic monetary primitives and the single-position evidence ledger used by certified Replay capabilities.

It may calculate adverse slippage, execution charges, exact-event funding cashflows and append-only ledger balances. It does not match orders, load market data, derive review decisions, model unsupported margin/liquidation, or write Control Plane state.
