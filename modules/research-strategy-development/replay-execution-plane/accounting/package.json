{
  "name": "rd-replay-accounting",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "bun run typecheck",
    "test": "bun test ./src/lib/*.test.ts",
    "check": "bun run lint && bun run test"
  }
}
