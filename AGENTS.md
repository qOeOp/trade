当前阶段只做一件事：

- 围绕 `docs/vision.md` 迭代 product vision

## Canonical Starting Point

- 先读 [docs/vision.md]
- `docs/vision.md` 是当前唯一优先参考的产品方向文档
- `README.md` 只作为仓库入口与摘要，不作为当前阶段的 vision 主文档
- 如果其他想法和 `docs/vision.md` 还没对齐，不要提前把它们写成固定制度

## Current Rule

- 不预先设计我们还没决定的结构
- 不预先固定 skill 数量、skill 职责、记录模型、策略流程
- 不把临时想法写成长期 memory

## Path Convention

- 路径和文件名使用 ASCII、小写、短横线
- 正文内容优先中文
- 时间默认使用 `Asia/Shanghai`
