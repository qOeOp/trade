interface:
  display_name: "Run Bounded Mission"
  short_description: "Run a bounded implementation or delivery mission"
  default_prompt: "Use $run-bounded-mission to frame, plan, execute, verify, and finalize this implementation or delivery outcome."
policy:
  allow_implicit_invocation: true
