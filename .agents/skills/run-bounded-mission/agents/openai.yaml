interface:
  display_name: "Run Bounded Mission"
  short_description: "Plan together, build once, and terminate cleanly"
  default_prompt: "Use $run-bounded-mission to plan this with me before building."
