interface:
  display_name: "Run Bounded Mission"
  short_description: "Run every turn through the complete mission lifecycle"
  default_prompt: "Use $run-bounded-mission for this entire turn. Emit the ordered Mission-Start, Contract, Plan, Build, Evaluate, Handoff, and Mission-Terminate receipts; use noop for stages without work instead of skipping them."
