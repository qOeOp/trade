# Frame Projection

Copy this shape into conversation only when `SKILL.md` activates it. Keep the raw user request and
repository or user authority canonical. This projection decorates that evidence; it does not
authorize effects, resolve user-owned choices, become lifecycle state or durable identity, or gain
an ID or hash.

Use ordinary prose when it is clearer. Keep only populated, decision-changing lines; combine fields
when that is shorter and omit empty boilerplate.

```text
Frame projection
Outcome / consumer: <observable result and who consumes it>
Included / excluded: <bounded scope and explicit non-goals>
Authority / effects: <canonical authority locator and permitted or prohibited effects>
Acceptance: <falsifiable evidence, including preserved unavailable proof>
Origin / Stop: <current origin and finite evidence-backed stopping predicates>
```

When a material new instruction arrives, replace only the affected projection fields and follow the
main lifecycle route. Invalidate every dependent Plan field, slice, or packet until it is reconciled;
never silently merge superseded intent into the projection.
