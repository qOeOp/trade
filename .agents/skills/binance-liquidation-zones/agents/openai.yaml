interface:
  display_name: "Binance Liquidation Zones"
  short_description: "在 OBSERVE 阶段推断 Binance USDM liquidation-like zones"
  default_prompt: "当前若需要补 liquidation-like 观察证据，就先协调上游只读 skill 准备 Binance USDM 的 aggTrades 与 symbol snapshot，再使用 $binance-liquidation-zones 推断最近可能存在的 liquidation cluster，并明确这只是公开数据推断。"
