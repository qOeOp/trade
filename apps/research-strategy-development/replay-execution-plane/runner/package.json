{
  "name": "rd-replay-trial-runner",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "test:release": "bun run lint && bun run test",
    "typecheck": "tsc --noEmit",
    "lint": "bun run typecheck",
    "test": "sh ../../../../scripts/run-exclusive-test.sh replay-runner-heavyweight env REPLAY_TEST_PROFILE=1 bun test ./src",
    "check": "bun run lint && bun run test"
  }
}
