# Execution Capability Contract

Defines the cross-domain validation surface for `trade.execution.capability.v1`. Live execution creates the capability; exchange gateway only validates exact action, idempotency, intent, scope, source refs, and expiry before dispatch.
