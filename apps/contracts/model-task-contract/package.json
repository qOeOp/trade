{
  "name": "model-task-contract",
  "private": true,
  "type": "module",
  "scripts": { "typecheck": "tsc --noEmit", "test": "bun test ./src/*.test.ts", "check": "bun run typecheck && bun run test" }
}
