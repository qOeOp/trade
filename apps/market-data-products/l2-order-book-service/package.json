{
  "name": "@trade/l2-order-book-service-control",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "bun run typecheck",
    "test": "bun test ./src/control/*.test.ts ./src/scripts/*.test.ts",
    "check": "bun run lint && bun run test && cargo fmt --all -- --check && cargo check && cargo clippy --all-targets -- -D warnings && cargo test"
  }
}
